commit 16a7003bc49a3a8b0168394a04e0910f1583da74
Author: 8xnvekjq <8xnvekjq@users.noreply.github.com>
Date:   Fri Jul 10 19:28:33 2026 +0900

    Fix: Exclude single-syllable false positive matches and clean up derivedGrade classification logic in App.tsx

diff --git a/src/App.tsx b/src/App.tsx
index 1dbd6c7..089b4a6 100644
--- a/src/App.tsx
+++ b/src/App.tsx
@@ -171,21 +171,39 @@ function App() {
               chapterTitle: t.chapter_title
             }));
 
-          // ?쒕ぉ?대굹 ?ㅻ챸???띿뒪?몃? ?듯빐 怨듯넻?섑븰1 / 怨듯넻?섑븰2 / ???怨쇰ぉ 遺꾨쪟 ?먮떒
+          // 1. ?ㅻ챸(description)?먯꽌 紐낆떆??怨쇰ぉ紐?異붿텧 (?섎뱶肄붾뵫 諛곗젣 諛??뺥솗??蹂댁옣)
           let derivedGrade = '湲고?';
-          const matchPool = (l.title + ' ' + (l.description || '')).toLowerCase();
-          if (matchPool.includes('怨듭닔2') || matchPool.includes('怨듯넻?섑븰2')) {
-            derivedGrade = '怨듯넻?섑븰2';
-          } else if (matchPool.includes('?뺥넻') || matchPool.includes('?뺣쪧怨쇳넻怨?) || matchPool.includes('?뺣쪧怨??듦퀎')) {
-            derivedGrade = '?뺣쪧怨??듦퀎';
-          } else if (matchPool.includes('怨듭닔1') || matchPool.includes('怨듯넻?섑븰1') || matchPool.includes('怨?')) {
-            derivedGrade = '怨듯넻?섑븰1';
-          } else if (matchPool.includes('???)) {
-            derivedGrade = '???;
-          } else if (matchPool.includes('誘몄쟻遺꾟뀻') || matchPool.includes('誘몄쟻遺?') || matchPool.includes('怨?')) {
-            derivedGrade = '誘몄쟻遺꾟뀫';
-          } else if (matchPool.includes('誘몄쟻遺?) || matchPool.includes('??') || matchPool.includes('洹뱁븳') || matchPool.includes('誘몃텇') || matchPool.includes('?곷텇') || matchPool.includes('怨?')) {
-            derivedGrade = '誘몄쟻遺꾟뀪';
+          const desc = l.description || '';
+          const knownGrades = ['怨듯넻?섑븰1', '怨듯넻?섑븰2', '???, '誘몄쟻遺꾟뀪', '誘몄쟻遺꾟뀫', '?뺣쪧怨??듦퀎', '湲고븯', '以?-1', '以?-2'];
+          for (const grade of knownGrades) {
+            if (desc.includes(grade)) {
+              derivedGrade = grade;
+              break;
+            }
+          }
+
+          // 2. ?ㅻ챸???놁쓣 ???쒕ぉ/?ㅼ썙??遺꾩꽍 留ㅽ븨 (踰붿슜 '怨?', '怨?' ???섎뱶肄붾뵫 諛곗젣)
+          if (derivedGrade === '湲고?') {
+            const matchPool = (l.title + ' ' + desc).toLowerCase().replace(/\s+/g, '');
+            if (matchPool.includes('怨듯넻?섑븰2') || matchPool.includes('怨듭닔2')) {
+              derivedGrade = '怨듯넻?섑븰2';
+            } else if (matchPool.includes('怨듯넻?섑븰1') || matchPool.includes('怨듭닔1')) {
+              derivedGrade = '怨듯넻?섑븰1';
+            } else if (matchPool.includes('?뺣쪧怨쇳넻怨?) || matchPool.includes('?뺥넻')) {
+              derivedGrade = '?뺣쪧怨??듦퀎';
+            } else if (matchPool.includes('???)) {
+              derivedGrade = '???;
+            } else if (matchPool.includes('誘몄쟻遺꾟뀻') || matchPool.includes('誘몄쟻遺?')) {
+              derivedGrade = '誘몄쟻遺꾟뀫';
+            } else if (matchPool.includes('誘몄쟻遺꾟뀺') || matchPool.includes('誘몄쟻遺?') || matchPool.includes('誘몄쟻遺?)) {
+              derivedGrade = '誘몄쟻遺꾟뀪';
+            } else if (matchPool.includes('湲고븯')) {
+              derivedGrade = '湲고븯';
+            } else if (matchPool.includes('以?-1') || matchPool.includes('以?1')) {
+              derivedGrade = '以?-1';
+            } else if (matchPool.includes('以?-2') || matchPool.includes('以?2')) {
+              derivedGrade = '以?-2';
+            }
           }
 
           return {

commit b3a3d24b0148b29053290cda2097b5a836413682
Author: 8xnvekjq <8xnvekjq@users.noreply.github.com>
Date:   Fri Jul 10 19:17:29 2026 +0900

    Fix: Add isPrintMode prop to LaTeXRenderer to force uniform font sizes and clean layouts for printed PDFs

diff --git a/src/App.tsx b/src/App.tsx
index e472060..1dbd6c7 100644
--- a/src/App.tsx
+++ b/src/App.tsx
@@ -976,7 +976,7 @@ function App() {
                   {/* 臾몄젣 ?곸뿭 (?띿뒪???먮뒗 ?대?吏) */}
                   {isTextPrint && hasProblemText ? (
                     <div className="text-[10px] text-slate-900 leading-relaxed font-sans select-text whitespace-pre-line py-1 border border-slate-100 rounded px-2 bg-slate-50/30">
-                      <LaTeXRenderer text={entry.analysis!.problemText || ''} className="text-[10px] text-slate-900 leading-relaxed" />
+                      <LaTeXRenderer text={entry.analysis!.problemText || ''} className="text-[10px] text-slate-900 leading-relaxed" isPrintMode={true} />
                     </div>
                   ) : (
                     <img src={entry.imageUrl} alt={entry.title} />

commit 27a4c6d9fd17e23412e80f38ba16372933f6b087
Author: 8xnvekjq <8xnvekjq@users.noreply.github.com>
Date:   Fri Jul 10 18:31:02 2026 +0900

    Feat: Add review date stamps below 1st, 2nd, and 3rd review checkmarks

diff --git a/src/App.tsx b/src/App.tsx
index c677c40..e472060 100644
--- a/src/App.tsx
+++ b/src/App.tsx
@@ -551,26 +551,79 @@ function App() {
   // Update reviews list in Supabase & local state
   const handleUpdateReviews = async (id: string, newReviews: ReviewState[]) => {
     try {
+      const targetEntry = mistakes.find(m => m.id === id);
+      if (!targetEntry) return;
+
+      const currentDates = [...(targetEntry.analysis?.reviewDates || ['', '', ''])];
+      const oldReviews = targetEntry.reviews || ['', '', ''];
+
+      for (let i = 0; i < 3; i++) {
+        if (newReviews[i] !== '' && oldReviews[i] === '') {
+          const now = new Date();
+          const dateStr = `${now.getMonth() + 1}/${now.getDate()}`;
+          currentDates[i] = dateStr;
+        } else if (newReviews[i] === '') {
+          currentDates[i] = '';
+        }
+      }
+
+      const updatedAnalysis: MistakeAnalysis = {
+        solvingProcess: targetEntry.analysis?.solvingProcess || '',
+        ...targetEntry.analysis,
+        reviewDates: currentDates
+      };
+
       const { error } = await supabase
         .from('mistakes')
         .update({ 
           reviews: newReviews,
+          analysis: updatedAnalysis,
           updated_at: new Date().toISOString()
         })
         .eq('id', id);
 
       if (error) throw error;
 
-      setMistakes(prev => prev.map(m => m.id === id ? { ...m, reviews: newReviews } : m));
-      setSelectedEntry(prev => prev && prev.id === id ? { ...prev, reviews: newReviews } : prev);
+      const updatedEntry = {
+        ...targetEntry,
+        reviews: newReviews,
+        analysis: updatedAnalysis
+      };
+
+      setMistakes(prev => prev.map(m => m.id === id ? updatedEntry : m));
+      setSelectedEntry(prev => prev && prev.id === id ? updatedEntry : prev);
       
       // Refresh peer activities locally
       fetchPeerActivities();
     } catch (err: any) {
       console.error('Failed to update reviews:', err);
       // Fallback: update local React state anyway for immediate validation
-      setMistakes(prev => prev.map(m => m.id === id ? { ...m, reviews: newReviews } : m));
-      setSelectedEntry(prev => prev && prev.id === id ? { ...prev, reviews: newReviews } : prev);
+      const targetEntry = mistakes.find(m => m.id === id);
+      if (targetEntry) {
+        const currentDates = [...(targetEntry.analysis?.reviewDates || ['', '', ''])];
+        const oldReviews = targetEntry.reviews || ['', '', ''];
+        for (let i = 0; i < 3; i++) {
+          if (newReviews[i] !== '' && oldReviews[i] === '') {
+            const now = new Date();
+            const dateStr = `${now.getMonth() + 1}/${now.getDate()}`;
+            currentDates[i] = dateStr;
+          } else if (newReviews[i] === '') {
+            currentDates[i] = '';
+          }
+        }
+        const updatedAnalysis: MistakeAnalysis = {
+          solvingProcess: targetEntry.analysis?.solvingProcess || '',
+          ...targetEntry.analysis,
+          reviewDates: currentDates
+        };
+        const updatedEntry = {
+          ...targetEntry,
+          reviews: newReviews,
+          analysis: updatedAnalysis
+        };
+        setMistakes(prev => prev.map(m => m.id === id ? updatedEntry : m));
+        setSelectedEntry(prev => prev && prev.id === id ? updatedEntry : prev);
+      }
     }
   };
 

commit 370575717e37ef686f387576377bed1393d60555
Author: 8xnvekjq <8xnvekjq@users.noreply.github.com>
Date:   Fri Jul 10 14:53:50 2026 +0900

    Fix: Unify font sizes inside LaTeXRenderer and defer print status update until after print dialog closes

diff --git a/src/App.tsx b/src/App.tsx
index 2266625..c677c40 100644
--- a/src/App.tsx
+++ b/src/App.tsx
@@ -381,19 +381,22 @@ function App() {
   };
 
   // ?꾨즺 ?ㅻ떟 ?쇨큵 ?몄뇙 ?몃━嫄??몃뱾??-  const handlePrintCompleted = async () => {
+  const handlePrintCompleted = () => {
     const completedList = mistakes.filter(m => m.reviews?.filter(r => r === 'O').length === 3);
     const toPrint = completedList.filter(m => selectedPrintIds.includes(m.id));
     if (toPrint.length === 0) return;
 
     setPrintItems(toPrint);
 
-    // 異쒕젰 ?꾨즺 ?곹깭 DB 諛?濡쒖뺄 ?깊겕
-    await handleMarkAsPrinted(toPrint.map(m => m.id));
-
     // React媛 DOM??留덉슫?명븯怨??대?吏瑜??곸옱???뚭퉴吏 250ms ?湲????몄뇙 ?ㅼ씠?쇰줈洹??ㅽ뻾
-    setTimeout(() => {
+    setTimeout(async () => {
+      // 1. ?몄뇙 ?ㅼ씠?쇰줈洹??꾩? (?ъ슜???몄뇙 ?≪뀡 ?湲?
       window.print();
+      
+      // 2. ?몄뇙李쎌씠 ?ロ엺 ?꾩뿉 鍮꾨줈???몄뇙 ?꾨즺(printed: true) 泥섎━ 諛?DB/?곹깭 ?숆린??+      await handleMarkAsPrinted(toPrint.map(m => m.id));
+      
+      // 3. ?몄뇙 ?꾩떆 ?꾩씠??珥덇린??       setPrintItems(null);
     }, 250);
   };

commit bf599495af6a4c56f6a441a797caf09c34db50bf
Author: 8xnvekjq <8xnvekjq@users.noreply.github.com>
Date:   Fri Jul 10 14:49:56 2026 +0900

    Fix: Import MistakeAnalysis type in App.tsx to resolve TS2304 compilation error

diff --git a/src/App.tsx b/src/App.tsx
index 6a2ec2d..2266625 100644
--- a/src/App.tsx
+++ b/src/App.tsx
@@ -1,5 +1,5 @@
 import { useState, useEffect, useMemo, useRef } from 'react';
-import type { ActiveTab, MistakeEntry, ReviewState } from './types';
+import type { ActiveTab, MistakeEntry, ReviewState, MistakeAnalysis } from './types';
 import { ROOT_CAUSE_OPTIONS, GRADE_LIST } from './types';
 import { CameraScanner } from './components/CameraScanner';
 import { analyzeMistakeWithGemini } from './services/gemini';

commit 47ff9e91a9acfc16295ca95acd6c11aec5651cc4
Author: 8xnvekjq <8xnvekjq@users.noreply.github.com>
Date:   Fri Jul 10 14:49:26 2026 +0900

    Fix: Add printed property to MistakeAnalysis interface and resolve TypeScript type casting mismatches

diff --git a/src/App.tsx b/src/App.tsx
index d8ed15f..6a2ec2d 100644
--- a/src/App.tsx
+++ b/src/App.tsx
@@ -404,7 +404,8 @@ function App() {
       const promises = mistakes
         .filter(m => ids.includes(m.id))
         .map(async (m) => {
-          const updatedAnalysis = {
+          const updatedAnalysis: MistakeAnalysis = {
+            solvingProcess: m.analysis?.solvingProcess || '',
             ...m.analysis,
             printed: true
           };

commit a3c2bd92029e6cfa1854f6bd2a44c4a48d6f1941
Author: 8xnvekjq <8xnvekjq@users.noreply.github.com>
Date:   Fri Jul 10 14:48:25 2026 +0900

    Feat: Add printable element selection checkboxes, automatic printed status markers, and unified LaTeX plain text fonts

diff --git a/src/App.tsx b/src/App.tsx
index 7068ff9..d8ed15f 100644
--- a/src/App.tsx
+++ b/src/App.tsx
@@ -1,4 +1,4 @@
-import { useState, useEffect, useMemo } from 'react';
+import { useState, useEffect, useMemo, useRef } from 'react';
 import type { ActiveTab, MistakeEntry, ReviewState } from './types';
 import { ROOT_CAUSE_OPTIONS, GRADE_LIST } from './types';
 import { CameraScanner } from './components/CameraScanner';
@@ -45,6 +45,22 @@ function App() {
   const [printItems, setPrintItems] = useState<MistakeEntry[] | null>(null);
   // 媛쒕퀎 ?ㅻ떟???몄뇙 ?뺤떇 ?곹깭 (id -> true: ?띿뒪?몃줈 ?몄뇙, false/undefined: ?대?吏濡??몄뇙)
   const [printAsTextMap, setPrintAsTextMap] = useState<Record<string, boolean>>({});
+  // ?몄뇙???꾩씠???좏깮 ID 紐⑸줉 ?곹깭
+  const [selectedPrintIds, setSelectedPrintIds] = useState<string[]>([]);
+  
+  const prevTabRef = useRef(activeTab);
+
+  // 3李?蹂듭뒿 ?꾨즺 蹂닿??????쒖꽦?????꾩쭅 ?몄뇙?섏? ?딆? 移대뱶留??뷀뤃?몃줈 泥댄겕 ?좏깮
+  useEffect(() => {
+    if (activeTab === 'completed' && prevTabRef.current !== 'completed') {
+      const completedList = mistakes.filter(m => m.reviews?.filter(r => r === 'O').length === 3);
+      const unprintedIds = completedList
+        .filter(m => !m.analysis?.printed)
+        .map(m => m.id);
+      setSelectedPrintIds(unprintedIds);
+    }
+    prevTabRef.current = activeTab;
+  }, [activeTab, mistakes]);
 
   // ?대쾲二??붿슂??00:00 KST UTC 寃쎄퀎 ?좎쭨 援ы븯湲?(???ㅼ떆媛?二쇨컙 ?먯닔 怨꾩궛??
   const myWeeklyScore = useMemo(() => {
@@ -365,11 +381,15 @@ function App() {
   };
 
   // ?꾨즺 ?ㅻ떟 ?쇨큵 ?몄뇙 ?몃━嫄??몃뱾??-  const handlePrintCompleted = () => {
+  const handlePrintCompleted = async () => {
     const completedList = mistakes.filter(m => m.reviews?.filter(r => r === 'O').length === 3);
-    if (completedList.length === 0) return;
+    const toPrint = completedList.filter(m => selectedPrintIds.includes(m.id));
+    if (toPrint.length === 0) return;
 
-    setPrintItems(completedList);
+    setPrintItems(toPrint);
+
+    // 異쒕젰 ?꾨즺 ?곹깭 DB 諛?濡쒖뺄 ?깊겕
+    await handleMarkAsPrinted(toPrint.map(m => m.id));
 
     // React媛 DOM??留덉슫?명븯怨??대?吏瑜??곸옱???뚭퉴吏 250ms ?湲????몄뇙 ?ㅼ씠?쇰줈洹??ㅽ뻾
     setTimeout(() => {
@@ -378,6 +398,59 @@ function App() {
     }, 250);
   };
 
+  // ?몄뇙 ?꾨즺 泥섎━ (Supabase DB 諛?濡쒖뺄 ?곹깭 ?숆린??
+  const handleMarkAsPrinted = async (ids: string[]) => {
+    try {
+      const promises = mistakes
+        .filter(m => ids.includes(m.id))
+        .map(async (m) => {
+          const updatedAnalysis = {
+            ...m.analysis,
+            printed: true
+          };
+
+          const { error } = await supabase
+            .from('mistakes')
+            .update({
+              analysis: updatedAnalysis
+            })
+            .eq('id', m.id);
+
+          if (error) throw error;
+          return { id: m.id, updatedAnalysis };
+        });
+
+      const results = await Promise.all(promises);
+
+      setMistakes(prev => prev.map(m => {
+        const match = results.find(r => r.id === m.id);
+        if (match) {
+          return { ...m, analysis: match.updatedAnalysis };
+        }
+        return m;
+      }));
+    } catch (err) {
+      console.error('Failed to mark as printed:', err);
+    }
+  };
+
+  // ?몄뇙 ?좏깮 ?좉? ?몃뱾??+  const handleTogglePrintSelect = (id: string) => {
+    setSelectedPrintIds(prev =>
+      prev.includes(id) ? prev.filter(x => x !== id) : [...prev, id]
+    );
+  };
+
+  // ?몄뇙 ?꾩껜 ?좏깮/?댁젣 ?몃뱾??+  const handleToggleAllPrintSelect = () => {
+    const completedList = mistakes.filter(m => m.reviews?.filter(r => r === 'O').length === 3);
+    if (selectedPrintIds.length === completedList.length) {
+      setSelectedPrintIds([]);
+    } else {
+      setSelectedPrintIds(completedList.map(m => m.id));
+    }
+  };
+
   // ?몄뇙 紐⑤뱶 (?띿뒪???대?吏) ?좉? ?몃뱾??   const handleTogglePrintAsText = (id: string) => {
     setPrintAsTextMap(prev => ({ ...prev, [id]: !prev[id] }));
@@ -650,6 +723,9 @@ function App() {
             peerActivities={peerActivities}
             printAsTextMap={printAsTextMap}
             onTogglePrintAsText={handleTogglePrintAsText}
+            selectedPrintIds={selectedPrintIds}
+            onTogglePrintSelect={handleTogglePrintSelect}
+            onToggleAllPrintSelect={handleToggleAllPrintSelect}
           />
         )}
 

commit d313b8c78a808b34d708aa946afbe2683ad05f7e
Author: 8xnvekjq <8xnvekjq@users.noreply.github.com>
Date:   Fri Jul 10 14:44:31 2026 +0900

    Fix: Add fallback string to problemText parameter to resolve TS2322 compile time error

diff --git a/src/App.tsx b/src/App.tsx
index 78ddcfd..7068ff9 100644
--- a/src/App.tsx
+++ b/src/App.tsx
@@ -843,7 +843,7 @@ function App() {
                   {/* 臾몄젣 ?곸뿭 (?띿뒪???먮뒗 ?대?吏) */}
                   {isTextPrint && hasProblemText ? (
                     <div className="text-[10px] text-slate-900 leading-relaxed font-sans select-text whitespace-pre-line py-1 border border-slate-100 rounded px-2 bg-slate-50/30">
-                      <LaTeXRenderer text={entry.analysis!.problemText} className="text-[10px] text-slate-900 leading-relaxed" />
+                      <LaTeXRenderer text={entry.analysis!.problemText || ''} className="text-[10px] text-slate-900 leading-relaxed" />
                     </div>
                   ) : (
                     <img src={entry.imageUrl} alt={entry.title} />

commit 524918c41811e20381c98c52de573c14d4c676d2
Author: 8xnvekjq <8xnvekjq@users.noreply.github.com>
Date:   Fri Jul 10 14:43:46 2026 +0900

    Feat: Add print mode toggles (Text/Image) for individual mistakes in the completed reviews tab

diff --git a/src/App.tsx b/src/App.tsx
index e9f56c0..78ddcfd 100644
--- a/src/App.tsx
+++ b/src/App.tsx
@@ -14,6 +14,7 @@ import { BottomNavigation } from './components/BottomNavigation';
 import { ImageCropper } from './components/ImageCropper';
 import { AdminPanel } from './components/AdminPanel';
 import { StudentGuide } from './components/StudentGuide';
+import { LaTeXRenderer } from './components/LaTeXRenderer';
 
 function App() {
   // If Supabase credentials are not configured, block and show the setup guide
@@ -42,6 +43,8 @@ function App() {
   const [peerActivities, setPeerActivities] = useState<any[]>([]);
   // ?몄뇙???꾨즺 ?ㅻ떟 由ъ뒪???꾩떆 蹂닿? ?곹깭
   const [printItems, setPrintItems] = useState<MistakeEntry[] | null>(null);
+  // 媛쒕퀎 ?ㅻ떟???몄뇙 ?뺤떇 ?곹깭 (id -> true: ?띿뒪?몃줈 ?몄뇙, false/undefined: ?대?吏濡??몄뇙)
+  const [printAsTextMap, setPrintAsTextMap] = useState<Record<string, boolean>>({});
 
   // ?대쾲二??붿슂??00:00 KST UTC 寃쎄퀎 ?좎쭨 援ы븯湲?(???ㅼ떆媛?二쇨컙 ?먯닔 怨꾩궛??
   const myWeeklyScore = useMemo(() => {
@@ -375,6 +378,11 @@ function App() {
     }, 250);
   };
 
+  // ?몄뇙 紐⑤뱶 (?띿뒪???대?吏) ?좉? ?몃뱾??+  const handleTogglePrintAsText = (id: string) => {
+    setPrintAsTextMap(prev => ({ ...prev, [id]: !prev[id] }));
+  };
+
   // Process crop completion, upload to Storage, and insert database record
   const handleCropComplete = async (croppedBase64: string) => {
     setTempCapturedImage(null);
@@ -640,6 +648,8 @@ function App() {
             currentUserId={session?.user?.id}
             viewMode="list"
             peerActivities={peerActivities}
+            printAsTextMap={printAsTextMap}
+            onTogglePrintAsText={handleTogglePrintAsText}
           />
         )}
 
@@ -820,6 +830,9 @@ function App() {
                 ? new Date(entry.date).toLocaleDateString('ko-KR', { year: '2-digit', month: '2-digit', day: '2-digit' }).replace(/\s/g, '')
                 : '??;
               
+              const isTextPrint = printAsTextMap[entry.id];
+              const hasProblemText = !!entry.analysis?.problemText;
+
               return (
                 <div key={entry.id} className="print-card-item">
                   {/* ?⑥젙???ㅻ뜑 ?대젰 諛?*/}
@@ -827,8 +840,14 @@ function App() {
                     <span className="font-bold text-slate-800">[{entry.grade || '怨듯넻'} ??{entry.chapter || '湲고?'}] {cleanTitle}...</span>
                     <span>?깅줉: {formattedDate}</span>
                   </div>
-                  {/* 臾몄젣 ?대?吏 */}
-                  <img src={entry.imageUrl} alt={entry.title} />
+                  {/* 臾몄젣 ?곸뿭 (?띿뒪???먮뒗 ?대?吏) */}
+                  {isTextPrint && hasProblemText ? (
+                    <div className="text-[10px] text-slate-900 leading-relaxed font-sans select-text whitespace-pre-line py-1 border border-slate-100 rounded px-2 bg-slate-50/30">
+                      <LaTeXRenderer text={entry.analysis!.problemText} className="text-[10px] text-slate-900 leading-relaxed" />
+                    </div>
+                  ) : (
+                    <img src={entry.imageUrl} alt={entry.title} />
+                  )}
                 </div>
               );
             })}

commit d607cf0bed37861c748b5797086af6173edfbc72
Author: ReviewNotes Developer <developer@reviewnotes.local>
Date:   Fri Jul 10 13:25:05 2026 +0900

    Feat: Implement columnar A4 print feature with vertical dashed lines and history header bar

diff --git a/src/App.tsx b/src/App.tsx
index ca8bfa9..e9f56c0 100644
--- a/src/App.tsx
+++ b/src/App.tsx
@@ -40,6 +40,8 @@ function App() {
   const [profilesGradeMap, setProfilesGradeMap] = useState<Record<string, string>>({});
   // ?ㅻⅨ ?숈깮?ㅼ쓽 ?ㅼ떆媛?蹂듭뒿 ?꾪솴 紐⑸줉
   const [peerActivities, setPeerActivities] = useState<any[]>([]);
+  // ?몄뇙???꾨즺 ?ㅻ떟 由ъ뒪???꾩떆 蹂닿? ?곹깭
+  const [printItems, setPrintItems] = useState<MistakeEntry[] | null>(null);
 
   // ?대쾲二??붿슂??00:00 KST UTC 寃쎄퀎 ?좎쭨 援ы븯湲?(???ㅼ떆媛?二쇨컙 ?먯닔 怨꾩궛??
   const myWeeklyScore = useMemo(() => {
@@ -359,6 +361,20 @@ function App() {
     setTempCapturedImage(base64Image);
   };
 
+  // ?꾨즺 ?ㅻ떟 ?쇨큵 ?몄뇙 ?몃━嫄??몃뱾??+  const handlePrintCompleted = () => {
+    const completedList = mistakes.filter(m => m.reviews?.filter(r => r === 'O').length === 3);
+    if (completedList.length === 0) return;
+
+    setPrintItems(completedList);
+
+    // React媛 DOM??留덉슫?명븯怨??대?吏瑜??곸옱???뚭퉴吏 250ms ?湲????몄뇙 ?ㅼ씠?쇰줈洹??ㅽ뻾
+    setTimeout(() => {
+      window.print();
+      setPrintItems(null);
+    }, 250);
+  };
+
   // Process crop completion, upload to Storage, and insert database record
   const handleCropComplete = async (croppedBase64: string) => {
     setTempCapturedImage(null);
@@ -615,6 +631,7 @@ function App() {
             onSelectEntry={(entry) => setSelectedEntry(entry)}
             onDeleteMistake={handleDeleteMistake}
             onAddClick={() => setActiveTab('camera')}
+            onPrintClick={handlePrintCompleted}
             title="蹂듭뒿 ?꾨즺 蹂닿???
             hideAddButton={true}
             emptyMessage="?꾩쭅 ?꾩쟾??蹂듭뒿 ?꾨즺(O 3???ъ꽦)???ㅻ떟???놁뒿?덈떎. ?댁떖???ㅻ떟??蹂듭뒿?섏뿬 ?뺣났??蹂댁꽭??"
@@ -782,6 +799,43 @@ function App() {
       {/* Floating Glassmorphic Bottom Navigation Bar */}
       <BottomNavigation activeTab={activeTab} setActiveTab={setActiveTab} isAdmin={isAdmin} />
 
+      {/* ?몄뇙 ?꾩슜 2???몃줈 援щ텇???덉씠?꾩썐 (@media print ?쒖뿉留??몄텧) */}
+      {printItems && printItems.length > 0 && (
+        <div className="print-only-layout hidden">
+          {/* ?뺢컝???숈뒿吏 ??댄? */}
+          <div className="border-b-2 border-slate-850 pb-2.5 mb-6 flex justify-between items-end">
+            <div>
+              <h1 className="text-base font-extrabold text-slate-900 tracking-tight">?붿퓼?ㅼ닔???ㅻ떟?명듃</h1>
+              <p className="text-[8px] text-slate-500 font-mono mt-0.5">?꾨즺??臾몄젣 紐⑥븘李띻린 ?숈뒿吏</p>
+            </div>
+            <div className="text-right text-[8px] text-slate-500 font-mono">
+              <span>?몄뇙?? {new Date().toLocaleDateString('ko-KR', { year: '2-digit', month: '2-digit', day: '2-digit' }).replace(/\s/g, '')}</span>
+            </div>
+          </div>
+
+          <div className="print-column-wrapper">
+            {printItems.map((entry) => {
+              const cleanTitle = (entry.title || '').replace(/\$[^$]+\$/g, '').replace(/[#*`_]/g, '').slice(0, 16);
+              const formattedDate = entry.date 
+                ? new Date(entry.date).toLocaleDateString('ko-KR', { year: '2-digit', month: '2-digit', day: '2-digit' }).replace(/\s/g, '')
+                : '??;
+              
+              return (
+                <div key={entry.id} className="print-card-item">
+                  {/* ?⑥젙???ㅻ뜑 ?대젰 諛?*/}
+                  <div className="flex justify-between items-center text-[7px] text-slate-500 border-b border-slate-200 pb-1 mb-1.5 font-mono">
+                    <span className="font-bold text-slate-800">[{entry.grade || '怨듯넻'} ??{entry.chapter || '湲고?'}] {cleanTitle}...</span>
+                    <span>?깅줉: {formattedDate}</span>
+                  </div>
+                  {/* 臾몄젣 ?대?吏 */}
+                  <img src={entry.imageUrl} alt={entry.title} />
+                </div>
+              );
+            })}
+          </div>
+        </div>
+      )}
+
     </div>
   );
 }

commit 4c81ae24da9a0b1734cfc13ae5f53809a0a8cafa
Author: ReviewNotes Developer <developer@reviewnotes.local>
Date:   Fri Jul 10 01:59:25 2026 +0900

    Feat: Add collaborative peer review feed to loading phrases and extend text cycle duration to 3s

diff --git a/src/App.tsx b/src/App.tsx
index d0599e6..ca8bfa9 100644
--- a/src/App.tsx
+++ b/src/App.tsx
@@ -756,6 +756,7 @@ function App() {
         <MistakeDetailModal
           selectedEntry={selectedEntry}
           allEntries={mistakes}
+          peerActivities={peerActivities}
           isAnalyzing={isAnalyzing}
           youtubeLectures={youtubeLectures}
           onClose={() => setSelectedEntry(null)}

commit 3c32c28a5d36699739569f6d2ef43fbf3348fe43
Author: ReviewNotes Developer <developer@reviewnotes.local>
Date:   Fri Jul 10 01:48:40 2026 +0900

    Feat: Implement dynamic statistics-based loading phrases during AI analysis

diff --git a/src/App.tsx b/src/App.tsx
index 906190d..d0599e6 100644
--- a/src/App.tsx
+++ b/src/App.tsx
@@ -755,6 +755,7 @@ function App() {
       {selectedEntry && (
         <MistakeDetailModal
           selectedEntry={selectedEntry}
+          allEntries={mistakes}
           isAnalyzing={isAnalyzing}
           youtubeLectures={youtubeLectures}
           onClose={() => setSelectedEntry(null)}

commit 2d7f07dced0e89e26cfcb45b1657527f6b430e03
Author: 8xnvekjq <8xnvekjq@users.noreply.github.com>
Date:   Thu Jul 9 21:34:24 2026 +0900

    Revert: Remove isEditable security restrictions and Fix React state reset bug using wasAnalyzingRef hook in MistakeDetailModal

diff --git a/src/App.tsx b/src/App.tsx
index 3078762..906190d 100644
--- a/src/App.tsx
+++ b/src/App.tsx
@@ -756,7 +756,6 @@ function App() {
         <MistakeDetailModal
           selectedEntry={selectedEntry}
           isAnalyzing={isAnalyzing}
-          isEditable={isAdmin || selectedEntry.userId === session?.user?.id}
           youtubeLectures={youtubeLectures}
           onClose={() => setSelectedEntry(null)}
           onDeleteMistake={handleDeleteMistake}

commit 2351d10b868f300f7a59b2a1013b0fcd96e610ec
Author: 8xnvekjq <8xnvekjq@users.noreply.github.com>
Date:   Thu Jul 9 21:32:23 2026 +0900

    Feat: Ensure grade and chapter are editable by the owner or admin in the detail modal, while securing inputs and buttons to read-only for other peers

diff --git a/src/App.tsx b/src/App.tsx
index 906190d..3078762 100644
--- a/src/App.tsx
+++ b/src/App.tsx
@@ -756,6 +756,7 @@ function App() {
         <MistakeDetailModal
           selectedEntry={selectedEntry}
           isAnalyzing={isAnalyzing}
+          isEditable={isAdmin || selectedEntry.userId === session?.user?.id}
           youtubeLectures={youtubeLectures}
           onClose={() => setSelectedEntry(null)}
           onDeleteMistake={handleDeleteMistake}

commit 9a2a756cada83b861bf17734b8fb009af5544a0a
Author: 8xnvekjq <8xnvekjq@users.noreply.github.com>
Date:   Thu Jul 9 21:29:38 2026 +0900

    Fix: Populate userId in newly created MistakeEntry, implement a robust normalizeGrade helper in gemini.ts, and add diagnostic logs for analysis

diff --git a/src/App.tsx b/src/App.tsx
index 7a7171a..906190d 100644
--- a/src/App.tsx
+++ b/src/App.tsx
@@ -406,6 +406,7 @@ function App() {
 
       const newEntry: MistakeEntry = {
         id: dbEntry.id,
+        userId: dbEntry.user_id,
         title: dbEntry.title,
         imageUrl: dbEntry.image_url,
         date: dbEntry.date,

commit d59fe7b149d7e92e0cc260c2c188ca986aa8eaf3
Author: ReviewNotes Developer <developer@reviewnotes.local>
Date:   Thu Jul 9 12:48:51 2026 +0900

    Feat: Remove stats insights carousel from loading screen and revert to minified circular timer layout

diff --git a/src/App.tsx b/src/App.tsx
index 938e060..7a7171a 100644
--- a/src/App.tsx
+++ b/src/App.tsx
@@ -754,7 +754,6 @@ function App() {
       {selectedEntry && (
         <MistakeDetailModal
           selectedEntry={selectedEntry}
-          allEntries={mistakes}
           isAnalyzing={isAnalyzing}
           youtubeLectures={youtubeLectures}
           onClose={() => setSelectedEntry(null)}

commit 1c78ce99bb77a1222a2f49fbbf56a8ce98252cc3
Author: ReviewNotes Developer <developer@reviewnotes.local>
Date:   Thu Jul 9 12:41:21 2026 +0900

    Feat: Implement circular progress timer with decelerating simulation and personal statistics carousel on AI loading screen

diff --git a/src/App.tsx b/src/App.tsx
index 7a7171a..938e060 100644
--- a/src/App.tsx
+++ b/src/App.tsx
@@ -754,6 +754,7 @@ function App() {
       {selectedEntry && (
         <MistakeDetailModal
           selectedEntry={selectedEntry}
+          allEntries={mistakes}
           isAnalyzing={isAnalyzing}
           youtubeLectures={youtubeLectures}
           onClose={() => setSelectedEntry(null)}

commit 0c7489839d4e43dae7cbdf5bade9740b88abc776
Author: ReviewNotes Developer <developer@reviewnotes.local>
Date:   Thu Jul 9 01:22:36 2026 +0900

    Feat: Ingest Probability and Statistics playlist, and implement isolated matching for 寃쎌슦????
diff --git a/src/App.tsx b/src/App.tsx
index 7c77c1b..7a7171a 100644
--- a/src/App.tsx
+++ b/src/App.tsx
@@ -155,6 +155,8 @@ function App() {
           const matchPool = (l.title + ' ' + (l.description || '')).toLowerCase();
           if (matchPool.includes('怨듭닔2') || matchPool.includes('怨듯넻?섑븰2')) {
             derivedGrade = '怨듯넻?섑븰2';
+          } else if (matchPool.includes('?뺥넻') || matchPool.includes('?뺣쪧怨쇳넻怨?) || matchPool.includes('?뺣쪧怨??듦퀎')) {
+            derivedGrade = '?뺣쪧怨??듦퀎';
           } else if (matchPool.includes('怨듭닔1') || matchPool.includes('怨듯넻?섑븰1') || matchPool.includes('怨?')) {
             derivedGrade = '怨듯넻?섑븰1';
           } else if (matchPool.includes('???)) {

commit 54c6afcfcc72edad38280f2cffc1feadfaea6ff5
Author: ReviewNotes Developer <developer@reviewnotes.local>
Date:   Thu Jul 9 01:17:34 2026 +0900

    Feat: Ingest Calculus II playlist videos and timelines to SQL generator and support UI matching

diff --git a/src/App.tsx b/src/App.tsx
index 976bd03..7c77c1b 100644
--- a/src/App.tsx
+++ b/src/App.tsx
@@ -159,7 +159,9 @@ function App() {
             derivedGrade = '怨듯넻?섑븰1';
           } else if (matchPool.includes('???)) {
             derivedGrade = '???;
-          } else if (matchPool.includes('誘몄쟻遺?) || matchPool.includes('??') || matchPool.includes('洹뱁븳') || matchPool.includes('誘몃텇') || matchPool.includes('?곷텇')) {
+          } else if (matchPool.includes('誘몄쟻遺꾟뀻') || matchPool.includes('誘몄쟻遺?') || matchPool.includes('怨?')) {
+            derivedGrade = '誘몄쟻遺꾟뀫';
+          } else if (matchPool.includes('誘몄쟻遺?) || matchPool.includes('??') || matchPool.includes('洹뱁븳') || matchPool.includes('誘몃텇') || matchPool.includes('?곷텇') || matchPool.includes('怨?')) {
             derivedGrade = '誘몄쟻遺꾟뀪';
           }
 

commit e35dae0487d9ea07c0b4846f8f9c7ac83a37191a
Author: ReviewNotes Developer <developer@reviewnotes.local>
Date:   Thu Jul 9 01:09:52 2026 +0900

    Feat: Ingest Calculus I playlist videos, titles, and timelines to SQL generator and support UI matching

diff --git a/src/App.tsx b/src/App.tsx
index 55ee6e5..976bd03 100644
--- a/src/App.tsx
+++ b/src/App.tsx
@@ -159,6 +159,8 @@ function App() {
             derivedGrade = '怨듯넻?섑븰1';
           } else if (matchPool.includes('???)) {
             derivedGrade = '???;
+          } else if (matchPool.includes('誘몄쟻遺?) || matchPool.includes('??') || matchPool.includes('洹뱁븳') || matchPool.includes('誘몃텇') || matchPool.includes('?곷텇')) {
+            derivedGrade = '誘몄쟻遺꾟뀪';
           }
 
           return {

commit 144b72febddca927d6f7058becf15cdd817d9997
Author: ReviewNotes Developer <developer@reviewnotes.local>
Date:   Thu Jul 9 00:49:51 2026 +0900

    Feat: Add Algebra YouTube lectures seeding script and real-time mapping logic

diff --git a/src/App.tsx b/src/App.tsx
index a9f8c22..55ee6e5 100644
--- a/src/App.tsx
+++ b/src/App.tsx
@@ -150,13 +150,15 @@ function App() {
               chapterTitle: t.chapter_title
             }));
 
-          // ?쒕ぉ?대굹 ?ㅻ챸???띿뒪?몃? ?듯빐 怨듯넻?섑븰1 / 怨듯넻?섑븰2 怨쇰ぉ 遺꾨쪟 ?먮떒
+          // ?쒕ぉ?대굹 ?ㅻ챸???띿뒪?몃? ?듯빐 怨듯넻?섑븰1 / 怨듯넻?섑븰2 / ???怨쇰ぉ 遺꾨쪟 ?먮떒
           let derivedGrade = '湲고?';
           const matchPool = (l.title + ' ' + (l.description || '')).toLowerCase();
           if (matchPool.includes('怨듭닔2') || matchPool.includes('怨듯넻?섑븰2')) {
             derivedGrade = '怨듯넻?섑븰2';
           } else if (matchPool.includes('怨듭닔1') || matchPool.includes('怨듯넻?섑븰1') || matchPool.includes('怨?')) {
             derivedGrade = '怨듯넻?섑븰1';
+          } else if (matchPool.includes('???)) {
+            derivedGrade = '???;
           }
 
           return {

commit c8ec34858ceea8431c2b481d164b1d470a8b99ad
Author: 8xnvekjq <8xnvekjq@users.noreply.github.com>
Date:   Wed Jul 8 20:57:49 2026 +0900

    Fix: Preserve student-edited root causes and action plan when starting and completing AI analysis

diff --git a/src/App.tsx b/src/App.tsx
index 163b40e..a9f8c22 100644
--- a/src/App.tsx
+++ b/src/App.tsx
@@ -319,14 +319,16 @@ function App() {
   const runAnalysisFlow = async (entry: MistakeEntry, apiKey: string, studentGrade?: string) => {
     const result = await analyzeMistakeWithGemini(entry.imageUrl, apiKey, youtubeLectures, studentGrade);
     
-    // Update mistake database record (including auto-classified grade/chapter)
+    // Update mistake database record (including auto-classified grade/chapter and preserving student edits)
     const { error: updateError } = await supabase
       .from('mistakes')
       .update({
         title: result.title,
         analysis: result.analysis,
-        grade: result.grade || null,
-        chapter: result.chapter || null,
+        grade: result.grade || entry.grade || null,
+        chapter: result.chapter || entry.chapter || null,
+        root_causes: entry.rootCauses || [],
+        user_action_plan: entry.userActionPlan || null,
       })
       .eq('id', entry.id);
 
@@ -336,8 +338,8 @@ function App() {
       ...entry,
       title: result.title,
       analysis: result.analysis,
-      grade: result.grade,
-      chapter: result.chapter,
+      grade: result.grade || entry.grade,
+      chapter: result.chapter || entry.chapter,
     };
 
     setMistakes(prev => prev.map(m => m.id === entry.id ? updatedEntry : m));

commit eb412e9e76bc3bd564150894c08b86024e117ad4
Author: 8xnvekjq <8xnvekjq@users.noreply.github.com>
Date:   Tue Jul 7 19:02:18 2026 +0900

    Fix: Make Gemini API key fallback robust by attempting paidKey on any error from freeKey

diff --git a/src/App.tsx b/src/App.tsx
index d409981..163b40e 100644
--- a/src/App.tsx
+++ b/src/App.tsx
@@ -296,22 +296,15 @@ function App() {
       if (freeKey) {
         try {
           await runAnalysisFlow(entry, freeKey, studentGrade);
-        } catch (err: any) {
-          const errorMsg = err?.message || '';
-          const isQuotaError = 
-            errorMsg.includes('429') || 
-            errorMsg.toUpperCase().includes('RESOURCE_EXHAUSTED') || 
-            errorMsg.includes('quota') || 
-            errorMsg.includes('limit');
-
-          if (paidKey && isQuotaError) {
-            console.warn('Free API Key limit hit. Retrying with Paid API Key...');
+        } catch (err) {
+          console.warn('Free API Key failed. Retrying with Paid API Key...', err);
+          if (paidKey) {
             await runAnalysisFlow(entry, paidKey, studentGrade);
           } else {
             throw err;
           }
         }
-      } else {
+      } else if (paidKey) {
         await runAnalysisFlow(entry, paidKey, studentGrade);
       }
     } catch (err: any) {

commit c7e584413772eb4a88327edd66ec5b61a4610282
Author: 8xnvekjq <8xnvekjq@users.noreply.github.com>
Date:   Tue Jul 7 17:29:50 2026 +0900

    Feat: Add real-time peer review activities widget to the Completed tab

diff --git a/src/App.tsx b/src/App.tsx
index 6c517bf..d409981 100644
--- a/src/App.tsx
+++ b/src/App.tsx
@@ -38,6 +38,8 @@ function App() {
   const [statsStudentFilter, setStatsStudentFilter] = useState<string>('all');
   // userId -> schoolGrade map (AI ?숇뀈蹂?遺꾨쪟 理쒖쟻?붿슜)
   const [profilesGradeMap, setProfilesGradeMap] = useState<Record<string, string>>({});
+  // ?ㅻⅨ ?숈깮?ㅼ쓽 ?ㅼ떆媛?蹂듭뒿 ?꾪솴 紐⑸줉
+  const [peerActivities, setPeerActivities] = useState<any[]>([]);
 
   // ?대쾲二??붿슂??00:00 KST UTC 寃쎄퀎 ?좎쭨 援ы븯湲?(???ㅼ떆媛?二쇨컙 ?먯닔 怨꾩궛??
   const myWeeklyScore = useMemo(() => {
@@ -212,6 +214,7 @@ function App() {
   const fetchUserData = async () => {
     try {
       loadWeeklyChampion(); // 理쒖떊 梨뷀뵾???뺣낫 ?숆린??+      fetchPeerActivities(); // ?ㅼ떆媛?移쒓뎄??蹂듭뒿 ?꾪솴 濡쒕뱶
       // Fetch all mistakes (RLS handles filtering: normal users see own, admin sees all)
       const { data: dbMistakes, error: mistakesError } = await supabase
         .from('mistakes')
@@ -255,6 +258,28 @@ function App() {
     }
   };
 
+  // Fetch recent peer review activities from read-only VIEW
+  const fetchPeerActivities = async () => {
+    try {
+      const { data, error } = await supabase
+        .from('recent_peer_activities')
+        .select('*')
+        .limit(10);
+
+      if (error) throw error;
+      setPeerActivities(data || []);
+    } catch (err) {
+      console.error('Failed to load peer activities:', err);
+    }
+  };
+
+  // Refresh peer activities when Completed reviews tab is opened
+  useEffect(() => {
+    if (activeTab === 'completed' && session?.user) {
+      fetchPeerActivities();
+    }
+  }, [activeTab, session]);
+
   // Start analysis trigger with automatic paid fallback
   const handleStartAnalysis = async (entry: MistakeEntry) => {
     const freeKey = import.meta.env.VITE_GEMINI_API_KEY || '';
@@ -423,13 +448,19 @@ function App() {
     try {
       const { error } = await supabase
         .from('mistakes')
-        .update({ reviews: newReviews })
+        .update({ 
+          reviews: newReviews,
+          updated_at: new Date().toISOString()
+        })
         .eq('id', id);
 
       if (error) throw error;
 
       setMistakes(prev => prev.map(m => m.id === id ? { ...m, reviews: newReviews } : m));
       setSelectedEntry(prev => prev && prev.id === id ? { ...prev, reviews: newReviews } : prev);
+      
+      // Refresh peer activities locally
+      fetchPeerActivities();
     } catch (err: any) {
       console.error('Failed to update reviews:', err);
       // Fallback: update local React state anyway for immediate validation
@@ -587,6 +618,7 @@ function App() {
             profilesMap={profilesMap}
             currentUserId={session?.user?.id}
             viewMode="list"
+            peerActivities={peerActivities}
           />
         )}
 

commit d4a930c72d6a3afcf4a3ffcd9f0ddbc812cbfc88
Author: 8xnvekjq <8xnvekjq@users.noreply.github.com>
Date:   Tue Jul 7 17:26:57 2026 +0900

    Feat: Redesign Completed tab to display a compact list view with grade and chapter dropdown filters

diff --git a/src/App.tsx b/src/App.tsx
index 8f3b8ed..6c517bf 100644
--- a/src/App.tsx
+++ b/src/App.tsx
@@ -586,6 +586,7 @@ function App() {
             isAdmin={isAdmin}
             profilesMap={profilesMap}
             currentUserId={session?.user?.id}
+            viewMode="list"
           />
         )}
 

commit 2e70bb69f8e96fc82c850e90c094c3a63b1cadfb
Author: 8xnvekjq <8xnvekjq@users.noreply.github.com>
Date:   Tue Jul 7 15:12:04 2026 +0900

    Feat: Implement student school grade tracking and Gemini AI classification bias tuning

diff --git a/src/App.tsx b/src/App.tsx
index a379d1f..8f3b8ed 100644
--- a/src/App.tsx
+++ b/src/App.tsx
@@ -36,6 +36,8 @@ function App() {
   const [weeklyChampion, setWeeklyChampion] = useState<any>(null);
   // 遺꾩꽍?듦퀎 ???숈깮 ?꾪꽣 ?곹깭 (?대뱶誘??꾩슜)
   const [statsStudentFilter, setStatsStudentFilter] = useState<string>('all');
+  // userId -> schoolGrade map (AI ?숇뀈蹂?遺꾨쪟 理쒖쟻?붿슜)
+  const [profilesGradeMap, setProfilesGradeMap] = useState<Record<string, string>>({});
 
   // ?대쾲二??붿슂??00:00 KST UTC 寃쎄퀎 ?좎쭨 援ы븯湲?(???ㅼ떆媛?二쇨컙 ?먯닔 怨꾩궛??
   const myWeeklyScore = useMemo(() => {
@@ -218,18 +220,21 @@ function App() {
 
       if (mistakesError) throw mistakesError;
 
-      // Fetch all profiles for admin name mapping (display_name ?ы븿)
+      // Fetch all profiles for admin name mapping (display_name, school_grade ?ы븿)
       const { data: profiles } = await supabase
         .from('profiles')
-        .select('id, email, display_name');
+        .select('id, email, display_name, school_grade');
 
       const pMap: Record<string, string> = {};
+      const gMap: Record<string, string> = {};
       (profiles || []).forEach((p: any) => {
         const username = p.email?.split('@')[0] || p.id.slice(0, 8);
         const displayName = p.display_name?.trim();
         pMap[p.id] = displayName ? `${displayName} (${username})` : username;
+        gMap[p.id] = p.school_grade || '';
       });
       setProfilesMap(pMap);
+      setProfilesGradeMap(gMap);
 
       const mappedMistakes: MistakeEntry[] = (dbMistakes || []).map((m: any) => ({
         id: m.id,
@@ -262,9 +267,10 @@ function App() {
 
     setIsAnalyzing(true);
     try {
+      const studentGrade = entry.userId ? (profilesGradeMap[entry.userId] || '') : '';
       if (freeKey) {
         try {
-          await runAnalysisFlow(entry, freeKey);
+          await runAnalysisFlow(entry, freeKey, studentGrade);
         } catch (err: any) {
           const errorMsg = err?.message || '';
           const isQuotaError = 
@@ -275,13 +281,13 @@ function App() {
 
           if (paidKey && isQuotaError) {
             console.warn('Free API Key limit hit. Retrying with Paid API Key...');
-            await runAnalysisFlow(entry, paidKey);
+            await runAnalysisFlow(entry, paidKey, studentGrade);
           } else {
             throw err;
           }
         }
       } else {
-        await runAnalysisFlow(entry, paidKey);
+        await runAnalysisFlow(entry, paidKey, studentGrade);
       }
     } catch (err: any) {
       console.error(err);
@@ -292,8 +298,8 @@ function App() {
   };
 
   // Gemini API analysis flow & database updating
-  const runAnalysisFlow = async (entry: MistakeEntry, apiKey: string) => {
-    const result = await analyzeMistakeWithGemini(entry.imageUrl, apiKey, youtubeLectures);
+  const runAnalysisFlow = async (entry: MistakeEntry, apiKey: string, studentGrade?: string) => {
+    const result = await analyzeMistakeWithGemini(entry.imageUrl, apiKey, youtubeLectures, studentGrade);
     
     // Update mistake database record (including auto-classified grade/chapter)
     const { error: updateError } = await supabase

commit caa648f47a5d2e0091611314069b00789b84892c
Author: 8xnvekjq <8xnvekjq@users.noreply.github.com>
Date:   Tue Jul 7 15:04:06 2026 +0900

    Feat: Add student-specific statistics filtering to the stats tab for admin accounts

diff --git a/src/App.tsx b/src/App.tsx
index eec6a50..a379d1f 100644
--- a/src/App.tsx
+++ b/src/App.tsx
@@ -34,6 +34,8 @@ function App() {
   const [youtubeLectures, setYoutubeLectures] = useState<any[]>([]);
   // 二쇨컙 理쒕떎 ?ㅻ떟 ?꾨즺 梨뷀뵾???곹깭
   const [weeklyChampion, setWeeklyChampion] = useState<any>(null);
+  // 遺꾩꽍?듦퀎 ???숈깮 ?꾪꽣 ?곹깭 (?대뱶誘??꾩슜)
+  const [statsStudentFilter, setStatsStudentFilter] = useState<string>('all');
 
   // ?대쾲二??붿슂??00:00 KST UTC 寃쎄퀎 ?좎쭨 援ы븯湲?(???ㅼ떆媛?二쇨컙 ?먯닔 怨꾩궛??
   const myWeeklyScore = useMemo(() => {
@@ -441,12 +443,19 @@ function App() {
   };
 
   // ?? ?듦퀎 怨꾩궛 ??????????????????????????????????????????????????
+  const filteredMistakesForStats = useMemo(() => {
+    if (isAdmin && statsStudentFilter !== 'all') {
+      return mistakes.filter(m => m.userId === statsStudentFilter);
+    }
+    return mistakes;
+  }, [mistakes, isAdmin, statsStudentFilter]);
+
   const stats = useMemo(() => {
     const gradeCounts: Record<string, number> = {};
     const chapterCounts: Record<string, number> = {};
     const causeCounts: Record<string, number> = {};
 
-    mistakes.forEach(m => {
+    filteredMistakesForStats.forEach(m => {
       if (m.grade) gradeCounts[m.grade] = (gradeCounts[m.grade] || 0) + 1;
       if (m.chapter && m.grade) {
         const key = `${m.grade} > ${m.chapter}`;
@@ -462,7 +471,7 @@ function App() {
     const topChapters = Object.entries(chapterCounts).sort((a, b) => b[1] - a[1]).slice(0, 5);
 
     return { gradeCounts, causeCounts, totalGrade, totalCause, topChapters };
-  }, [mistakes]);
+  }, [filteredMistakesForStats]);
 
   const maskId = (username: string) => {
     if (!username) return '';
@@ -594,10 +603,30 @@ function App() {
           <div className="space-y-6">
             <div>
               <h2 className="text-lg font-bold text-white">?뱤 ?섏쓽 ?쎌젏 遺꾩꽍</h2>
-              <p className="text-xs text-slate-400 mt-0.5">珥?{mistakes.length}媛쒖쓽 ?ㅻ떟 湲곕줉 湲곕컲</p>
+              <p className="text-xs text-slate-400 mt-0.5">珥?{filteredMistakesForStats.length}媛쒖쓽 ?ㅻ떟 湲곕줉 湲곕컲</p>
             </div>
 
-            {mistakes.length === 0 ? (
+            {/* ?대뱶誘쇱씤 寃쎌슦 ?숈깮 ?꾪꽣 ??됲꽣 異붽? */}
+            {isAdmin && (
+              <div className="flex items-center space-x-2 bg-slate-900 border border-slate-800 p-3.5 rounded-2xl shadow-sm">
+                <span className="text-xs text-slate-300 font-extrabold flex-none">?뫀 ?숈깮蹂??듦퀎 議고쉶</span>
+                <select
+                  value={statsStudentFilter}
+                  onChange={e => setStatsStudentFilter(e.target.value)}
+                  className="flex-1 px-3 py-2 rounded-xl bg-slate-950 border border-slate-800 text-xs text-white outline-none focus:border-indigo-500 transition-colors cursor-pointer font-bold"
+                >
+                  <option value="all">?꾩껜 ?숈깮 ?⑷퀎 ({mistakes.length}媛?</option>
+                  {Array.from(new Set(mistakes.map(m => m.userId).filter(Boolean) as string[]))
+                    .map(uid => {
+                      const name = profilesMap[uid] || uid.slice(0, 8);
+                      const cnt = mistakes.filter(m => m.userId === uid).length;
+                      return <option key={uid} value={uid}>{name} ({cnt}媛?</option>;
+                    })}
+                </select>
+              </div>
+            )}
+
+            {filteredMistakesForStats.length === 0 ? (
               <div className="flex flex-col items-center justify-center py-20 text-center border-2 border-dashed border-slate-800 rounded-2xl bg-slate-900/20">
                 <div className="text-3xl mb-3">?뱤</div>
                 <p className="text-slate-300 font-medium text-sm">?꾩쭅 遺꾩꽍???곗씠?곌? ?놁뒿?덈떎</p>

commit d1f70f26bfa497d85f3adefc6971ade5bb07ea5b
Author: ReviewNotes Developer <developer@reviewnotes.local>
Date:   Tue Jul 7 01:38:57 2026 +0900

    fix: fallback to last week's leader when current week's top score is 0

diff --git a/src/App.tsx b/src/App.tsx
index ee84262..eec6a50 100644
--- a/src/App.tsx
+++ b/src/App.tsx
@@ -82,7 +82,7 @@ function App() {
 
       if (error) throw error;
 
-      if (data && data.length > 0) {
+      if (data && data.length > 0 && data[0].score > 0) {
         setWeeklyChampion({
           ...data[0],
           isLastWeek: false

commit 24206a2cc683021197d759df6c7326b5e98ef0c9
Author: ReviewNotes Developer <developer@reviewnotes.local>
Date:   Tue Jul 7 00:07:55 2026 +0900

    Revert "feat: Add experimental native camera capture button inside CameraScanner for admin accounts"
    
    This reverts commit 088cd90701cf0b143904fe673b352c2ce97dc68e.

diff --git a/src/App.tsx b/src/App.tsx
index 52eb42b..ee84262 100644
--- a/src/App.tsx
+++ b/src/App.tsx
@@ -578,7 +578,6 @@ function App() {
           <CameraScanner 
             onCapture={handleCameraCapture}
             onClose={() => setActiveTab('notes')}
-            isAdmin={isAdmin}
           />
         )}
 

commit 088cd90701cf0b143904fe673b352c2ce97dc68e
Author: ReviewNotes Developer <developer@reviewnotes.local>
Date:   Tue Jul 7 00:02:06 2026 +0900

    feat: Add experimental native camera capture button inside CameraScanner for admin accounts

diff --git a/src/App.tsx b/src/App.tsx
index ee84262..52eb42b 100644
--- a/src/App.tsx
+++ b/src/App.tsx
@@ -578,6 +578,7 @@ function App() {
           <CameraScanner 
             onCapture={handleCameraCapture}
             onClose={() => setActiveTab('notes')}
+            isAdmin={isAdmin}
           />
         )}
 

commit c854d2eb897abd54c2bc8f498de361cb26968a1e
Author: ReviewNotes Developer <developer@reviewnotes.local>
Date:   Mon Jul 6 01:17:34 2026 +0900

    style: Enhance gold gradient and glow opacity for weekly champion Hall of Fame banner

diff --git a/src/App.tsx b/src/App.tsx
index d4739d6..ee84262 100644
--- a/src/App.tsx
+++ b/src/App.tsx
@@ -486,7 +486,7 @@ function App() {
           <>
             {/* 二쇨컙 蹂듭뒿??諛곕꼫 (?꾩껜 ?숈깮 ?ㅽ뵂 / 1二??댁썡 諛?由ъ뀑 濡ㅼ삤踰? */}
             {weeklyChampion ? (
-              <div className="bg-slate-900/80 border border-amber-500/20 rounded-2xl p-3 mb-4 flex items-center justify-between shadow-lg shadow-amber-950/10 animate-fade-in">
+              <div className="bg-gradient-to-r from-slate-900/90 via-amber-950/20 to-slate-900/90 border border-amber-500/40 rounded-2xl p-3 mb-4 flex items-center justify-between shadow-[0_0_15px_rgba(245,158,11,0.12)] animate-fade-in">
                 <div className="flex items-center space-x-2.5 min-w-0">
                   <span className="text-xl animate-bounce flex-none">?몣</span>
                   <div className="min-w-0 leading-tight">

commit bba5f2d250423833e7ce24876a919af2b84740fc
Author: ReviewNotes Developer <developer@reviewnotes.local>
Date:   Mon Jul 6 01:15:16 2026 +0900

    feat: Restore original Header title sizes and paddings, and enable score badge visibility for admin accounts

diff --git a/src/App.tsx b/src/App.tsx
index daa41df..d4739d6 100644
--- a/src/App.tsx
+++ b/src/App.tsx
@@ -478,7 +478,7 @@ function App() {
     <div className="h-full flex flex-col bg-slate-950 text-slate-100 select-none">
       
       {/* Top Header */}
-      <Header currentUser={currentUser} onLogout={handleLogout} myScore={isAdmin ? undefined : myWeeklyScore.score} />
+      <Header currentUser={currentUser} onLogout={handleLogout} myScore={myWeeklyScore.score} />
 
       {/* Main Content Area */}
       <main className="flex-1 overflow-y-auto px-4 py-6 pb-28">

commit 58cc641021a9e2c2463049934f3a33e6c06ce63e
Author: ReviewNotes Developer <developer@reviewnotes.local>
Date:   Mon Jul 6 01:11:31 2026 +0900

    feat: Move weekly score badge to Header next to username and clean up bottom navigation ribbon

diff --git a/src/App.tsx b/src/App.tsx
index 725b88e..daa41df 100644
--- a/src/App.tsx
+++ b/src/App.tsx
@@ -478,7 +478,7 @@ function App() {
     <div className="h-full flex flex-col bg-slate-950 text-slate-100 select-none">
       
       {/* Top Header */}
-      <Header currentUser={currentUser} onLogout={handleLogout} />
+      <Header currentUser={currentUser} onLogout={handleLogout} myScore={isAdmin ? undefined : myWeeklyScore.score} />
 
       {/* Main Content Area */}
       <main className="flex-1 overflow-y-auto px-4 py-6 pb-28">
@@ -705,23 +705,6 @@ function App() {
         />
       )}
 
-      {/* ??二쇨컙 ?ㅼ퐫??誘몃땲 ?뚮줈??由щ낯諛?(?쇰컲 ?숈깮 濡쒓렇????BottomNavigation 諛붾줈 ?꾩뿉 ?뚮줈?? */}
-      {!isAdmin && currentUser && (
-        <div className="fixed bottom-[84px] left-4 right-4 z-30 bg-slate-900/95 backdrop-blur-md border border-slate-800/80 rounded-xl px-4 py-1.5 flex items-center justify-between text-[10px] text-slate-400 font-bold shadow-lg shadow-indigo-950/30">
-          <div className="flex items-center space-x-1.5">
-            <span className="text-amber-400">??/span>
-            <span>??二쇨컙 蹂듭뒿 ?꾪솴:</span>
-            <span className="text-white font-extrabold">{myWeeklyScore.completed}媛??꾨즺</span>
-            <span className="text-slate-600">/</span>
-            <span className="text-slate-400">{myWeeklyScore.total}媛??깅줉</span>
-          </div>
-          <div className="flex items-center space-x-1">
-            <span className="text-slate-500">???먯닔:</span>
-            <span className="text-amber-400 font-black text-xs">{myWeeklyScore.score}??/span>
-          </div>
-        </div>
-      )}
-
       {/* Floating Glassmorphic Bottom Navigation Bar */}
       <BottomNavigation activeTab={activeTab} setActiveTab={setActiveTab} isAdmin={isAdmin} />
 

commit a797aaece7ceeeaa03f8502595e93a22ee38c73b
Author: ReviewNotes Developer <developer@reviewnotes.local>
Date:   Mon Jul 6 01:09:24 2026 +0900

    feat: Redesign weekly score ribbon to hover above BottomNavigation and sync changelog/package.json version to 1.8.4

diff --git a/src/App.tsx b/src/App.tsx
index f754a6c..725b88e 100644
--- a/src/App.tsx
+++ b/src/App.tsx
@@ -705,9 +705,9 @@ function App() {
         />
       )}
 
-      {/* ??二쇨컙 ?ㅼ퐫??誘몃땲 由щ낯諛?(?쇰컲 ?숈깮 濡쒓렇?????곸떆 ?몄텧) */}
+      {/* ??二쇨컙 ?ㅼ퐫??誘몃땲 ?뚮줈??由щ낯諛?(?쇰컲 ?숈깮 濡쒓렇????BottomNavigation 諛붾줈 ?꾩뿉 ?뚮줈?? */}
       {!isAdmin && currentUser && (
-        <div className="fixed bottom-[56px] left-0 right-0 z-20 bg-slate-900/95 backdrop-blur-md border-t border-slate-800/80 px-4 py-1.5 flex items-center justify-between text-[10px] text-slate-400 font-bold safe-bottom-offset shadow-lg shadow-indigo-950/20">
+        <div className="fixed bottom-[84px] left-4 right-4 z-30 bg-slate-900/95 backdrop-blur-md border border-slate-800/80 rounded-xl px-4 py-1.5 flex items-center justify-between text-[10px] text-slate-400 font-bold shadow-lg shadow-indigo-950/30">
           <div className="flex items-center space-x-1.5">
             <span className="text-amber-400">??/span>
             <span>??二쇨컙 蹂듭뒿 ?꾪솴:</span>

commit 5a812f4711c4c0bf9737b8ed80195c37ae46536d
Author: ReviewNotes Developer <developer@reviewnotes.local>
Date:   Mon Jul 6 01:04:46 2026 +0900

    feat: Add user weekly score sticky ribbon and integrate last_weekly_leaderboard database query fallback

diff --git a/src/App.tsx b/src/App.tsx
index c4567c5..f754a6c 100644
--- a/src/App.tsx
+++ b/src/App.tsx
@@ -35,6 +35,38 @@ function App() {
   // 二쇨컙 理쒕떎 ?ㅻ떟 ?꾨즺 梨뷀뵾???곹깭
   const [weeklyChampion, setWeeklyChampion] = useState<any>(null);
 
+  // ?대쾲二??붿슂??00:00 KST UTC 寃쎄퀎 ?좎쭨 援ы븯湲?(???ㅼ떆媛?二쇨컙 ?먯닔 怨꾩궛??
+  const myWeeklyScore = useMemo(() => {
+    if (!currentUser || mistakes.length === 0) return { total: 0, completed: 0, score: 0 };
+
+    const now = new Date();
+    const kstTime = new Date(now.getTime() + 9 * 60 * 60 * 1000);
+    const day = kstTime.getUTCDay();
+    const diff = kstTime.getUTCDate() - day + (day === 0 ? -6 : 1);
+    const mondayKst = new Date(Date.UTC(kstTime.getUTCFullYear(), kstTime.getUTCMonth(), diff, 0, 0, 0));
+    const thisMondayUtc = new Date(mondayKst.getTime() - 9 * 60 * 60 * 1000);
+
+    // ?대쾲 二??붿슂???댄썑 ?깅줉??蹂몄씤 ?ㅻ떟 ?꾪꽣留?+    const myWeeklyMistakes = mistakes.filter(m => {
+      const mDate = new Date(m.date);
+      return mDate >= thisMondayUtc;
+    });
+
+    const total = myWeeklyMistakes.length;
+    const completed = myWeeklyMistakes.filter(m => 
+      m.reviews && m.reviews.filter(r => r === 'O').length === 3
+    ).length;
+
+    const rate = total > 0 ? (completed / total) : 0;
+    const score = (completed * 10) + (rate * 100);
+
+    return {
+      total,
+      completed,
+      score: Math.round(score)
+    };
+  }, [currentUser, mistakes]);
+
   // State for image cropping flow
   const [tempCapturedImage, setTempCapturedImage] = useState<string | null>(null);
 
@@ -58,76 +90,21 @@ function App() {
         return;
       }
 
-      // 2. ?대쾲 二??곗씠?곌? ?놁쑝硫?吏?쒖＜(Last Week) 踰붿쐞 怨꾩궛
-      const now = new Date();
-      const kstTime = new Date(now.getTime() + 9 * 60 * 60 * 1000);
-      const day = kstTime.getUTCDay();
-      const diff = kstTime.getUTCDate() - day + (day === 0 ? -6 : 1);
-      const mondayKst = new Date(Date.UTC(kstTime.getUTCFullYear(), kstTime.getUTCMonth(), diff, 0, 0, 0));
-      const thisMondayUtc = new Date(mondayKst.getTime() - 9 * 60 * 60 * 1000); // UTC ?좎쭨
-      const lastMondayUtc = new Date(thisMondayUtc.getTime() - 7 * 24 * 60 * 60 * 1000);
-
-      // 吏?쒖＜ ?붿슂??UTC ~ ?대쾲二??붿슂??UTC 寃쎄퀎???ъ씠???ㅻ떟??荑쇰━
-      const { data: lastWeekMistakes } = await supabase
-        .from('mistakes')
-        .select('user_id, reviews')
-        .gte('created_at', lastMondayUtc.toISOString())
-        .lt('created_at', thisMondayUtc.toISOString());
-
-      if (lastWeekMistakes && lastWeekMistakes.length > 0) {
-        // ?좎? ?꾩씠?붾퀎 吏묎퀎
-        const userStats: Record<string, { total: number; completed: number }> = {};
-        lastWeekMistakes.forEach((m: any) => {
-          if (!m.user_id) return;
-          if (!userStats[m.user_id]) {
-            userStats[m.user_id] = { total: 0, completed: 0 };
-          }
-          userStats[m.user_id].total += 1;
-          const reviews: string[] = m.reviews || [];
-          const isCompleted = reviews.filter(r => r === 'O').length === 3;
-          if (isCompleted) {
-            userStats[m.user_id].completed += 1;
-          }
-        });
+      // 2. ?대쾲 二??곗씠?곌? ?놁쑝硫?吏?쒖＜ ??궧 酉?RLS ?고쉶) 議고쉶
+      const { data: lastWeekData, error: lastWeekError } = await supabase
+        .from('last_weekly_leaderboard')
+        .select('*')
+        .order('score', { ascending: false })
+        .limit(1);
 
-        // 理쒓퀬 ?먯닔 ?곗듅???좎젙
-        let bestUserId = '';
-        let maxScore = 0;
-        let bestStat = { total: 0, completed: 0 };
-
-        Object.entries(userStats).forEach(([uid, stat]) => {
-          const rate = stat.total > 0 ? (stat.completed / stat.total) : 0;
-          const score = (stat.completed * 10) + (rate * 100);
-          // 理쒖냼 0??珥덇낵(?ㅻ떟???꾨즺???대젰)??寃쎌슦?먮쭔 ??궧 吏묎퀎
-          if (score > maxScore && stat.completed > 0) {
-            maxScore = score;
-            bestUserId = uid;
-            bestStat = stat;
-          }
-        });
+      if (lastWeekError) throw lastWeekError;
 
-        if (bestUserId) {
-          // ?곗듅???꾨줈??濡쒕뱶 (?됰꽕???몄텧??
-          const { data: profile } = await supabase
-            .from('profiles')
-            .select('email, display_name')
-            .eq('id', bestUserId)
-            .single();
-
-          if (profile) {
-            const username = profile.email?.split('@')[0] || profile.display_name || bestUserId.slice(0, 8);
-            setWeeklyChampion({
-              username,
-              display_name: profile.display_name?.trim() || undefined,
-              weekly_total_count: bestStat.total,
-              weekly_completed_count: bestStat.completed,
-              completion_rate: bestStat.total > 0 ? (bestStat.completed / bestStat.total) : 0,
-              score: maxScore,
-              isLastWeek: true
-            });
-            return;
-          }
-        }
+      if (lastWeekData && lastWeekData.length > 0 && lastWeekData[0].score > 0) {
+        setWeeklyChampion({
+          ...lastWeekData[0],
+          isLastWeek: true
+        });
+        return;
       }
 
       // 3. 吏?쒖＜留덉? ?먯닔媛 ?놁쑝硫?諛곕꼫瑜?鍮꾩썙 ?숆린遺???좊룄
@@ -504,7 +481,7 @@ function App() {
       <Header currentUser={currentUser} onLogout={handleLogout} />
 
       {/* Main Content Area */}
-      <main className="flex-1 overflow-y-auto px-4 py-6 pb-24">
+      <main className="flex-1 overflow-y-auto px-4 py-6 pb-28">
         {activeTab === 'notes' && (
           <>
             {/* 二쇨컙 蹂듭뒿??諛곕꼫 (?꾩껜 ?숈깮 ?ㅽ뵂 / 1二??댁썡 諛?由ъ뀑 濡ㅼ삤踰? */}
@@ -728,6 +705,23 @@ function App() {
         />
       )}
 
+      {/* ??二쇨컙 ?ㅼ퐫??誘몃땲 由щ낯諛?(?쇰컲 ?숈깮 濡쒓렇?????곸떆 ?몄텧) */}
+      {!isAdmin && currentUser && (
+        <div className="fixed bottom-[56px] left-0 right-0 z-20 bg-slate-900/95 backdrop-blur-md border-t border-slate-800/80 px-4 py-1.5 flex items-center justify-between text-[10px] text-slate-400 font-bold safe-bottom-offset shadow-lg shadow-indigo-950/20">
+          <div className="flex items-center space-x-1.5">
+            <span className="text-amber-400">??/span>
+            <span>??二쇨컙 蹂듭뒿 ?꾪솴:</span>
+            <span className="text-white font-extrabold">{myWeeklyScore.completed}媛??꾨즺</span>
+            <span className="text-slate-600">/</span>
+            <span className="text-slate-400">{myWeeklyScore.total}媛??깅줉</span>
+          </div>
+          <div className="flex items-center space-x-1">
+            <span className="text-slate-500">???먯닔:</span>
+            <span className="text-amber-400 font-black text-xs">{myWeeklyScore.score}??/span>
+          </div>
+        </div>
+      )}
+
       {/* Floating Glassmorphic Bottom Navigation Bar */}
       <BottomNavigation activeTab={activeTab} setActiveTab={setActiveTab} isAdmin={isAdmin} />
 

commit 593c18bbaa07666a8ab891d7a95a770422a66174
Author: ReviewNotes Developer <developer@reviewnotes.local>
Date:   Mon Jul 6 01:01:22 2026 +0900

    feat: Implement weekly champion 1-week rollover and empty state fallback banner

diff --git a/src/App.tsx b/src/App.tsx
index e580cf6..c4567c5 100644
--- a/src/App.tsx
+++ b/src/App.tsx
@@ -38,9 +38,10 @@ function App() {
   // State for image cropping flow
   const [tempCapturedImage, setTempCapturedImage] = useState<string | null>(null);
 
-  // 二쇨컙 理쒕떎 ?ㅻ떟 ?꾨즺 梨뷀뵾???뺣낫 濡쒕뱶
+  // 二쇨컙 理쒕떎 ?ㅻ떟 ?꾨즺 梨뷀뵾???뺣낫 濡쒕뱶 (?섏씠釉뚮━??1二??댁썡 諛?由ъ뀑 濡ㅼ삤踰?
   const loadWeeklyChampion = async () => {
     try {
+      // 1. ?대쾲 二???궧 酉?議고쉶
       const { data, error } = await supabase
         .from('weekly_leaderboard')
         .select('*')
@@ -50,28 +51,90 @@ function App() {
       if (error) throw error;
 
       if (data && data.length > 0) {
-        setWeeklyChampion(data[0]);
-      } else {
-        // Fallback: 酉곗뿉 ?곗씠?곌? ?녿뒗 寃쎌슦
         setWeeklyChampion({
-          username: 'test',
-          display_name: '?띻만??,
-          weekly_total_count: 5,
-          weekly_completed_count: 3,
-          completion_rate: 0.6,
-          score: 90
+          ...data[0],
+          isLastWeek: false
+        });
+        return;
+      }
+
+      // 2. ?대쾲 二??곗씠?곌? ?놁쑝硫?吏?쒖＜(Last Week) 踰붿쐞 怨꾩궛
+      const now = new Date();
+      const kstTime = new Date(now.getTime() + 9 * 60 * 60 * 1000);
+      const day = kstTime.getUTCDay();
+      const diff = kstTime.getUTCDate() - day + (day === 0 ? -6 : 1);
+      const mondayKst = new Date(Date.UTC(kstTime.getUTCFullYear(), kstTime.getUTCMonth(), diff, 0, 0, 0));
+      const thisMondayUtc = new Date(mondayKst.getTime() - 9 * 60 * 60 * 1000); // UTC ?좎쭨
+      const lastMondayUtc = new Date(thisMondayUtc.getTime() - 7 * 24 * 60 * 60 * 1000);
+
+      // 吏?쒖＜ ?붿슂??UTC ~ ?대쾲二??붿슂??UTC 寃쎄퀎???ъ씠???ㅻ떟??荑쇰━
+      const { data: lastWeekMistakes } = await supabase
+        .from('mistakes')
+        .select('user_id, reviews')
+        .gte('created_at', lastMondayUtc.toISOString())
+        .lt('created_at', thisMondayUtc.toISOString());
+
+      if (lastWeekMistakes && lastWeekMistakes.length > 0) {
+        // ?좎? ?꾩씠?붾퀎 吏묎퀎
+        const userStats: Record<string, { total: number; completed: number }> = {};
+        lastWeekMistakes.forEach((m: any) => {
+          if (!m.user_id) return;
+          if (!userStats[m.user_id]) {
+            userStats[m.user_id] = { total: 0, completed: 0 };
+          }
+          userStats[m.user_id].total += 1;
+          const reviews: string[] = m.reviews || [];
+          const isCompleted = reviews.filter(r => r === 'O').length === 3;
+          if (isCompleted) {
+            userStats[m.user_id].completed += 1;
+          }
         });
+
+        // 理쒓퀬 ?먯닔 ?곗듅???좎젙
+        let bestUserId = '';
+        let maxScore = 0;
+        let bestStat = { total: 0, completed: 0 };
+
+        Object.entries(userStats).forEach(([uid, stat]) => {
+          const rate = stat.total > 0 ? (stat.completed / stat.total) : 0;
+          const score = (stat.completed * 10) + (rate * 100);
+          // 理쒖냼 0??珥덇낵(?ㅻ떟???꾨즺???대젰)??寃쎌슦?먮쭔 ??궧 吏묎퀎
+          if (score > maxScore && stat.completed > 0) {
+            maxScore = score;
+            bestUserId = uid;
+            bestStat = stat;
+          }
+        });
+
+        if (bestUserId) {
+          // ?곗듅???꾨줈??濡쒕뱶 (?됰꽕???몄텧??
+          const { data: profile } = await supabase
+            .from('profiles')
+            .select('email, display_name')
+            .eq('id', bestUserId)
+            .single();
+
+          if (profile) {
+            const username = profile.email?.split('@')[0] || profile.display_name || bestUserId.slice(0, 8);
+            setWeeklyChampion({
+              username,
+              display_name: profile.display_name?.trim() || undefined,
+              weekly_total_count: bestStat.total,
+              weekly_completed_count: bestStat.completed,
+              completion_rate: bestStat.total > 0 ? (bestStat.completed / bestStat.total) : 0,
+              score: maxScore,
+              isLastWeek: true
+            });
+            return;
+          }
+        }
       }
+
+      // 3. 吏?쒖＜留덉? ?먯닔媛 ?놁쑝硫?諛곕꼫瑜?鍮꾩썙 ?숆린遺???좊룄
+      setWeeklyChampion(null);
     } catch (err) {
-      // Fallback
-      setWeeklyChampion({
-        username: 'test',
-        display_name: '?띻만??,
-        weekly_total_count: 5,
-        weekly_completed_count: 3,
-        completion_rate: 0.6,
-        score: 90
-      });
+      console.error('loadWeeklyChampion failed:', err);
+      setWeeklyChampion(null);
     }
   };
 
@@ -444,13 +507,15 @@ function App() {
       <main className="flex-1 overflow-y-auto px-4 py-6 pb-24">
         {activeTab === 'notes' && (
           <>
-            {/* 二쇨컙 蹂듭뒿??諛곕꼫 (?꾩껜 ?숈깮 ?ㅽ뵂) */}
-            {weeklyChampion && (
+            {/* 二쇨컙 蹂듭뒿??諛곕꼫 (?꾩껜 ?숈깮 ?ㅽ뵂 / 1二??댁썡 諛?由ъ뀑 濡ㅼ삤踰? */}
+            {weeklyChampion ? (
               <div className="bg-slate-900/80 border border-amber-500/20 rounded-2xl p-3 mb-4 flex items-center justify-between shadow-lg shadow-amber-950/10 animate-fade-in">
                 <div className="flex items-center space-x-2.5 min-w-0">
                   <span className="text-xl animate-bounce flex-none">?몣</span>
                   <div className="min-w-0 leading-tight">
-                    <div className="text-[10px] text-amber-400 font-bold uppercase tracking-wider">?대쾲二?理쒕떎 ?ㅻ떟 ?꾨즺</div>
+                    <div className="text-[10px] text-amber-400 font-bold uppercase tracking-wider">
+                      {weeklyChampion.isLastWeek ? '吏?쒖＜ 蹂듭뒿 MVP' : '?대쾲二?理쒕떎 ?ㅻ떟 ?꾨즺'}
+                    </div>
                     <div className="flex items-center space-x-1.5 mt-0.5 min-w-0">
                       <span className="text-xs font-black text-white truncate">
                         {weeklyChampion.display_name 
@@ -458,7 +523,7 @@ function App() {
                           : `${maskId(weeklyChampion.username)} ?숈깮`}
                       </span>
                       <span className="text-[8px] font-black bg-amber-500/10 text-amber-300 border border-amber-500/20 px-1.5 py-0.5 rounded-full flex items-center flex-none">
-                        狩?二쇨컙 MVP
+                        {weeklyChampion.isLastWeek ? '狩?吏?쒖＜ MVP' : '狩?二쇨컙 MVP'}
                       </span>
                     </div>
                   </div>
@@ -473,6 +538,24 @@ function App() {
                   </span>
                 </div>
               </div>
+            ) : (
+              /* ??二??곗냽 蹂듭뒿 ?꾨즺?먭? ?꾪? ?놁쓣 ?? ?숆린遺??怨듬갚 諛곕꼫 ?쒓났 */
+              <div className="bg-slate-900/40 border border-slate-800/80 rounded-2xl p-3 mb-4 flex items-center justify-between shadow-lg shadow-indigo-950/5 animate-fade-in">
+                <div className="flex items-center space-x-2.5 min-w-0">
+                  <span className="text-xl animate-pulse flex-none">?몣</span>
+                  <div className="min-w-0 leading-tight">
+                    <div className="text-[10px] text-slate-500 font-bold uppercase tracking-wider">蹂듭뒿??由ш렇</div>
+                    <div className="text-xs font-bold text-slate-300 mt-0.5">
+                      ?대쾲二?泥?蹂듭뒿?뺤쓽 二쇱씤怨듭씠 ?섏뼱蹂댁꽭??
+                    </div>
+                  </div>
+                </div>
+                <div className="text-right flex-none pl-3 border-l border-slate-800/50">
+                  <span className="text-[8px] text-indigo-400 font-black bg-indigo-500/10 px-2 py-0.5 rounded-full border border-indigo-500/20">
+                    ?꾩쟾 ?湲?以?+                  </span>
+                </div>
+              </div>
             )}
 
             <MistakeList

commit 076f75d68c354304f28f2aa076db843fcf0d4cfd
Author: ReviewNotes Developer <developer@reviewnotes.local>
Date:   Sun Jul 5 22:02:58 2026 +0900

    feat: Implement Option 1 high-precision AI-driven YouTube lecture matching system

diff --git a/src/App.tsx b/src/App.tsx
index 873d614..e580cf6 100644
--- a/src/App.tsx
+++ b/src/App.tsx
@@ -251,7 +251,7 @@ function App() {
 
   // Gemini API analysis flow & database updating
   const runAnalysisFlow = async (entry: MistakeEntry, apiKey: string) => {
-    const result = await analyzeMistakeWithGemini(entry.imageUrl, apiKey);
+    const result = await analyzeMistakeWithGemini(entry.imageUrl, apiKey, youtubeLectures);
     
     // Update mistake database record (including auto-classified grade/chapter)
     const { error: updateError } = await supabase

commit a60255b822e0679ae5477298674be3cbabc81e7e
Author: ReviewNotes Developer <developer@reviewnotes.local>
Date:   Sun Jul 5 20:47:57 2026 +0900

    feat: Hide weekly champion username, display only displayName and attach weekly MVP badge

diff --git a/src/App.tsx b/src/App.tsx
index 78f6c44..873d614 100644
--- a/src/App.tsx
+++ b/src/App.tsx
@@ -451,10 +451,15 @@ function App() {
                   <span className="text-xl animate-bounce flex-none">?몣</span>
                   <div className="min-w-0 leading-tight">
                     <div className="text-[10px] text-amber-400 font-bold uppercase tracking-wider">?대쾲二?理쒕떎 ?ㅻ떟 ?꾨즺</div>
-                    <div className="text-xs font-extrabold text-white truncate mt-0.5">
-                      {weeklyChampion.display_name 
-                        ? `${maskId(weeklyChampion.username)}(${weeklyChampion.display_name})` 
-                        : maskId(weeklyChampion.username)}
+                    <div className="flex items-center space-x-1.5 mt-0.5 min-w-0">
+                      <span className="text-xs font-black text-white truncate">
+                        {weeklyChampion.display_name 
+                          ? `${weeklyChampion.display_name} ?숈깮` 
+                          : `${maskId(weeklyChampion.username)} ?숈깮`}
+                      </span>
+                      <span className="text-[8px] font-black bg-amber-500/10 text-amber-300 border border-amber-500/20 px-1.5 py-0.5 rounded-full flex items-center flex-none">
+                        狩?二쇨컙 MVP
+                      </span>
                     </div>
                   </div>
                 </div>

commit 958d6ffab94286fb0dbef76fe186c08a9c8b6b16
Author: ReviewNotes Developer <developer@reviewnotes.local>
Date:   Sun Jul 5 20:44:11 2026 +0900

    feat: Release weekly champion leaderboard banner to all users globally

diff --git a/src/App.tsx b/src/App.tsx
index d176430..78f6c44 100644
--- a/src/App.tsx
+++ b/src/App.tsx
@@ -444,8 +444,8 @@ function App() {
       <main className="flex-1 overflow-y-auto px-4 py-6 pb-24">
         {activeTab === 'notes' && (
           <>
-            {/* 二쇨컙 蹂듭뒿??諛곕꼫 (test ?숈깮 ?꾩슜 移대굹由?濡ㅼ븘?? */}
-            {currentUser === 'test' && weeklyChampion && (
+            {/* 二쇨컙 蹂듭뒿??諛곕꼫 (?꾩껜 ?숈깮 ?ㅽ뵂) */}
+            {weeklyChampion && (
               <div className="bg-slate-900/80 border border-amber-500/20 rounded-2xl p-3 mb-4 flex items-center justify-between shadow-lg shadow-amber-950/10 animate-fade-in">
                 <div className="flex items-center space-x-2.5 min-w-0">
                   <span className="text-xl animate-bounce flex-none">?몣</span>

commit 099d4773b444c1194ebf2614299b81179b4595d2
Author: ReviewNotes Developer <developer@reviewnotes.local>
Date:   Sun Jul 5 20:40:50 2026 +0900

    feat: Implement student ID privacy masking and enable weekly score ranking and indicators in AdminPanel

diff --git a/src/App.tsx b/src/App.tsx
index 4502f4e..d176430 100644
--- a/src/App.tsx
+++ b/src/App.tsx
@@ -424,6 +424,12 @@ function App() {
     return { gradeCounts, causeCounts, totalGrade, totalCause, topChapters };
   }, [mistakes]);
 
+  const maskId = (username: string) => {
+    if (!username) return '';
+    if (username.length <= 3) return username;
+    return username.slice(0, 3) + '*'.repeat(username.length - 3);
+  };
+
   if (!currentUser) {
     return <AuthScreen onLogin={(username) => setCurrentUser(username)} />;
   }
@@ -447,8 +453,8 @@ function App() {
                     <div className="text-[10px] text-amber-400 font-bold uppercase tracking-wider">?대쾲二?理쒕떎 ?ㅻ떟 ?꾨즺</div>
                     <div className="text-xs font-extrabold text-white truncate mt-0.5">
                       {weeklyChampion.display_name 
-                        ? `${weeklyChampion.username}(${weeklyChampion.display_name})` 
-                        : weeklyChampion.username}
+                        ? `${maskId(weeklyChampion.username)}(${weeklyChampion.display_name})` 
+                        : maskId(weeklyChampion.username)}
                     </div>
                   </div>
                 </div>

commit 1f07d9a39216dc4d47fa38d23c782b7b1da0c8b5
Author: ReviewNotes Developer <developer@reviewnotes.local>
Date:   Sun Jul 5 20:36:29 2026 +0900

    feat: Add weekly champion banner at the top of Mistakes view for test student canary rollout

diff --git a/src/App.tsx b/src/App.tsx
index f6cb935..4502f4e 100644
--- a/src/App.tsx
+++ b/src/App.tsx
@@ -32,10 +32,49 @@ function App() {
   const [profilesMap, setProfilesMap] = useState<Record<string, string>>({});
   // ?좏뒠釉?留ㅼ묶??媛뺤쓽 留덉뒪??由ъ뒪???곹깭
   const [youtubeLectures, setYoutubeLectures] = useState<any[]>([]);
+  // 二쇨컙 理쒕떎 ?ㅻ떟 ?꾨즺 梨뷀뵾???곹깭
+  const [weeklyChampion, setWeeklyChampion] = useState<any>(null);
 
   // State for image cropping flow
   const [tempCapturedImage, setTempCapturedImage] = useState<string | null>(null);
 
+  // 二쇨컙 理쒕떎 ?ㅻ떟 ?꾨즺 梨뷀뵾???뺣낫 濡쒕뱶
+  const loadWeeklyChampion = async () => {
+    try {
+      const { data, error } = await supabase
+        .from('weekly_leaderboard')
+        .select('*')
+        .order('score', { ascending: false })
+        .limit(1);
+
+      if (error) throw error;
+
+      if (data && data.length > 0) {
+        setWeeklyChampion(data[0]);
+      } else {
+        // Fallback: 酉곗뿉 ?곗씠?곌? ?녿뒗 寃쎌슦
+        setWeeklyChampion({
+          username: 'test',
+          display_name: '?띻만??,
+          weekly_total_count: 5,
+          weekly_completed_count: 3,
+          completion_rate: 0.6,
+          score: 90
+        });
+      }
+    } catch (err) {
+      // Fallback
+      setWeeklyChampion({
+        username: 'test',
+        display_name: '?띻만??,
+        weekly_total_count: 5,
+        weekly_completed_count: 3,
+        completion_rate: 0.6,
+        score: 90
+      });
+    }
+  };
+
   // Check admin status from profiles table
   const fetchAdminStatus = async (userId: string) => {
     try {
@@ -99,6 +138,7 @@ function App() {
         fetchUserData();
         fetchAdminStatus(session.user.id);
         loadYoutubeLectures(); // ?좏뒠釉?媛뺤쓽 ?곗씠??濡쒕뱶
+        loadWeeklyChampion(); // 二쇨컙 梨뷀뵾??濡쒕뱶
       }
     });
 
@@ -111,11 +151,13 @@ function App() {
         fetchUserData();
         fetchAdminStatus(session.user.id);
         loadYoutubeLectures(); // ?좏뒠釉?媛뺤쓽 ?곗씠??濡쒕뱶
+        loadWeeklyChampion(); // 二쇨컙 梨뷀뵾??濡쒕뱶
       } else {
         setCurrentUser('');
         setIsAdmin(false);
         setMistakes([]);
         setYoutubeLectures([]);
+        setWeeklyChampion(null);
       }
     });
 
@@ -125,6 +167,7 @@ function App() {
   // Fetch mistakes from Supabase
   const fetchUserData = async () => {
     try {
+      loadWeeklyChampion(); // 理쒖떊 梨뷀뵾???뺣낫 ?숆린??       // Fetch all mistakes (RLS handles filtering: normal users see own, admin sees all)
       const { data: dbMistakes, error: mistakesError } = await supabase
         .from('mistakes')
@@ -394,10 +437,37 @@ function App() {
       {/* Main Content Area */}
       <main className="flex-1 overflow-y-auto px-4 py-6 pb-24">
         {activeTab === 'notes' && (
-          <MistakeList
-            mistakes={[...mistakes]
-              .filter(m => !(m.reviews?.filter(r => r === 'O').length === 3))
-              .sort((a, b) => {
+          <>
+            {/* 二쇨컙 蹂듭뒿??諛곕꼫 (test ?숈깮 ?꾩슜 移대굹由?濡ㅼ븘?? */}
+            {currentUser === 'test' && weeklyChampion && (
+              <div className="bg-slate-900/80 border border-amber-500/20 rounded-2xl p-3 mb-4 flex items-center justify-between shadow-lg shadow-amber-950/10 animate-fade-in">
+                <div className="flex items-center space-x-2.5 min-w-0">
+                  <span className="text-xl animate-bounce flex-none">?몣</span>
+                  <div className="min-w-0 leading-tight">
+                    <div className="text-[10px] text-amber-400 font-bold uppercase tracking-wider">?대쾲二?理쒕떎 ?ㅻ떟 ?꾨즺</div>
+                    <div className="text-xs font-extrabold text-white truncate mt-0.5">
+                      {weeklyChampion.display_name 
+                        ? `${weeklyChampion.username}(${weeklyChampion.display_name})` 
+                        : weeklyChampion.username}
+                    </div>
+                  </div>
+                </div>
+                <div className="text-right flex-none pl-3 border-l border-slate-800/80">
+                  <span className="text-[10px] text-slate-500 block">醫낇빀 ?먯닔</span>
+                  <span className="text-xs font-black text-amber-400">
+                    {Math.round(weeklyChampion.score)}??+                  </span>
+                  <span className="text-[9px] text-slate-400 block mt-0.5">
+                    ?꾨즺 {weeklyChampion.weekly_completed_count}媛?({Math.round(weeklyChampion.completion_rate * 100)}%)
+                  </span>
+                </div>
+              </div>
+            )}
+
+            <MistakeList
+              mistakes={[...mistakes]
+                .filter(m => !(m.reviews?.filter(r => r === 'O').length === 3))
+                .sort((a, b) => {
                 const aStruggles = a.reviews ? a.reviews.filter(r => r === 'X' || r === 'star').length : 0;
                 const bStruggles = b.reviews ? b.reviews.filter(r => r === 'X' || r === 'star').length : 0;
                 
@@ -415,6 +485,7 @@ function App() {
             profilesMap={profilesMap}
             currentUserId={session?.user?.id}
           />
+          </>
         )}
 
         {activeTab === 'completed' && (

commit 4a6eb04afea00ebd1f0a4c36a935317f8341dd40
Author: ReviewNotes Developer <developer@reviewnotes.local>
Date:   Sun Jul 5 20:23:23 2026 +0900

    feat: Add Student Guide tab and refactor Admin Panel to use sub-tabs without mistake images

diff --git a/src/App.tsx b/src/App.tsx
index 9eaab89..f6cb935 100644
--- a/src/App.tsx
+++ b/src/App.tsx
@@ -13,6 +13,7 @@ import { MistakeDetailModal } from './components/MistakeDetailModal';
 import { BottomNavigation } from './components/BottomNavigation';
 import { ImageCropper } from './components/ImageCropper';
 import { AdminPanel } from './components/AdminPanel';
+import { StudentGuide } from './components/StudentGuide';
 
 function App() {
   // If Supabase credentials are not configured, block and show the setup guide
@@ -442,6 +443,10 @@ function App() {
           <AdminPanel />
         )}
 
+        {activeTab === 'guide' && !isAdmin && (
+          <StudentGuide />
+        )}
+
         {/* ?? 遺꾩꽍?듦퀎 ???? */}
         {activeTab === 'stats' && (
           <div className="space-y-6">

commit d5e6db9ad227260aa83037a96155d01d3e4e1d19
Author: ReviewNotes Developer <developer@reviewnotes.local>
Date:   Sun Jul 5 04:06:23 2026 +0900

    feat: Connect Supabase YouTube lectures to all students and implement dynamic matching

diff --git a/src/App.tsx b/src/App.tsx
index b3c73fb..9eaab89 100644
--- a/src/App.tsx
+++ b/src/App.tsx
@@ -29,6 +29,8 @@ function App() {
   const [isAnalyzing, setIsAnalyzing] = useState(false);
   // userId -> displayName map (admin ?꾩슜)
   const [profilesMap, setProfilesMap] = useState<Record<string, string>>({});
+  // ?좏뒠釉?留ㅼ묶??媛뺤쓽 留덉뒪??由ъ뒪???곹깭
+  const [youtubeLectures, setYoutubeLectures] = useState<any[]>([]);
 
   // State for image cropping flow
   const [tempCapturedImage, setTempCapturedImage] = useState<string | null>(null);
@@ -47,6 +49,44 @@ function App() {
     }
   };
 
+  // Supabase濡쒕???55媛?媛뺤쓽 諛???꾨씪???뺣낫瑜???踰덉뿉 ?쎌뼱? 留ㅼ묶??援ъ“濡?媛怨?+  const loadYoutubeLectures = async () => {
+    try {
+      const { data: lectures } = await supabase.from('youtube_lectures').select('*');
+      const { data: timelines } = await supabase.from('youtube_timelines').select('*').order('start_seconds', { ascending: true });
+
+      if (lectures) {
+        const mapped = lectures.map(l => {
+          const chapters = (timelines || [])
+            .filter(t => t.lecture_id === l.id)
+            .map(t => ({
+              startSeconds: t.start_seconds,
+              chapterTitle: t.chapter_title
+            }));
+
+          // ?쒕ぉ?대굹 ?ㅻ챸???띿뒪?몃? ?듯빐 怨듯넻?섑븰1 / 怨듯넻?섑븰2 怨쇰ぉ 遺꾨쪟 ?먮떒
+          let derivedGrade = '湲고?';
+          const matchPool = (l.title + ' ' + (l.description || '')).toLowerCase();
+          if (matchPool.includes('怨듭닔2') || matchPool.includes('怨듯넻?섑븰2')) {
+            derivedGrade = '怨듯넻?섑븰2';
+          } else if (matchPool.includes('怨듭닔1') || matchPool.includes('怨듯넻?섑븰1') || matchPool.includes('怨?')) {
+            derivedGrade = '怨듯넻?섑븰1';
+          }
+
+          return {
+            videoId: l.video_id,
+            title: l.title,
+            grade: derivedGrade,
+            chapters: chapters.length > 0 ? chapters : undefined
+          };
+        });
+        setYoutubeLectures(mapped);
+      }
+    } catch (err) {
+      console.error('Error loading youtube lectures:', err);
+    }
+  };
+
   // Monitor Supabase Authentication States
   useEffect(() => {
     // 1. Get initial session
@@ -57,6 +97,7 @@ function App() {
         setCurrentUser(username);
         fetchUserData();
         fetchAdminStatus(session.user.id);
+        loadYoutubeLectures(); // ?좏뒠釉?媛뺤쓽 ?곗씠??濡쒕뱶
       }
     });
 
@@ -68,10 +109,12 @@ function App() {
         setCurrentUser(username);
         fetchUserData();
         fetchAdminStatus(session.user.id);
+        loadYoutubeLectures(); // ?좏뒠釉?媛뺤쓽 ?곗씠??濡쒕뱶
       } else {
         setCurrentUser('');
         setIsAdmin(false);
         setMistakes([]);
+        setYoutubeLectures([]);
       }
     });
 
@@ -494,7 +537,7 @@ function App() {
         <MistakeDetailModal
           selectedEntry={selectedEntry}
           isAnalyzing={isAnalyzing}
-          currentUser={currentUser}
+          youtubeLectures={youtubeLectures}
           onClose={() => setSelectedEntry(null)}
           onDeleteMistake={handleDeleteMistake}
           onStartAnalysis={handleStartAnalysis}

commit 104eb98322ad503eb115a1e557017ca028c503c3
Author: ReviewNotes Developer <developer@reviewnotes.local>
Date:   Sun Jul 5 02:59:40 2026 +0900

    feat: deploy youtube recommendation card exclusively for student test

diff --git a/src/App.tsx b/src/App.tsx
index e558ce6..b3c73fb 100644
--- a/src/App.tsx
+++ b/src/App.tsx
@@ -494,6 +494,7 @@ function App() {
         <MistakeDetailModal
           selectedEntry={selectedEntry}
           isAnalyzing={isAnalyzing}
+          currentUser={currentUser}
           onClose={() => setSelectedEntry(null)}
           onDeleteMistake={handleDeleteMistake}
           onStartAnalysis={handleStartAnalysis}

commit 28fc615bd0dc9069366bf7e2a1a86950ded4946c
Author: ReviewNotes Developer <developer@reviewnotes.local>
Date:   Sun Jul 5 01:48:19 2026 +0900

    feat: Show display_name alongside username ID in admin panel and note badges

diff --git a/src/App.tsx b/src/App.tsx
index 38f3aec..e558ce6 100644
--- a/src/App.tsx
+++ b/src/App.tsx
@@ -89,14 +89,16 @@ function App() {
 
       if (mistakesError) throw mistakesError;
 
-      // Fetch all profiles for admin name mapping
+      // Fetch all profiles for admin name mapping (display_name ?ы븿)
       const { data: profiles } = await supabase
         .from('profiles')
-        .select('id, email');
+        .select('id, email, display_name');
 
       const pMap: Record<string, string> = {};
       (profiles || []).forEach((p: any) => {
-        pMap[p.id] = p.email?.split('@')[0] || p.id.slice(0, 8);
+        const username = p.email?.split('@')[0] || p.id.slice(0, 8);
+        const displayName = p.display_name?.trim();
+        pMap[p.id] = displayName ? `${displayName} (${username})` : username;
       });
       setProfilesMap(pMap);
 

commit 228632133716a4000211bb7f43299ade864c0da8
Author: ReviewNotes Developer <developer@reviewnotes.local>
Date:   Sat Jul 4 13:01:01 2026 +0900

    feat: Admin can view all students notes with name badge, filter select, and detail preview modal

diff --git a/src/App.tsx b/src/App.tsx
index 95c9719..38f3aec 100644
--- a/src/App.tsx
+++ b/src/App.tsx
@@ -27,6 +27,8 @@ function App() {
   const [mistakes, setMistakes] = useState<MistakeEntry[]>([]);
   const [selectedEntry, setSelectedEntry] = useState<MistakeEntry | null>(null);
   const [isAnalyzing, setIsAnalyzing] = useState(false);
+  // userId -> displayName map (admin ?꾩슜)
+  const [profilesMap, setProfilesMap] = useState<Record<string, string>>({});
 
   // State for image cropping flow
   const [tempCapturedImage, setTempCapturedImage] = useState<string | null>(null);
@@ -79,6 +81,7 @@ function App() {
   // Fetch mistakes from Supabase
   const fetchUserData = async () => {
     try {
+      // Fetch all mistakes (RLS handles filtering: normal users see own, admin sees all)
       const { data: dbMistakes, error: mistakesError } = await supabase
         .from('mistakes')
         .select('*')
@@ -86,8 +89,20 @@ function App() {
 
       if (mistakesError) throw mistakesError;
 
+      // Fetch all profiles for admin name mapping
+      const { data: profiles } = await supabase
+        .from('profiles')
+        .select('id, email');
+
+      const pMap: Record<string, string> = {};
+      (profiles || []).forEach((p: any) => {
+        pMap[p.id] = p.email?.split('@')[0] || p.id.slice(0, 8);
+      });
+      setProfilesMap(pMap);
+
       const mappedMistakes: MistakeEntry[] = (dbMistakes || []).map((m: any) => ({
         id: m.id,
+        userId: m.user_id,
         title: m.title,
         imageUrl: m.image_url,
         date: m.date,
@@ -292,6 +307,7 @@ function App() {
     setIsAdmin(false);
     setSession(null);
     setMistakes([]);
+    setProfilesMap({});
     setActiveTab('notes');
   };
 
@@ -349,6 +365,9 @@ function App() {
             onAddClick={() => setActiveTab('camera')}
             title="?섏쓽 ?ㅻ떟?명듃"
             emptyMessage="?꾩쭅 ?깅줉???ㅻ떟???놁뒿?덈떎. ?꾨옒 移대찓??踰꾪듉???뚮윭 ?섑븰 臾몄젣瑜?珥ъ쁺?섍퀬 AI??留욎땄 遺꾩꽍??諛쏆븘蹂댁꽭??"
+            isAdmin={isAdmin}
+            profilesMap={profilesMap}
+            currentUserId={session?.user?.id}
           />
         )}
 
@@ -361,6 +380,9 @@ function App() {
             title="蹂듭뒿 ?꾨즺 蹂닿???
             hideAddButton={true}
             emptyMessage="?꾩쭅 ?꾩쟾??蹂듭뒿 ?꾨즺(O 3???ъ꽦)???ㅻ떟???놁뒿?덈떎. ?댁떖???ㅻ떟??蹂듭뒿?섏뿬 ?뺣났??蹂댁꽭??"
+            isAdmin={isAdmin}
+            profilesMap={profilesMap}
+            currentUserId={session?.user?.id}
           />
         )}
 

commit 758218f4d4559e86837afae5a58f622611fc67d2
Author: ReviewNotes Developer <developer@reviewnotes.local>
Date:   Sat Jul 4 12:50:40 2026 +0900

    feat: Add auto-classification, root cause checkboxes, manual action plan, and stats dashboard tab

diff --git a/src/App.tsx b/src/App.tsx
index 1565140..95c9719 100644
--- a/src/App.tsx
+++ b/src/App.tsx
@@ -1,5 +1,6 @@
-import { useState, useEffect } from 'react';
+import { useState, useEffect, useMemo } from 'react';
 import type { ActiveTab, MistakeEntry, ReviewState } from './types';
+import { ROOT_CAUSE_OPTIONS, GRADE_LIST } from './types';
 import { CameraScanner } from './components/CameraScanner';
 import { analyzeMistakeWithGemini } from './services/gemini';
 import { AuthScreen } from './components/AuthScreen';
@@ -91,7 +92,11 @@ function App() {
         imageUrl: m.image_url,
         date: m.date,
         analysis: m.analysis || undefined,
-        reviews: m.reviews || ['', '', '']
+        reviews: m.reviews || ['', '', ''],
+        grade: m.grade || undefined,
+        chapter: m.chapter || undefined,
+        rootCauses: m.root_causes || [],
+        userActionPlan: m.user_action_plan || undefined,
       }));
       setMistakes(mappedMistakes);
     } catch (err) {
@@ -144,12 +149,14 @@ function App() {
   const runAnalysisFlow = async (entry: MistakeEntry, apiKey: string) => {
     const result = await analyzeMistakeWithGemini(entry.imageUrl, apiKey);
     
-    // Update mistake database record
+    // Update mistake database record (including auto-classified grade/chapter)
     const { error: updateError } = await supabase
       .from('mistakes')
       .update({
         title: result.title,
-        analysis: result.analysis
+        analysis: result.analysis,
+        grade: result.grade || null,
+        chapter: result.chapter || null,
       })
       .eq('id', entry.id);
 
@@ -158,7 +165,9 @@ function App() {
     const updatedEntry: MistakeEntry = {
       ...entry,
       title: result.title,
-      analysis: result.analysis
+      analysis: result.analysis,
+      grade: result.grade,
+      chapter: result.chapter,
     };
 
     setMistakes(prev => prev.map(m => m.id === entry.id ? updatedEntry : m));
@@ -204,7 +213,11 @@ function App() {
           title: newEntryTitle,
           image_url: publicUrl,
           analysis: null,
-          reviews: ['', '', '']
+          reviews: ['', '', ''],
+          grade: null,
+          chapter: null,
+          root_causes: [],
+          user_action_plan: null,
         })
         .select()
         .single();
@@ -217,7 +230,8 @@ function App() {
         imageUrl: dbEntry.image_url,
         date: dbEntry.date,
         analysis: undefined,
-        reviews: ['', '', '']
+        reviews: ['', '', ''],
+        rootCauses: [],
       };
       
       setMistakes(prev => [newEntry, ...prev]);
@@ -281,6 +295,30 @@ function App() {
     setActiveTab('notes');
   };
 
+  // ?? ?듦퀎 怨꾩궛 ??????????????????????????????????????????????????
+  const stats = useMemo(() => {
+    const gradeCounts: Record<string, number> = {};
+    const chapterCounts: Record<string, number> = {};
+    const causeCounts: Record<string, number> = {};
+
+    mistakes.forEach(m => {
+      if (m.grade) gradeCounts[m.grade] = (gradeCounts[m.grade] || 0) + 1;
+      if (m.chapter && m.grade) {
+        const key = `${m.grade} > ${m.chapter}`;
+        chapterCounts[key] = (chapterCounts[key] || 0) + 1;
+      }
+      (m.rootCauses || []).forEach(c => {
+        causeCounts[c] = (causeCounts[c] || 0) + 1;
+      });
+    });
+
+    const totalGrade = Object.values(gradeCounts).reduce((a, b) => a + b, 0);
+    const totalCause = Object.values(causeCounts).reduce((a, b) => a + b, 0);
+    const topChapters = Object.entries(chapterCounts).sort((a, b) => b[1] - a[1]).slice(0, 5);
+
+    return { gradeCounts, causeCounts, totalGrade, totalCause, topChapters };
+  }, [mistakes]);
+
   if (!currentUser) {
     return <AuthScreen onLogin={(username) => setCurrentUser(username)} />;
   }
@@ -336,6 +374,95 @@ function App() {
         {activeTab === 'admin' && isAdmin && (
           <AdminPanel />
         )}
+
+        {/* ?? 遺꾩꽍?듦퀎 ???? */}
+        {activeTab === 'stats' && (
+          <div className="space-y-6">
+            <div>
+              <h2 className="text-lg font-bold text-white">?뱤 ?섏쓽 ?쎌젏 遺꾩꽍</h2>
+              <p className="text-xs text-slate-400 mt-0.5">珥?{mistakes.length}媛쒖쓽 ?ㅻ떟 湲곕줉 湲곕컲</p>
+            </div>
+
+            {mistakes.length === 0 ? (
+              <div className="flex flex-col items-center justify-center py-20 text-center border-2 border-dashed border-slate-800 rounded-2xl bg-slate-900/20">
+                <div className="text-3xl mb-3">?뱤</div>
+                <p className="text-slate-300 font-medium text-sm">?꾩쭅 遺꾩꽍???곗씠?곌? ?놁뒿?덈떎</p>
+                <p className="text-xs text-slate-500 mt-1">?ㅻ떟???깅줉?섍퀬 ?ㅼ닔 ?먯씤??泥댄겕??二쇱꽭??</p>
+              </div>
+            ) : (
+              <>
+                {/* 怨쇰ぉ蹂??ㅻ떟 遺꾪룷 */}
+                <div className="bg-slate-900/60 border border-slate-800 rounded-2xl p-5 space-y-3">
+                  <h3 className="text-sm font-extrabold text-indigo-400">?뱴 怨쇰ぉ蹂??ㅻ떟 遺꾪룷</h3>
+                  {stats.totalGrade === 0 ? (
+                    <p className="text-xs text-slate-500">AI 遺꾩꽍???ㅽ뻾?섎㈃ 怨쇰ぉ???먮룞 遺꾨쪟?⑸땲??</p>
+                  ) : (
+                    GRADE_LIST.filter(g => stats.gradeCounts[g]).map(grade => {
+                      const count = stats.gradeCounts[grade] || 0;
+                      const pct = Math.round((count / stats.totalGrade) * 100);
+                      return (
+                        <div key={grade} className="space-y-1">
+                          <div className="flex justify-between text-xs">
+                            <span className="text-slate-300 font-medium">{grade}</span>
+                            <span className="text-slate-400">{count}媛?({pct}%)</span>
+                          </div>
+                          <div className="w-full h-2.5 bg-slate-800 rounded-full overflow-hidden">
+                            <div className="h-full bg-gradient-to-r from-indigo-600 to-indigo-400 rounded-full transition-all duration-700" style={{ width: `${pct}%` }} />
+                          </div>
+                        </div>
+                      );
+                    })
+                  )}
+                </div>
+
+                {/* ?ㅼ닔 ?좏삎蹂?遺꾪룷 */}
+                <div className="bg-slate-900/60 border border-slate-800 rounded-2xl p-5 space-y-3">
+                  <h3 className="text-sm font-extrabold text-amber-400">?좑툘 ?섏쓽 ?ㅼ닔 ?좏삎 遺꾩꽍</h3>
+                  {stats.totalCause === 0 ? (
+                    <p className="text-xs text-slate-500">?ㅻ떟 ?곸꽭李쎌뿉???ㅼ닔 ?먯씤??泥댄겕??二쇱꽭??</p>
+                  ) : (
+                    ROOT_CAUSE_OPTIONS.map(opt => {
+                      const count = stats.causeCounts[opt.id] || 0;
+                      const pct = Math.round((count / stats.totalCause) * 100);
+                      return (
+                        <div key={opt.id} className="space-y-1">
+                          <div className="flex justify-between text-xs">
+                            <span className="text-slate-300 font-medium">{opt.label}</span>
+                            <span className="text-slate-400">{count}??({pct}%)</span>
+                          </div>
+                          <div className="w-full h-2.5 bg-slate-800 rounded-full overflow-hidden">
+                            <div className="h-full bg-gradient-to-r from-amber-500 to-amber-400 rounded-full transition-all duration-700" style={{ width: `${pct}%` }} />
+                          </div>
+                        </div>
+                      );
+                    })
+                  )}
+                </div>
+
+                {/* 痍⑥빟 ?⑥썝 TOP 5 */}
+                {stats.topChapters.length > 0 && (
+                  <div className="bg-slate-900/60 border border-slate-800 rounded-2xl p-5 space-y-3">
+                    <h3 className="text-sm font-extrabold text-emerald-400">?룇 痍⑥빟 ?⑥썝 TOP {stats.topChapters.length}</h3>
+                    {stats.topChapters.map(([key, count], i) => (
+                      <div key={key} className="flex items-center space-x-3 bg-slate-950/50 rounded-xl p-3 border border-slate-800/60">
+                        <div className={`w-8 h-8 rounded-full flex items-center justify-center text-sm font-black flex-none ${
+                          i === 0 ? 'bg-amber-500/20 text-amber-400 border border-amber-500/30' :
+                          i === 1 ? 'bg-slate-500/20 text-slate-300 border border-slate-600/30' :
+                          i === 2 ? 'bg-orange-700/20 text-orange-400 border border-orange-700/30' :
+                          'bg-slate-800 text-slate-500'
+                        }`}>{i + 1}</div>
+                        <div className="flex-1 min-w-0">
+                          <p className="text-xs font-bold text-white truncate">{key}</p>
+                        </div>
+                        <div className="text-xs font-black text-red-400 flex-none">{count}媛?/div>
+                      </div>
+                    ))}
+                  </div>
+                )}
+              </>
+            )}
+          </div>
+        )}
       </main>
 
       {/* Selected Entry Detail Modal */}
@@ -347,6 +474,10 @@ function App() {
           onDeleteMistake={handleDeleteMistake}
           onStartAnalysis={handleStartAnalysis}
           onUpdateReviews={handleUpdateReviews}
+          onUpdateEntry={(updated) => {
+            setMistakes(prev => prev.map(m => m.id === updated.id ? updated : m));
+            setSelectedEntry(updated);
+          }}
         />
       )}
 

commit 0501c675812ec2c20886f824766475bc70abd88b
Author: 8xnvekjq <8xnvekjq@users.noreply.github.com>
Date:   Fri Jul 3 21:51:59 2026 +0900

    Feat: Add automatic VITE_GEMINI_API_KEY_PAID fallback system when free VITE_GEMINI_API_KEY hits rate limit/quota limits

diff --git a/src/App.tsx b/src/App.tsx
index 33df939..1565140 100644
--- a/src/App.tsx
+++ b/src/App.tsx
@@ -99,41 +99,39 @@ function App() {
     }
   };
 
-  // Start analysis trigger
-  const handleStartAnalysis = (entry: MistakeEntry) => {
-    const envKey = import.meta.env.VITE_GEMINI_API_KEY || '';
-    if (envKey) {
-      runAnalysisFlow(entry, envKey);
-    } else {
-      alert('愿由ъ옄 ?쒕쾭??Gemini API Key(VITE_GEMINI_API_KEY)媛 ?깅줉?섏? ?딆븯?듬땲?? Vercel ?섍꼍 蹂???ㅼ젙???꾨즺??二쇱꽭??');
+  // Start analysis trigger with automatic paid fallback
+  const handleStartAnalysis = async (entry: MistakeEntry) => {
+    const freeKey = import.meta.env.VITE_GEMINI_API_KEY || '';
+    const paidKey = import.meta.env.VITE_GEMINI_API_KEY_PAID || '';
+
+    if (!freeKey && !paidKey) {
+      alert('愿由ъ옄 ?쒕쾭??Gemini API Key媛 ?깅줉?섏? ?딆븯?듬땲?? Vercel ?섍꼍 蹂???ㅼ젙???꾨즺??二쇱꽭??');
+      return;
     }
-  };
 
-  // Gemini API analysis flow & database updating
-  const runAnalysisFlow = async (entry: MistakeEntry, apiKey: string) => {
     setIsAnalyzing(true);
     try {
-      const result = await analyzeMistakeWithGemini(entry.imageUrl, apiKey);
-      
-      // Update mistake database record
-      const { error: updateError } = await supabase
-        .from('mistakes')
-        .update({
-          title: result.title,
-          analysis: result.analysis
-        })
-        .eq('id', entry.id);
-
-      if (updateError) throw updateError;
-
-      const updatedEntry: MistakeEntry = {
-        ...entry,
-        title: result.title,
-        analysis: result.analysis
-      };
-
-      setMistakes(prev => prev.map(m => m.id === entry.id ? updatedEntry : m));
-      setSelectedEntry(updatedEntry);
+      if (freeKey) {
+        try {
+          await runAnalysisFlow(entry, freeKey);
+        } catch (err: any) {
+          const errorMsg = err?.message || '';
+          const isQuotaError = 
+            errorMsg.includes('429') || 
+            errorMsg.toUpperCase().includes('RESOURCE_EXHAUSTED') || 
+            errorMsg.includes('quota') || 
+            errorMsg.includes('limit');
+
+          if (paidKey && isQuotaError) {
+            console.warn('Free API Key limit hit. Retrying with Paid API Key...');
+            await runAnalysisFlow(entry, paidKey);
+          } else {
+            throw err;
+          }
+        }
+      } else {
+        await runAnalysisFlow(entry, paidKey);
+      }
     } catch (err: any) {
       console.error(err);
       alert(err.message || 'AI 遺꾩꽍 ?ㅽ뻾 以??ㅻ쪟媛 諛쒖깮?덉뒿?덈떎.');
@@ -142,6 +140,31 @@ function App() {
     }
   };
 
+  // Gemini API analysis flow & database updating
+  const runAnalysisFlow = async (entry: MistakeEntry, apiKey: string) => {
+    const result = await analyzeMistakeWithGemini(entry.imageUrl, apiKey);
+    
+    // Update mistake database record
+    const { error: updateError } = await supabase
+      .from('mistakes')
+      .update({
+        title: result.title,
+        analysis: result.analysis
+      })
+      .eq('id', entry.id);
+
+    if (updateError) throw updateError;
+
+    const updatedEntry: MistakeEntry = {
+      ...entry,
+      title: result.title,
+      analysis: result.analysis
+    };
+
+    setMistakes(prev => prev.map(m => m.id === entry.id ? updatedEntry : m));
+    setSelectedEntry(updatedEntry);
+  };
+
   // Intercept camera capture and start cropping flow
   const handleCameraCapture = (base64Image: string) => {
     setTempCapturedImage(base64Image);

commit 52cf2ed8c268e70d3933aa04bf022f9a605286db
Author: 8xnvekjq <8xnvekjq@users.noreply.github.com>
Date:   Fri Jul 3 21:09:02 2026 +0900

    Revert "Feat: Restore client-side encrypted Gemini API Key registration & settings PIN code system"
    
    This reverts commit 7e68acea30dcfc45cd0070e307dca4686674ef50.

diff --git a/src/App.tsx b/src/App.tsx
index 732f550..33df939 100644
--- a/src/App.tsx
+++ b/src/App.tsx
@@ -12,7 +12,6 @@ import { MistakeDetailModal } from './components/MistakeDetailModal';
 import { BottomNavigation } from './components/BottomNavigation';
 import { ImageCropper } from './components/ImageCropper';
 import { AdminPanel } from './components/AdminPanel';
-import { encryptApiKey, decryptApiKey } from './utils/crypto';
 
 function App() {
   // If Supabase credentials are not configured, block and show the setup guide
@@ -27,20 +26,6 @@ function App() {
   const [mistakes, setMistakes] = useState<MistakeEntry[]>([]);
   const [selectedEntry, setSelectedEntry] = useState<MistakeEntry | null>(null);
   const [isAnalyzing, setIsAnalyzing] = useState(false);
-  
-  // Encrypted API key states for users
-  const [encryptedApiKey, setEncryptedApiKey] = useState<string>('');
-  const [decryptedKey, setDecryptedKey] = useState<string>(() => {
-    return sessionStorage.getItem('gemini_api_key_temp') || '';
-  });
-
-  // Modal and Form States
-  const [apiKeyInput, setApiKeyInput] = useState('');
-  const [pinInput, setPinInput] = useState('');
-  const [unlockPin, setUnlockPin] = useState('');
-  const [showUnlockModal, setShowUnlockModal] = useState(false);
-  const [showApiGuideModal, setShowApiGuideModal] = useState(false);
-  const [statusMessage, setStatusMessage] = useState<{ text: string; isError: boolean } | null>(null);
 
   // State for image cropping flow
   const [tempCapturedImage, setTempCapturedImage] = useState<string | null>(null);
@@ -84,21 +69,15 @@ function App() {
         setCurrentUser('');
         setIsAdmin(false);
         setMistakes([]);
-        setEncryptedApiKey('');
-        setDecryptedKey('');
-        sessionStorage.removeItem('gemini_api_key_temp');
       }
     });
 
     return () => subscription.unsubscribe();
   }, []);
 
-  // Fetch mistakes and user profile API key from Supabase
+  // Fetch mistakes from Supabase
   const fetchUserData = async () => {
     try {
-      const { data: { user } } = await supabase.auth.getUser();
-      if (!user) return;
-
       const { data: dbMistakes, error: mistakesError } = await supabase
         .from('mistakes')
         .select('*')
@@ -115,104 +94,18 @@ function App() {
         reviews: m.reviews || ['', '', '']
       }));
       setMistakes(mappedMistakes);
-
-      // Fetch profile encrypted key
-      const { data: profileData, error: profileError } = await supabase
-        .from('profiles')
-        .select('encrypted_api_key')
-        .eq('id', user.id)
-        .maybeSingle();
-
-      if (profileData && !profileError) {
-        setEncryptedApiKey(profileData.encrypted_api_key || '');
-      }
     } catch (err) {
       console.error('Error loading Supabase user data:', err);
     }
   };
 
-  // Securely encrypt and save the API Key to Supabase database profiles table
-  const handleSaveApiKey = async (e: React.FormEvent) => {
-    e.preventDefault();
-    setStatusMessage(null);
-
-    if (!apiKeyInput.trim()) {
-      setStatusMessage({ text: 'API Key瑜??낅젰??二쇱꽭??', isError: true });
-      return;
-    }
-    if (pinInput.length < 4) {
-      setStatusMessage({ text: 'PIN 踰덊샇??理쒖냼 4?먮━ ?댁긽?댁뼱???⑸땲??', isError: true });
-      return;
-    }
-    
-    const { data: { user } } = await supabase.auth.getUser();
-    if (!user) return;
-
-    try {
-      const encrypted = await encryptApiKey(apiKeyInput.trim(), pinInput);
-      
-      const { error: profileError } = await supabase
-        .from('profiles')
-        .upsert({
-          id: user.id,
-          encrypted_api_key: encrypted,
-          updated_at: new Date().toISOString()
-        });
-
-      if (profileError) throw profileError;
-
-      setEncryptedApiKey(encrypted);
-      
-      // Save temporarily in session storage
-      setDecryptedKey(apiKeyInput.trim());
-      sessionStorage.setItem('gemini_api_key_temp', apiKeyInput.trim());
-
-      setApiKeyInput('');
-      setPinInput('');
-      setStatusMessage({ text: 'API Key媛 ?뷀샇?붾릺???대씪?곕뱶 ?꾨줈?꾩뿉 ?덉쟾?섍쾶 ?숆린?붾릺?덉뒿?덈떎.', isError: false });
-    } catch (err: any) {
-      console.error(err);
-      setStatusMessage({ text: `???以??ㅻ쪟 諛쒖깮: ${err.message}`, isError: true });
-    }
-  };
-
-  // Decrypt Key handler (used when analyzing)
-  const handleUnlockKey = async (e: React.FormEvent) => {
-    e.preventDefault();
-    setStatusMessage(null);
-
-    if (!encryptedApiKey) {
-      setStatusMessage({ text: '??λ맂 API Key媛 ?놁뒿?덈떎. ?ㅼ젙 ??뿉??癒쇱? ?깅줉??二쇱꽭??', isError: true });
-      return;
-    }
-
-    try {
-      const decrypted = await decryptApiKey(encryptedApiKey, unlockPin);
-      setDecryptedKey(decrypted);
-      sessionStorage.setItem('gemini_api_key_temp', decrypted);
-      setShowUnlockModal(false);
-      setUnlockPin('');
-      
-      if (selectedEntry) {
-        runAnalysisFlow(selectedEntry, decrypted);
-      }
-    } catch (err: any) {
-      setStatusMessage({ text: err.message || '蹂듯샇???ㅽ뙣', isError: true });
-    }
-  };
-
   // Start analysis trigger
   const handleStartAnalysis = (entry: MistakeEntry) => {
     const envKey = import.meta.env.VITE_GEMINI_API_KEY || '';
-    const activeKey = decryptedKey || envKey;
-
-    if (activeKey) {
-      runAnalysisFlow(entry, activeKey);
-    } else if (encryptedApiKey) {
-      setShowUnlockModal(true);
+    if (envKey) {
+      runAnalysisFlow(entry, envKey);
     } else {
-      setActiveTab('settings');
-      alert('AI ?ㅻ떟 遺꾩꽍??吏꾪뻾?섎젮硫?癒쇱? ?ㅼ젙 ??뿉??Gemini API Key瑜??깅줉?섍굅??.env ?뚯씪??VITE_GEMINI_API_KEY瑜??ㅼ젙?댁빞 ?⑸땲??');
+      alert('愿由ъ옄 ?쒕쾭??Gemini API Key(VITE_GEMINI_API_KEY)媛 ?깅줉?섏? ?딆븯?듬땲?? Vercel ?섍꼍 蹂???ㅼ젙???꾨즺??二쇱꽭??');
     }
   };
 
@@ -362,9 +255,6 @@ function App() {
     setIsAdmin(false);
     setSession(null);
     setMistakes([]);
-    setEncryptedApiKey('');
-    setDecryptedKey('');
-    sessionStorage.removeItem('gemini_api_key_temp');
     setActiveTab('notes');
   };
 
@@ -376,13 +266,7 @@ function App() {
     <div className="h-full flex flex-col bg-slate-950 text-slate-100 select-none">
       
       {/* Top Header */}
-      <Header 
-        currentUser={currentUser} 
-        onLogout={handleLogout} 
-        encryptedApiKey={encryptedApiKey}
-        decryptedKey={decryptedKey}
-        onShowApiGuide={() => setShowApiGuideModal(true)}
-      />
+      <Header currentUser={currentUser} onLogout={handleLogout} />
 
       {/* Main Content Area */}
       <main className="flex-1 overflow-y-auto px-4 py-6 pb-24">
@@ -429,111 +313,6 @@ function App() {
         {activeTab === 'admin' && isAdmin && (
           <AdminPanel />
         )}
-
-        {activeTab === 'settings' && (
-          <div className="space-y-6 max-w-lg mx-auto">
-            <div>
-              <h2 className="text-xl font-bold text-white">???ㅼ젙</h2>
-              <p className="text-xs text-slate-400 mt-1">API 蹂댁븞 ?ㅼ젙 諛????섍꼍?ㅼ젙</p>
-            </div>
-
-            {/* API Security Card */}
-            <form onSubmit={handleSaveApiKey} className="bg-slate-900/40 border border-slate-800 rounded-2xl p-5 space-y-4">
-              <div className="flex items-center space-x-3">
-                <div className="w-10 h-10 rounded-xl bg-indigo-950 border border-indigo-800 flex items-center justify-center text-xl text-indigo-400">
-                  ?뵎
-                </div>
-                <div>
-                  <h3 className="font-bold text-white text-sm">Gemini API 蹂댁븞 ?ㅼ젙</h3>
-                  <p className="text-[10px] text-slate-400">?대씪?곕뱶 ?꾨줈???????媛뺣젰 ?뷀샇???곸슜</p>
-                </div>
-              </div>
-              
-              <div className="space-y-3 pt-2">
-                <div className="space-y-1">
-                  <label className="text-xs text-slate-400 font-medium font-bold flex items-center justify-between">
-                    <span>Gemini API Key</span>
-                    <button
-                      type="button"
-                      onClick={() => setShowApiGuideModal(true)}
-                      className="text-[10px] text-indigo-400 hover:underline"
-                    >
-                      諛쒓툒 諛⑸쾿 蹂닿린 ?
-                    </button>
-                  </label>
-                  <input 
-                    type="password" 
-                    value={apiKeyInput}
-                    onChange={(e) => setApiKeyInput(e.target.value)}
-                    placeholder="AIzaSy..." 
-                    className="w-full px-4 py-3 rounded-xl bg-slate-950 border border-slate-800 focus:border-indigo-500 focus:ring-1 focus:ring-indigo-500 text-sm text-white placeholder-slate-600 outline-none transition-all"
-                  />
-                </div>
-                <div className="space-y-1">
-                  <label className="text-xs text-slate-400 font-medium font-bold">??蹂듯샇??PIN (4~6?먮━ ?レ옄)</label>
-                  <input 
-                    type="password" 
-                    maxLength={6}
-                    value={pinInput}
-                    onChange={(e) => setPinInput(e.target.value.replace(/[^0-9]/g, ''))}
-                    placeholder="?レ옄 4~6?먮━ ?낅젰" 
-                    className="w-full px-4 py-3 rounded-xl bg-slate-950 border border-slate-800 focus:border-indigo-500 focus:ring-1 focus:ring-indigo-500 text-sm text-white placeholder-slate-600 outline-none transition-all"
-                  />
-                  <p className="text-[9px] text-slate-500 leading-normal">
-                    * ?ㅼ젙??PIN? ??蹂듯샇?????몄쬆 ?섎떒?쇰줈 ?ъ슜?⑸땲?? ?대씪?곕뱶???곕룞 ??λ릺誘濡?湲곌린媛 諛붾뚯뼱????PIN留??덉쑝硫??몄젣???ㅻ? 遺덈윭? 遺꾩꽍?????덉뒿?덈떎.
-                  </p>
-                </div>
-
-                {statusMessage && (
-                  <div className={`p-3 rounded-lg text-xs ${statusMessage.isError ? 'bg-red-500/10 text-red-400 border border-red-500/20' : 'bg-emerald-500/10 text-emerald-400 border border-emerald-500/20'}`}>
-                    {statusMessage.text}
-                  </div>
-                )}
-
-                <button 
-                  type="submit"
-                  className="w-full py-3 mt-2 rounded-xl bg-indigo-600 hover:bg-indigo-500 active:scale-95 transition-all text-xs font-bold text-white shadow-md shadow-indigo-600/10"
-                >
-                  蹂댁븞 ??ν븯湲?-                </button>
-              </div>
-            </form>
-
-            {/* App Info Card */}
-            <div className="bg-slate-900/40 border border-slate-800 rounded-2xl p-5 space-y-3">
-              <h3 className="font-bold text-white text-sm">?좏뵆由ъ??댁뀡 ?뺣낫</h3>
-              <div className="divide-y divide-slate-800/60 text-xs text-slate-300">
-                <div className="flex justify-between py-2.5">
-                  <span className="text-slate-400">踰꾩쟾</span>
-                  <span className="text-slate-200 font-medium">v1.6.0 (PWA + Supabase)</span>
-                </div>
-                <div className="flex justify-between py-2.5">
-                  <span className="text-slate-400">?몄쬆 諛??곗씠?곕쿋?댁뒪</span>
-                  <span className="text-emerald-400 font-medium font-bold">Supabase Cloud</span>
-                </div>
-                <div className="flex justify-between py-2.5">
-                  <span className="text-slate-400">蹂댁븞 ?섏?</span>
-                  <span className="text-emerald-400 font-medium font-bold">AES-GCM ?뷀샇??/span>
-                </div>
-                {decryptedKey && (
-                  <div className="flex justify-between py-2.5 items-center">
-                    <span className="text-slate-400">?몄뀡 ?곹깭</span>
-                    <button 
-                      onClick={() => {
-                        setDecryptedKey('');
-                        sessionStorage.removeItem('gemini_api_key_temp');
-                        alert('API Key ?몄뀡??留뚮즺?섏뿀?듬땲?? ?ㅼ떆 ?덉쟾?섍쾶 ?좉꼈?듬땲??');
-                      }}
-                      className="px-2 py-1 bg-red-950 border border-red-800 text-red-400 rounded-lg text-[10px] font-bold"
-                    >
-                      ?몄뀡 硫붾え由??좉툑
-                    </button>
-                  </div>
-                )}
-              </div>
-            </div>
-          </div>
-        )}
       </main>
 
       {/* Selected Entry Detail Modal */}
@@ -557,121 +336,6 @@ function App() {
         />
       )}
 
-      {/* Unlock API Key PIN Modal */}
-      {showUnlockModal && (
-        <div className="fixed inset-0 z-50 flex items-center justify-center bg-black/80 backdrop-blur-sm p-4">
-          <form 
-            onSubmit={handleUnlockKey}
-            className="w-full max-w-sm bg-slate-900 border border-slate-800 rounded-3xl p-6 space-y-4 shadow-2xl animate-scale-up text-slate-100"
-          >
-            <div className="text-center space-y-2">
-              <div className="w-12 h-12 rounded-full bg-amber-500/10 border border-amber-500/30 text-2xl flex items-center justify-center mx-auto text-amber-400">
-                ?뵏
-              </div>
-              <h3 className="font-bold text-white text-base">蹂댁븞 蹂듯샇??/h3>
-              <p className="text-xs text-slate-400 max-w-xs mx-auto leading-relaxed">
-                ?뷀샇?붾맂 API Key瑜?濡쒕뱶?섍린 ?꾪빐 蹂댁븞 PIN 肄붾뱶瑜??낅젰??二쇱꽭??
-              </p>
-            </div>
-
-            <div className="space-y-3 pt-2">
-              <input 
-                type="password" 
-                maxLength={6}
-                value={unlockPin}
-                onChange={(e) => setUnlockPin(e.target.value.replace(/[^0-9]/g, ''))}
-                placeholder="?レ옄 4~6?먮━ PIN ?낅젰" 
-                className="w-full px-4 py-3 text-center tracking-widest text-lg rounded-xl bg-slate-950 border border-slate-850 focus:border-indigo-500 focus:ring-1 focus:ring-indigo-500 text-white placeholder-slate-600 outline-none transition-all"
-              />
-
-              {statusMessage && (
-                <div className="p-3 rounded-lg text-xs bg-red-500/10 text-red-400 border border-red-500/20">
-                  {statusMessage.text}
-                </div>
-              )}
-
-              <div className="flex space-x-3 pt-2">
-                <button 
-                  type="button"
-                  onClick={() => {
-                    setShowUnlockModal(false);
-                    setUnlockPin('');
-                    setStatusMessage(null);
-                  }}
-                  className="flex-1 py-3 rounded-xl border border-slate-800 hover:bg-slate-800 active:scale-95 text-xs font-semibold text-slate-300"
-                >
-                  痍⑥냼
-                </button>
-                <button 
-                  type="submit"
-                  className="flex-1 py-3 rounded-xl bg-indigo-600 hover:bg-indigo-500 active:scale-95 text-xs font-bold text-white shadow-md shadow-indigo-600/20"
-                >
-                  ?좉툑 ?댁젣
-                </button>
-              </div>
-            </div>
-          </form>
-        </div>
-      )}
-
-      {/* Gemini API Key Guide Modal */}
-      {showApiGuideModal && (
-        <div className="fixed inset-0 z-50 flex items-center justify-center bg-black/80 backdrop-blur-sm p-4">
-          <div className="w-full max-w-md bg-slate-900 border border-slate-800 rounded-3xl p-6 space-y-4 shadow-2xl animate-scale-up text-slate-100">
-            <div className="text-center space-y-2">
-              <div className="w-12 h-12 rounded-full bg-indigo-500/10 border border-indigo-500/30 text-xl flex items-center justify-center mx-auto text-indigo-400">
-                ?쨼
-              </div>
-              <h3 className="font-bold text-white text-base">Gemini API ??諛쒓툒 媛?대뱶</h3>
-              <p className="text-xs text-slate-400 max-w-xs mx-auto leading-relaxed">
-                AI ?ㅻ떟?명듃???먮룞 痍⑥빟??吏꾨떒 湲곕뒫???댁슜?섎젮硫?臾대즺 Google Gemini API ?ㅺ? ?꾩슂?⑸땲??
-              </p>
-            </div>
-
-            <div className="space-y-3 pt-2 text-left text-xs text-slate-300">
-              <div className="bg-slate-950 p-4 rounded-2xl border border-slate-800 space-y-3.5 leading-relaxed">
-                <div className="flex items-start space-x-2.5">
-                  <span className="w-5 h-5 rounded-full bg-indigo-900 text-indigo-300 text-[10px] font-bold flex items-center justify-center flex-none mt-0.5">1</span>
-                  <p>
-                    ?꾨옒 <strong>\'API ??諛쒓툒?섎윭 媛湲?'</strong> 踰꾪듉???대┃?섏뿬 Google AI Studio ?ъ씠?몃줈 ?대룞?⑸땲??(援ш? 怨꾩젙 ?꾩슂).
-                  </p>
-                </div>
-                <div className="flex items-start space-x-2.5">
-                  <span className="w-5 h-5 rounded-full bg-indigo-900 text-indigo-300 text-[10px] font-bold flex items-center justify-center flex-none mt-0.5">2</span>
-                  <p>
-                    濡쒓렇?????곷떒??<strong>"Get API key"</strong> ?먮뒗 <strong>"Create API key"</strong> 踰꾪듉???대┃?섏뿬 ???ㅻ? 諛쒓툒諛쏆뒿?덈떎.
-                  </p>
-                </div>
-                <div className="flex items-start space-x-2.5">
-                  <span className="w-5 h-5 rounded-full bg-indigo-900 text-indigo-300 text-[10px] font-bold flex items-center justify-center flex-none mt-0.5">3</span>
-                  <p>
-                    諛쒓툒????<code className="bg-slate-900 px-1.5 py-0.5 rounded text-indigo-400 text-[10px] font-mono">AIzaSy...</code>)瑜?蹂듭궗???? ?ㅻ떟?명듃 <strong>[?ㅼ젙]</strong> ??뿉??PIN 踰덊샇? ?④퍡 ?깅줉?⑸땲??
-                  </p>
-                </div>
-              </div>
-
-              <div className="flex space-x-3 pt-2">
-                <button
-                  type="button"
-                  onClick={() => setShowApiGuideModal(false)}
-                  className="flex-1 py-3 rounded-xl border border-slate-800 hover:bg-slate-800 active:scale-95 text-xs font-semibold text-slate-300"
-                >
-                  ?リ린
-                </button>
-                <a 
-                  href="https://aistudio.google.com/" 
-                  target="_blank" 
-                  rel="noopener noreferrer"
-                  className="flex-1 py-3 rounded-xl bg-gradient-to-r from-indigo-600 to-indigo-500 hover:from-indigo-500 hover:to-indigo-400 active:scale-95 text-xs font-bold text-white text-center shadow-md shadow-indigo-600/20 flex items-center justify-center"
-                >
-                  API ??諛쒓툒?섍린 &rarr;
-                </a>
-              </div>
-            </div>
-          </div>
-        </div>
-      )}
-
       {/* Floating Glassmorphic Bottom Navigation Bar */}
       <BottomNavigation activeTab={activeTab} setActiveTab={setActiveTab} isAdmin={isAdmin} />
 

commit 7e68acea30dcfc45cd0070e307dca4686674ef50
Author: 8xnvekjq <8xnvekjq@users.noreply.github.com>
Date:   Fri Jul 3 21:02:02 2026 +0900

    Feat: Restore client-side encrypted Gemini API Key registration & settings PIN code system

diff --git a/src/App.tsx b/src/App.tsx
index 33df939..732f550 100644
--- a/src/App.tsx
+++ b/src/App.tsx
@@ -12,6 +12,7 @@ import { MistakeDetailModal } from './components/MistakeDetailModal';
 import { BottomNavigation } from './components/BottomNavigation';
 import { ImageCropper } from './components/ImageCropper';
 import { AdminPanel } from './components/AdminPanel';
+import { encryptApiKey, decryptApiKey } from './utils/crypto';
 
 function App() {
   // If Supabase credentials are not configured, block and show the setup guide
@@ -26,6 +27,20 @@ function App() {
   const [mistakes, setMistakes] = useState<MistakeEntry[]>([]);
   const [selectedEntry, setSelectedEntry] = useState<MistakeEntry | null>(null);
   const [isAnalyzing, setIsAnalyzing] = useState(false);
+  
+  // Encrypted API key states for users
+  const [encryptedApiKey, setEncryptedApiKey] = useState<string>('');
+  const [decryptedKey, setDecryptedKey] = useState<string>(() => {
+    return sessionStorage.getItem('gemini_api_key_temp') || '';
+  });
+
+  // Modal and Form States
+  const [apiKeyInput, setApiKeyInput] = useState('');
+  const [pinInput, setPinInput] = useState('');
+  const [unlockPin, setUnlockPin] = useState('');
+  const [showUnlockModal, setShowUnlockModal] = useState(false);
+  const [showApiGuideModal, setShowApiGuideModal] = useState(false);
+  const [statusMessage, setStatusMessage] = useState<{ text: string; isError: boolean } | null>(null);
 
   // State for image cropping flow
   const [tempCapturedImage, setTempCapturedImage] = useState<string | null>(null);
@@ -69,15 +84,21 @@ function App() {
         setCurrentUser('');
         setIsAdmin(false);
         setMistakes([]);
+        setEncryptedApiKey('');
+        setDecryptedKey('');
+        sessionStorage.removeItem('gemini_api_key_temp');
       }
     });
 
     return () => subscription.unsubscribe();
   }, []);
 
-  // Fetch mistakes from Supabase
+  // Fetch mistakes and user profile API key from Supabase
   const fetchUserData = async () => {
     try {
+      const { data: { user } } = await supabase.auth.getUser();
+      if (!user) return;
+
       const { data: dbMistakes, error: mistakesError } = await supabase
         .from('mistakes')
         .select('*')
@@ -94,18 +115,104 @@ function App() {
         reviews: m.reviews || ['', '', '']
       }));
       setMistakes(mappedMistakes);
+
+      // Fetch profile encrypted key
+      const { data: profileData, error: profileError } = await supabase
+        .from('profiles')
+        .select('encrypted_api_key')
+        .eq('id', user.id)
+        .maybeSingle();
+
+      if (profileData && !profileError) {
+        setEncryptedApiKey(profileData.encrypted_api_key || '');
+      }
     } catch (err) {
       console.error('Error loading Supabase user data:', err);
     }
   };
 
+  // Securely encrypt and save the API Key to Supabase database profiles table
+  const handleSaveApiKey = async (e: React.FormEvent) => {
+    e.preventDefault();
+    setStatusMessage(null);
+
+    if (!apiKeyInput.trim()) {
+      setStatusMessage({ text: 'API Key瑜??낅젰??二쇱꽭??', isError: true });
+      return;
+    }
+    if (pinInput.length < 4) {
+      setStatusMessage({ text: 'PIN 踰덊샇??理쒖냼 4?먮━ ?댁긽?댁뼱???⑸땲??', isError: true });
+      return;
+    }
+    
+    const { data: { user } } = await supabase.auth.getUser();
+    if (!user) return;
+
+    try {
+      const encrypted = await encryptApiKey(apiKeyInput.trim(), pinInput);
+      
+      const { error: profileError } = await supabase
+        .from('profiles')
+        .upsert({
+          id: user.id,
+          encrypted_api_key: encrypted,
+          updated_at: new Date().toISOString()
+        });
+
+      if (profileError) throw profileError;
+
+      setEncryptedApiKey(encrypted);
+      
+      // Save temporarily in session storage
+      setDecryptedKey(apiKeyInput.trim());
+      sessionStorage.setItem('gemini_api_key_temp', apiKeyInput.trim());
+
+      setApiKeyInput('');
+      setPinInput('');
+      setStatusMessage({ text: 'API Key媛 ?뷀샇?붾릺???대씪?곕뱶 ?꾨줈?꾩뿉 ?덉쟾?섍쾶 ?숆린?붾릺?덉뒿?덈떎.', isError: false });
+    } catch (err: any) {
+      console.error(err);
+      setStatusMessage({ text: `???以??ㅻ쪟 諛쒖깮: ${err.message}`, isError: true });
+    }
+  };
+
+  // Decrypt Key handler (used when analyzing)
+  const handleUnlockKey = async (e: React.FormEvent) => {
+    e.preventDefault();
+    setStatusMessage(null);
+
+    if (!encryptedApiKey) {
+      setStatusMessage({ text: '??λ맂 API Key媛 ?놁뒿?덈떎. ?ㅼ젙 ??뿉??癒쇱? ?깅줉??二쇱꽭??', isError: true });
+      return;
+    }
+
+    try {
+      const decrypted = await decryptApiKey(encryptedApiKey, unlockPin);
+      setDecryptedKey(decrypted);
+      sessionStorage.setItem('gemini_api_key_temp', decrypted);
+      setShowUnlockModal(false);
+      setUnlockPin('');
+      
+      if (selectedEntry) {
+        runAnalysisFlow(selectedEntry, decrypted);
+      }
+    } catch (err: any) {
+      setStatusMessage({ text: err.message || '蹂듯샇???ㅽ뙣', isError: true });
+    }
+  };
+
   // Start analysis trigger
   const handleStartAnalysis = (entry: MistakeEntry) => {
     const envKey = import.meta.env.VITE_GEMINI_API_KEY || '';
-    if (envKey) {
-      runAnalysisFlow(entry, envKey);
+    const activeKey = decryptedKey || envKey;
+
+    if (activeKey) {
+      runAnalysisFlow(entry, activeKey);
+    } else if (encryptedApiKey) {
+      setShowUnlockModal(true);
     } else {
-      alert('愿由ъ옄 ?쒕쾭??Gemini API Key(VITE_GEMINI_API_KEY)媛 ?깅줉?섏? ?딆븯?듬땲?? Vercel ?섍꼍 蹂???ㅼ젙???꾨즺??二쇱꽭??');
+      setActiveTab('settings');
+      alert('AI ?ㅻ떟 遺꾩꽍??吏꾪뻾?섎젮硫?癒쇱? ?ㅼ젙 ??뿉??Gemini API Key瑜??깅줉?섍굅??.env ?뚯씪??VITE_GEMINI_API_KEY瑜??ㅼ젙?댁빞 ?⑸땲??');
     }
   };
 
@@ -255,6 +362,9 @@ function App() {
     setIsAdmin(false);
     setSession(null);
     setMistakes([]);
+    setEncryptedApiKey('');
+    setDecryptedKey('');
+    sessionStorage.removeItem('gemini_api_key_temp');
     setActiveTab('notes');
   };
 
@@ -266,7 +376,13 @@ function App() {
     <div className="h-full flex flex-col bg-slate-950 text-slate-100 select-none">
       
       {/* Top Header */}
-      <Header currentUser={currentUser} onLogout={handleLogout} />
+      <Header 
+        currentUser={currentUser} 
+        onLogout={handleLogout} 
+        encryptedApiKey={encryptedApiKey}
+        decryptedKey={decryptedKey}
+        onShowApiGuide={() => setShowApiGuideModal(true)}
+      />
 
       {/* Main Content Area */}
       <main className="flex-1 overflow-y-auto px-4 py-6 pb-24">
@@ -313,6 +429,111 @@ function App() {
         {activeTab === 'admin' && isAdmin && (
           <AdminPanel />
         )}
+
+        {activeTab === 'settings' && (
+          <div className="space-y-6 max-w-lg mx-auto">
+            <div>
+              <h2 className="text-xl font-bold text-white">???ㅼ젙</h2>
+              <p className="text-xs text-slate-400 mt-1">API 蹂댁븞 ?ㅼ젙 諛????섍꼍?ㅼ젙</p>
+            </div>
+
+            {/* API Security Card */}
+            <form onSubmit={handleSaveApiKey} className="bg-slate-900/40 border border-slate-800 rounded-2xl p-5 space-y-4">
+              <div className="flex items-center space-x-3">
+                <div className="w-10 h-10 rounded-xl bg-indigo-950 border border-indigo-800 flex items-center justify-center text-xl text-indigo-400">
+                  ?뵎
+                </div>
+                <div>
+                  <h3 className="font-bold text-white text-sm">Gemini API 蹂댁븞 ?ㅼ젙</h3>
+                  <p className="text-[10px] text-slate-400">?대씪?곕뱶 ?꾨줈???????媛뺣젰 ?뷀샇???곸슜</p>
+                </div>
+              </div>
+              
+              <div className="space-y-3 pt-2">
+                <div className="space-y-1">
+                  <label className="text-xs text-slate-400 font-medium font-bold flex items-center justify-between">
+                    <span>Gemini API Key</span>
+                    <button
+                      type="button"
+                      onClick={() => setShowApiGuideModal(true)}
+                      className="text-[10px] text-indigo-400 hover:underline"
+                    >
+                      諛쒓툒 諛⑸쾿 蹂닿린 ?
+                    </button>
+                  </label>
+                  <input 
+                    type="password" 
+                    value={apiKeyInput}
+                    onChange={(e) => setApiKeyInput(e.target.value)}
+                    placeholder="AIzaSy..." 
+                    className="w-full px-4 py-3 rounded-xl bg-slate-950 border border-slate-800 focus:border-indigo-500 focus:ring-1 focus:ring-indigo-500 text-sm text-white placeholder-slate-600 outline-none transition-all"
+                  />
+                </div>
+                <div className="space-y-1">
+                  <label className="text-xs text-slate-400 font-medium font-bold">??蹂듯샇??PIN (4~6?먮━ ?レ옄)</label>
+                  <input 
+                    type="password" 
+                    maxLength={6}
+                    value={pinInput}
+                    onChange={(e) => setPinInput(e.target.value.replace(/[^0-9]/g, ''))}
+                    placeholder="?レ옄 4~6?먮━ ?낅젰" 
+                    className="w-full px-4 py-3 rounded-xl bg-slate-950 border border-slate-800 focus:border-indigo-500 focus:ring-1 focus:ring-indigo-500 text-sm text-white placeholder-slate-600 outline-none transition-all"
+                  />
+                  <p className="text-[9px] text-slate-500 leading-normal">
+                    * ?ㅼ젙??PIN? ??蹂듯샇?????몄쬆 ?섎떒?쇰줈 ?ъ슜?⑸땲?? ?대씪?곕뱶???곕룞 ??λ릺誘濡?湲곌린媛 諛붾뚯뼱????PIN留??덉쑝硫??몄젣???ㅻ? 遺덈윭? 遺꾩꽍?????덉뒿?덈떎.
+                  </p>
+                </div>
+
+                {statusMessage && (
+                  <div className={`p-3 rounded-lg text-xs ${statusMessage.isError ? 'bg-red-500/10 text-red-400 border border-red-500/20' : 'bg-emerald-500/10 text-emerald-400 border border-emerald-500/20'}`}>
+                    {statusMessage.text}
+                  </div>
+                )}
+
+                <button 
+                  type="submit"
+                  className="w-full py-3 mt-2 rounded-xl bg-indigo-600 hover:bg-indigo-500 active:scale-95 transition-all text-xs font-bold text-white shadow-md shadow-indigo-600/10"
+                >
+                  蹂댁븞 ??ν븯湲?+                </button>
+              </div>
+            </form>
+
+            {/* App Info Card */}
+            <div className="bg-slate-900/40 border border-slate-800 rounded-2xl p-5 space-y-3">
+              <h3 className="font-bold text-white text-sm">?좏뵆由ъ??댁뀡 ?뺣낫</h3>
+              <div className="divide-y divide-slate-800/60 text-xs text-slate-300">
+                <div className="flex justify-between py-2.5">
+                  <span className="text-slate-400">踰꾩쟾</span>
+                  <span className="text-slate-200 font-medium">v1.6.0 (PWA + Supabase)</span>
+                </div>
+                <div className="flex justify-between py-2.5">
+                  <span className="text-slate-400">?몄쬆 諛??곗씠?곕쿋?댁뒪</span>
+                  <span className="text-emerald-400 font-medium font-bold">Supabase Cloud</span>
+                </div>
+                <div className="flex justify-between py-2.5">
+                  <span className="text-slate-400">蹂댁븞 ?섏?</span>
+                  <span className="text-emerald-400 font-medium font-bold">AES-GCM ?뷀샇??/span>
+                </div>
+                {decryptedKey && (
+                  <div className="flex justify-between py-2.5 items-center">
+                    <span className="text-slate-400">?몄뀡 ?곹깭</span>
+                    <button 
+                      onClick={() => {
+                        setDecryptedKey('');
+                        sessionStorage.removeItem('gemini_api_key_temp');
+                        alert('API Key ?몄뀡??留뚮즺?섏뿀?듬땲?? ?ㅼ떆 ?덉쟾?섍쾶 ?좉꼈?듬땲??');
+                      }}
+                      className="px-2 py-1 bg-red-950 border border-red-800 text-red-400 rounded-lg text-[10px] font-bold"
+                    >
+                      ?몄뀡 硫붾え由??좉툑
+                    </button>
+                  </div>
+                )}
+              </div>
+            </div>
+          </div>
+        )}
       </main>
 
       {/* Selected Entry Detail Modal */}
@@ -336,6 +557,121 @@ function App() {
         />
       )}
 
+      {/* Unlock API Key PIN Modal */}
+      {showUnlockModal && (
+        <div className="fixed inset-0 z-50 flex items-center justify-center bg-black/80 backdrop-blur-sm p-4">
+          <form 
+            onSubmit={handleUnlockKey}
+            className="w-full max-w-sm bg-slate-900 border border-slate-800 rounded-3xl p-6 space-y-4 shadow-2xl animate-scale-up text-slate-100"
+          >
+            <div className="text-center space-y-2">
+              <div className="w-12 h-12 rounded-full bg-amber-500/10 border border-amber-500/30 text-2xl flex items-center justify-center mx-auto text-amber-400">
+                ?뵏
+              </div>
+              <h3 className="font-bold text-white text-base">蹂댁븞 蹂듯샇??/h3>
+              <p className="text-xs text-slate-400 max-w-xs mx-auto leading-relaxed">
+                ?뷀샇?붾맂 API Key瑜?濡쒕뱶?섍린 ?꾪빐 蹂댁븞 PIN 肄붾뱶瑜??낅젰??二쇱꽭??
+              </p>
+            </div>
+
+            <div className="space-y-3 pt-2">
+              <input 
+                type="password" 
+                maxLength={6}
+                value={unlockPin}
+                onChange={(e) => setUnlockPin(e.target.value.replace(/[^0-9]/g, ''))}
+                placeholder="?レ옄 4~6?먮━ PIN ?낅젰" 
+                className="w-full px-4 py-3 text-center tracking-widest text-lg rounded-xl bg-slate-950 border border-slate-850 focus:border-indigo-500 focus:ring-1 focus:ring-indigo-500 text-white placeholder-slate-600 outline-none transition-all"
+              />
+
+              {statusMessage && (
+                <div className="p-3 rounded-lg text-xs bg-red-500/10 text-red-400 border border-red-500/20">
+                  {statusMessage.text}
+                </div>
+              )}
+
+              <div className="flex space-x-3 pt-2">
+                <button 
+                  type="button"
+                  onClick={() => {
+                    setShowUnlockModal(false);
+                    setUnlockPin('');
+                    setStatusMessage(null);
+                  }}
+                  className="flex-1 py-3 rounded-xl border border-slate-800 hover:bg-slate-800 active:scale-95 text-xs font-semibold text-slate-300"
+                >
+                  痍⑥냼
+                </button>
+                <button 
+                  type="submit"
+                  className="flex-1 py-3 rounded-xl bg-indigo-600 hover:bg-indigo-500 active:scale-95 text-xs font-bold text-white shadow-md shadow-indigo-600/20"
+                >
+                  ?좉툑 ?댁젣
+                </button>
+              </div>
+            </div>
+          </form>
+        </div>
+      )}
+
+      {/* Gemini API Key Guide Modal */}
+      {showApiGuideModal && (
+        <div className="fixed inset-0 z-50 flex items-center justify-center bg-black/80 backdrop-blur-sm p-4">
+          <div className="w-full max-w-md bg-slate-900 border border-slate-800 rounded-3xl p-6 space-y-4 shadow-2xl animate-scale-up text-slate-100">
+            <div className="text-center space-y-2">
+              <div className="w-12 h-12 rounded-full bg-indigo-500/10 border border-indigo-500/30 text-xl flex items-center justify-center mx-auto text-indigo-400">
+                ?쨼
+              </div>
+              <h3 className="font-bold text-white text-base">Gemini API ??諛쒓툒 媛?대뱶</h3>
+              <p className="text-xs text-slate-400 max-w-xs mx-auto leading-relaxed">
+                AI ?ㅻ떟?명듃???먮룞 痍⑥빟??吏꾨떒 湲곕뒫???댁슜?섎젮硫?臾대즺 Google Gemini API ?ㅺ? ?꾩슂?⑸땲??
+              </p>
+            </div>
+
+            <div className="space-y-3 pt-2 text-left text-xs text-slate-300">
+              <div className="bg-slate-950 p-4 rounded-2xl border border-slate-800 space-y-3.5 leading-relaxed">
+                <div className="flex items-start space-x-2.5">
+                  <span className="w-5 h-5 rounded-full bg-indigo-900 text-indigo-300 text-[10px] font-bold flex items-center justify-center flex-none mt-0.5">1</span>
+                  <p>
+                    ?꾨옒 <strong>\'API ??諛쒓툒?섎윭 媛湲?'</strong> 踰꾪듉???대┃?섏뿬 Google AI Studio ?ъ씠?몃줈 ?대룞?⑸땲??(援ш? 怨꾩젙 ?꾩슂).
+                  </p>
+                </div>
+                <div className="flex items-start space-x-2.5">
+                  <span className="w-5 h-5 rounded-full bg-indigo-900 text-indigo-300 text-[10px] font-bold flex items-center justify-center flex-none mt-0.5">2</span>
+                  <p>
+                    濡쒓렇?????곷떒??<strong>"Get API key"</strong> ?먮뒗 <strong>"Create API key"</strong> 踰꾪듉???대┃?섏뿬 ???ㅻ? 諛쒓툒諛쏆뒿?덈떎.
+                  </p>
+                </div>
+                <div className="flex items-start space-x-2.5">
+                  <span className="w-5 h-5 rounded-full bg-indigo-900 text-indigo-300 text-[10px] font-bold flex items-center justify-center flex-none mt-0.5">3</span>
+                  <p>
+                    諛쒓툒????<code className="bg-slate-900 px-1.5 py-0.5 rounded text-indigo-400 text-[10px] font-mono">AIzaSy...</code>)瑜?蹂듭궗???? ?ㅻ떟?명듃 <strong>[?ㅼ젙]</strong> ??뿉??PIN 踰덊샇? ?④퍡 ?깅줉?⑸땲??
+                  </p>
+                </div>
+              </div>
+
+              <div className="flex space-x-3 pt-2">
+                <button
+                  type="button"
+                  onClick={() => setShowApiGuideModal(false)}
+                  className="flex-1 py-3 rounded-xl border border-slate-800 hover:bg-slate-800 active:scale-95 text-xs font-semibold text-slate-300"
+                >
+                  ?リ린
+                </button>
+                <a 
+                  href="https://aistudio.google.com/" 
+                  target="_blank" 
+                  rel="noopener noreferrer"
+                  className="flex-1 py-3 rounded-xl bg-gradient-to-r from-indigo-600 to-indigo-500 hover:from-indigo-500 hover:to-indigo-400 active:scale-95 text-xs font-bold text-white text-center shadow-md shadow-indigo-600/20 flex items-center justify-center"
+                >
+                  API ??諛쒓툒?섍린 &rarr;
+                </a>
+              </div>
+            </div>
+          </div>
+        </div>
+      )}
+
       {/* Floating Glassmorphic Bottom Navigation Bar */}
       <BottomNavigation activeTab={activeTab} setActiveTab={setActiveTab} isAdmin={isAdmin} />
 

commit eea7ba594e48f528f52aa5f8854dfb776057ad23
Author: 8xnvekjq <8xnvekjq@users.noreply.github.com>
Date:   Fri Jul 3 19:44:47 2026 +0900

    Fix: Resolve camera blackscreen freeze on cancel/retake, and perfectly align default crop box with centered camera viewfinder coordinates

diff --git a/src/App.tsx b/src/App.tsx
index 194a6e6..33df939 100644
--- a/src/App.tsx
+++ b/src/App.tsx
@@ -303,7 +303,7 @@ function App() {
           />
         )}
 
-        {activeTab === 'camera' && (
+        {activeTab === 'camera' && !tempCapturedImage && (
           <CameraScanner 
             onCapture={handleCameraCapture}
             onClose={() => setActiveTab('notes')}

commit fbeda70db7a2a50236c4770b4cf1f373ad630ffd
Author: 8xnvekjq <8xnvekjq@users.noreply.github.com>
Date:   Fri Jul 3 19:27:44 2026 +0900

    Feat: Pin 3-time incorrect/starred mistakes to top and add '?슚 吏묒쨷 怨듬왂 ?쎌젏' badge next to the title

diff --git a/src/App.tsx b/src/App.tsx
index 50a1561..194a6e6 100644
--- a/src/App.tsx
+++ b/src/App.tsx
@@ -272,7 +272,17 @@ function App() {
       <main className="flex-1 overflow-y-auto px-4 py-6 pb-24">
         {activeTab === 'notes' && (
           <MistakeList
-            mistakes={mistakes.filter(m => !(m.reviews?.filter(r => r === 'O').length === 3))}
+            mistakes={[...mistakes]
+              .filter(m => !(m.reviews?.filter(r => r === 'O').length === 3))
+              .sort((a, b) => {
+                const aStruggles = a.reviews ? a.reviews.filter(r => r === 'X' || r === 'star').length : 0;
+                const bStruggles = b.reviews ? b.reviews.filter(r => r === 'X' || r === 'star').length : 0;
+                
+                if (aStruggles === 3 && bStruggles !== 3) return -1;
+                if (bStruggles === 3 && aStruggles !== 3) return 1;
+                
+                return new Date(b.date).getTime() - new Date(a.date).getTime();
+              })}
             onSelectEntry={(entry) => setSelectedEntry(entry)}
             onDeleteMistake={handleDeleteMistake}
             onAddClick={() => setActiveTab('camera')}

commit 2a6a36e7b11a3216c453c4c4ee2a4e3fcbd43e22
Author: 8xnvekjq <8xnvekjq@users.noreply.github.com>
Date:   Fri Jul 3 19:21:49 2026 +0900

    Feat: Restore manual corner-dragging ImageCropper screen in camera capture flow

diff --git a/src/App.tsx b/src/App.tsx
index dcb4ba0..50a1561 100644
--- a/src/App.tsx
+++ b/src/App.tsx
@@ -10,6 +10,7 @@ import { Header } from './components/Header';
 import { MistakeList } from './components/MistakeList';
 import { MistakeDetailModal } from './components/MistakeDetailModal';
 import { BottomNavigation } from './components/BottomNavigation';
+import { ImageCropper } from './components/ImageCropper';
 import { AdminPanel } from './components/AdminPanel';
 
 function App() {
@@ -26,6 +27,9 @@ function App() {
   const [selectedEntry, setSelectedEntry] = useState<MistakeEntry | null>(null);
   const [isAnalyzing, setIsAnalyzing] = useState(false);
 
+  // State for image cropping flow
+  const [tempCapturedImage, setTempCapturedImage] = useState<string | null>(null);
+
   // Check admin status from profiles table
   const fetchAdminStatus = async (userId: string) => {
     try {
@@ -138,13 +142,19 @@ function App() {
     }
   };
 
-  // Process camera capture, upload directly to Storage, and insert database record (No cropping)
-  const handleCameraCapture = async (base64Image: string) => {
+  // Intercept camera capture and start cropping flow
+  const handleCameraCapture = (base64Image: string) => {
+    setTempCapturedImage(base64Image);
+  };
+
+  // Process crop completion, upload to Storage, and insert database record
+  const handleCropComplete = async (croppedBase64: string) => {
+    setTempCapturedImage(null);
     if (!session?.user) return;
 
     setIsAnalyzing(true);
     try {
-      const blob = base64ToBlob(base64Image);
+      const blob = base64ToBlob(croppedBase64);
       const fileExt = blob.type.split('/')[1] || 'jpg';
       const fileName = `${session.user.id}/${Date.now()}.${fileExt}`;
 
@@ -307,6 +317,15 @@ function App() {
         />
       )}
 
+      {/* Interactive Image Cropper Bounding Box overlay */}
+      {tempCapturedImage && (
+        <ImageCropper
+          imageSrc={tempCapturedImage}
+          onCropComplete={handleCropComplete}
+          onCancel={() => setTempCapturedImage(null)}
+        />
+      )}
+
       {/* Floating Glassmorphic Bottom Navigation Bar */}
       <BottomNavigation activeTab={activeTab} setActiveTab={setActiveTab} isAdmin={isAdmin} />
 

commit 54fbf2d6ec1f48dc1a80bc0c8e586f8d2c8a2a84
Author: 8xnvekjq <8xnvekjq@users.noreply.github.com>
Date:   Fri Jul 3 19:20:52 2026 +0900

    Feat: Remove image cropping step and crop preview, and make all analysis cards toggleable accordions

diff --git a/src/App.tsx b/src/App.tsx
index 1417567..dcb4ba0 100644
--- a/src/App.tsx
+++ b/src/App.tsx
@@ -10,7 +10,6 @@ import { Header } from './components/Header';
 import { MistakeList } from './components/MistakeList';
 import { MistakeDetailModal } from './components/MistakeDetailModal';
 import { BottomNavigation } from './components/BottomNavigation';
-import { ImageCropper } from './components/ImageCropper';
 import { AdminPanel } from './components/AdminPanel';
 
 function App() {
@@ -26,9 +25,6 @@ function App() {
   const [mistakes, setMistakes] = useState<MistakeEntry[]>([]);
   const [selectedEntry, setSelectedEntry] = useState<MistakeEntry | null>(null);
   const [isAnalyzing, setIsAnalyzing] = useState(false);
-  
-  // State for image cropping flow
-  const [tempCapturedImage, setTempCapturedImage] = useState<string | null>(null);
 
   // Check admin status from profiles table
   const fetchAdminStatus = async (userId: string) => {
@@ -142,19 +138,13 @@ function App() {
     }
   };
 
-  // Intercept camera capture and start cropping flow
-  const handleCameraCapture = (base64Image: string) => {
-    setTempCapturedImage(base64Image);
-  };
-
-  // Process crop completion, upload to Storage, and insert database record
-  const handleCropComplete = async (croppedBase64: string) => {
-    setTempCapturedImage(null);
+  // Process camera capture, upload directly to Storage, and insert database record (No cropping)
+  const handleCameraCapture = async (base64Image: string) => {
     if (!session?.user) return;
 
     setIsAnalyzing(true);
     try {
-      const blob = base64ToBlob(croppedBase64);
+      const blob = base64ToBlob(base64Image);
       const fileExt = blob.type.split('/')[1] || 'jpg';
       const fileName = `${session.user.id}/${Date.now()}.${fileExt}`;
 
@@ -317,15 +307,6 @@ function App() {
         />
       )}
 
-      {/* Interactive Image Cropper Bounding Box overlay */}
-      {tempCapturedImage && (
-        <ImageCropper
-          imageSrc={tempCapturedImage}
-          onCropComplete={handleCropComplete}
-          onCancel={() => setTempCapturedImage(null)}
-        />
-      )}
-
       {/* Floating Glassmorphic Bottom Navigation Bar */}
       <BottomNavigation activeTab={activeTab} setActiveTab={setActiveTab} isAdmin={isAdmin} />
 

commit 59058f6c7bfc9969bdfcd3eed6ea9eb6fee60b42
Author: 8xnvekjq <8xnvekjq@users.noreply.github.com>
Date:   Fri Jul 3 18:41:08 2026 +0900

    Feat: Add admin dashboard tab with per-user mistake stats (requires Supabase migration)

diff --git a/src/App.tsx b/src/App.tsx
index f3c72e6..1417567 100644
--- a/src/App.tsx
+++ b/src/App.tsx
@@ -11,6 +11,7 @@ import { MistakeList } from './components/MistakeList';
 import { MistakeDetailModal } from './components/MistakeDetailModal';
 import { BottomNavigation } from './components/BottomNavigation';
 import { ImageCropper } from './components/ImageCropper';
+import { AdminPanel } from './components/AdminPanel';
 
 function App() {
   // If Supabase credentials are not configured, block and show the setup guide
@@ -20,6 +21,7 @@ function App() {
 
   const [session, setSession] = useState<any>(null);
   const [currentUser, setCurrentUser] = useState<string>('');
+  const [isAdmin, setIsAdmin] = useState(false);
   const [activeTab, setActiveTab] = useState<ActiveTab>('notes');
   const [mistakes, setMistakes] = useState<MistakeEntry[]>([]);
   const [selectedEntry, setSelectedEntry] = useState<MistakeEntry | null>(null);
@@ -28,6 +30,20 @@ function App() {
   // State for image cropping flow
   const [tempCapturedImage, setTempCapturedImage] = useState<string | null>(null);
 
+  // Check admin status from profiles table
+  const fetchAdminStatus = async (userId: string) => {
+    try {
+      const { data } = await supabase
+        .from('profiles')
+        .select('is_admin')
+        .eq('id', userId)
+        .single();
+      setIsAdmin(data?.is_admin === true);
+    } catch {
+      setIsAdmin(false);
+    }
+  };
+
   // Monitor Supabase Authentication States
   useEffect(() => {
     // 1. Get initial session
@@ -37,6 +53,7 @@ function App() {
         const username = session.user.email?.split('@')[0] || 'User';
         setCurrentUser(username);
         fetchUserData();
+        fetchAdminStatus(session.user.id);
       }
     });
 
@@ -47,8 +64,10 @@ function App() {
         const username = session.user.email?.split('@')[0] || 'User';
         setCurrentUser(username);
         fetchUserData();
+        fetchAdminStatus(session.user.id);
       } else {
         setCurrentUser('');
+        setIsAdmin(false);
         setMistakes([]);
       }
     });
@@ -233,6 +252,7 @@ function App() {
   const handleLogout = async () => {
     await supabase.auth.signOut();
     setCurrentUser('');
+    setIsAdmin(false);
     setSession(null);
     setMistakes([]);
     setActiveTab('notes');
@@ -279,6 +299,10 @@ function App() {
             onClose={() => setActiveTab('notes')}
           />
         )}
+
+        {activeTab === 'admin' && isAdmin && (
+          <AdminPanel />
+        )}
       </main>
 
       {/* Selected Entry Detail Modal */}
@@ -303,7 +327,7 @@ function App() {
       )}
 
       {/* Floating Glassmorphic Bottom Navigation Bar */}
-      <BottomNavigation activeTab={activeTab} setActiveTab={setActiveTab} />
+      <BottomNavigation activeTab={activeTab} setActiveTab={setActiveTab} isAdmin={isAdmin} />
 
     </div>
   );

commit 3785a38ccfaa22f47f78e9e9321d2e51da403a15
Author: 8xnvekjq <8xnvekjq@users.noreply.github.com>
Date:   Fri Jul 3 14:45:43 2026 +0900

    Feat: Add interactive ImageCropper component and connect it to scanner flow to crop specific problems before upload

diff --git a/src/App.tsx b/src/App.tsx
index d28edf3..f3c72e6 100644
--- a/src/App.tsx
+++ b/src/App.tsx
@@ -10,6 +10,7 @@ import { Header } from './components/Header';
 import { MistakeList } from './components/MistakeList';
 import { MistakeDetailModal } from './components/MistakeDetailModal';
 import { BottomNavigation } from './components/BottomNavigation';
+import { ImageCropper } from './components/ImageCropper';
 
 function App() {
   // If Supabase credentials are not configured, block and show the setup guide
@@ -23,6 +24,9 @@ function App() {
   const [mistakes, setMistakes] = useState<MistakeEntry[]>([]);
   const [selectedEntry, setSelectedEntry] = useState<MistakeEntry | null>(null);
   const [isAnalyzing, setIsAnalyzing] = useState(false);
+  
+  // State for image cropping flow
+  const [tempCapturedImage, setTempCapturedImage] = useState<string | null>(null);
 
   // Monitor Supabase Authentication States
   useEffect(() => {
@@ -119,13 +123,19 @@ function App() {
     }
   };
 
-  // Handle camera capture, upload to Storage, and insert database record
-  const handleCameraCapture = async (base64Image: string) => {
+  // Intercept camera capture and start cropping flow
+  const handleCameraCapture = (base64Image: string) => {
+    setTempCapturedImage(base64Image);
+  };
+
+  // Process crop completion, upload to Storage, and insert database record
+  const handleCropComplete = async (croppedBase64: string) => {
+    setTempCapturedImage(null);
     if (!session?.user) return;
 
     setIsAnalyzing(true);
     try {
-      const blob = base64ToBlob(base64Image);
+      const blob = base64ToBlob(croppedBase64);
       const fileExt = blob.type.split('/')[1] || 'jpg';
       const fileName = `${session.user.id}/${Date.now()}.${fileExt}`;
 
@@ -283,6 +293,15 @@ function App() {
         />
       )}
 
+      {/* Interactive Image Cropper Bounding Box overlay */}
+      {tempCapturedImage && (
+        <ImageCropper
+          imageSrc={tempCapturedImage}
+          onCropComplete={handleCropComplete}
+          onCancel={() => setTempCapturedImage(null)}
+        />
+      )}
+
       {/* Floating Glassmorphic Bottom Navigation Bar */}
       <BottomNavigation activeTab={activeTab} setActiveTab={setActiveTab} />
 

commit 8351ffeafccd73932f3f532146161635aff0304f
Author: 8xnvekjq <8xnvekjq@users.noreply.github.com>
Date:   Fri Jul 3 14:09:58 2026 +0900

    Feat: Add 3-step review status selector (O/X/Star) and Completed Reviews tab filtering

diff --git a/src/App.tsx b/src/App.tsx
index 023f1dd..d28edf3 100644
--- a/src/App.tsx
+++ b/src/App.tsx
@@ -1,5 +1,5 @@
 import { useState, useEffect } from 'react';
-import type { ActiveTab, MistakeEntry } from './types';
+import type { ActiveTab, MistakeEntry, ReviewState } from './types';
 import { CameraScanner } from './components/CameraScanner';
 import { analyzeMistakeWithGemini } from './services/gemini';
 import { AuthScreen } from './components/AuthScreen';
@@ -67,7 +67,8 @@ function App() {
         title: m.title,
         imageUrl: m.image_url,
         date: m.date,
-        analysis: m.analysis || undefined
+        analysis: m.analysis || undefined,
+        reviews: m.reviews || ['', '', '']
       }));
       setMistakes(mappedMistakes);
     } catch (err) {
@@ -150,7 +151,8 @@ function App() {
           user_id: session.user.id,
           title: newEntryTitle,
           image_url: publicUrl,
-          analysis: null
+          analysis: null,
+          reviews: ['', '', '']
         })
         .select()
         .single();
@@ -162,7 +164,8 @@ function App() {
         title: dbEntry.title,
         imageUrl: dbEntry.image_url,
         date: dbEntry.date,
-        analysis: undefined
+        analysis: undefined,
+        reviews: ['', '', '']
       };
       
       setMistakes(prev => [newEntry, ...prev]);
@@ -197,6 +200,26 @@ function App() {
     }
   };
 
+  // Update reviews list in Supabase & local state
+  const handleUpdateReviews = async (id: string, newReviews: ReviewState[]) => {
+    try {
+      const { error } = await supabase
+        .from('mistakes')
+        .update({ reviews: newReviews })
+        .eq('id', id);
+
+      if (error) throw error;
+
+      setMistakes(prev => prev.map(m => m.id === id ? { ...m, reviews: newReviews } : m));
+      setSelectedEntry(prev => prev && prev.id === id ? { ...prev, reviews: newReviews } : prev);
+    } catch (err: any) {
+      console.error('Failed to update reviews:', err);
+      // Fallback: update local React state anyway for immediate validation
+      setMistakes(prev => prev.map(m => m.id === id ? { ...m, reviews: newReviews } : m));
+      setSelectedEntry(prev => prev && prev.id === id ? { ...prev, reviews: newReviews } : prev);
+    }
+  };
+
   const handleLogout = async () => {
     await supabase.auth.signOut();
     setCurrentUser('');
@@ -219,10 +242,24 @@ function App() {
       <main className="flex-1 overflow-y-auto px-4 py-6 pb-24">
         {activeTab === 'notes' && (
           <MistakeList
-            mistakes={mistakes}
+            mistakes={mistakes.filter(m => !(m.reviews?.filter(r => r === 'O').length === 3))}
+            onSelectEntry={(entry) => setSelectedEntry(entry)}
+            onDeleteMistake={handleDeleteMistake}
+            onAddClick={() => setActiveTab('camera')}
+            title="?섏쓽 ?ㅻ떟?명듃"
+            emptyMessage="?꾩쭅 ?깅줉???ㅻ떟???놁뒿?덈떎. ?꾨옒 移대찓??踰꾪듉???뚮윭 ?섑븰 臾몄젣瑜?珥ъ쁺?섍퀬 AI??留욎땄 遺꾩꽍??諛쏆븘蹂댁꽭??"
+          />
+        )}
+
+        {activeTab === 'completed' && (
+          <MistakeList
+            mistakes={mistakes.filter(m => m.reviews?.filter(r => r === 'O').length === 3)}
             onSelectEntry={(entry) => setSelectedEntry(entry)}
             onDeleteMistake={handleDeleteMistake}
             onAddClick={() => setActiveTab('camera')}
+            title="蹂듭뒿 ?꾨즺 蹂닿???
+            hideAddButton={true}
+            emptyMessage="?꾩쭅 ?꾩쟾??蹂듭뒿 ?꾨즺(O 3???ъ꽦)???ㅻ떟???놁뒿?덈떎. ?댁떖???ㅻ떟??蹂듭뒿?섏뿬 ?뺣났??蹂댁꽭??"
           />
         )}
 
@@ -242,6 +279,7 @@ function App() {
           onClose={() => setSelectedEntry(null)}
           onDeleteMistake={handleDeleteMistake}
           onStartAnalysis={handleStartAnalysis}
+          onUpdateReviews={handleUpdateReviews}
         />
       )}
 

commit 7e5faf4fdff6a57bfd71a66adbf3518dccc9cd4f
Author: 8xnvekjq <8xnvekjq@users.noreply.github.com>
Date:   Fri Jul 3 14:00:31 2026 +0900

    Fix: Resolve TS6133 unused parameter error on App.tsx

diff --git a/src/App.tsx b/src/App.tsx
index 7394c06..023f1dd 100644
--- a/src/App.tsx
+++ b/src/App.tsx
@@ -32,7 +32,7 @@ function App() {
       if (session?.user) {
         const username = session.user.email?.split('@')[0] || 'User';
         setCurrentUser(username);
-        fetchUserData(session.user.id);
+        fetchUserData();
       }
     });
 
@@ -42,7 +42,7 @@ function App() {
       if (session?.user) {
         const username = session.user.email?.split('@')[0] || 'User';
         setCurrentUser(username);
-        fetchUserData(session.user.id);
+        fetchUserData();
       } else {
         setCurrentUser('');
         setMistakes([]);
@@ -53,7 +53,7 @@ function App() {
   }, []);
 
   // Fetch mistakes from Supabase
-  const fetchUserData = async (userId: string) => {
+  const fetchUserData = async () => {
     try {
       const { data: dbMistakes, error: mistakesError } = await supabase
         .from('mistakes')

commit 365a82745391701a63bbd123efd35b24cb675417
Author: 8xnvekjq <8xnvekjq@users.noreply.github.com>
Date:   Fri Jul 3 13:50:14 2026 +0900

    Refactor: Modularize App.tsx into components and utilities, and fix safe area layout

diff --git a/src/App.tsx b/src/App.tsx
index e865170..7394c06 100644
--- a/src/App.tsx
+++ b/src/App.tsx
@@ -3,51 +3,18 @@ import type { ActiveTab, MistakeEntry } from './types';
 import { CameraScanner } from './components/CameraScanner';
 import { analyzeMistakeWithGemini } from './services/gemini';
 import { AuthScreen } from './components/AuthScreen';
-import { LaTeXRenderer } from './components/LaTeXRenderer';
 import { supabase, isSupabaseConfigured } from './services/supabase';
-
-// Helper to convert Base64 Data URL to Blob for Storage upload
-const base64ToBlob = (base64DataUrl: string): Blob => {
-  const parts = base64DataUrl.split(';base64,');
-  const contentType = parts[0].split(':')[1];
-  const raw = window.atob(parts[1]);
-  const rawLength = raw.length;
-  const uInt8Array = new Uint8Array(rawLength);
-  for (let i = 0; i < rawLength; ++i) {
-    uInt8Array[i] = raw.charCodeAt(i);
-  }
-  return new Blob([uInt8Array], { type: contentType });
-};
+import { base64ToBlob } from './utils/image';
+import { SupabaseConfigWarning } from './components/SupabaseConfigWarning';
+import { Header } from './components/Header';
+import { MistakeList } from './components/MistakeList';
+import { MistakeDetailModal } from './components/MistakeDetailModal';
+import { BottomNavigation } from './components/BottomNavigation';
 
 function App() {
   // If Supabase credentials are not configured, block and show the setup guide
   if (!isSupabaseConfigured) {
-    return (
-      <div className="h-full flex items-center justify-center p-6 bg-slate-950 text-slate-100 text-center">
-        <div className="max-w-md w-full p-8 bg-slate-900/60 border border-slate-800 rounded-3xl space-y-6 shadow-2xl backdrop-blur-md animate-scale-up">
-          <div className="w-16 h-16 mx-auto rounded-2xl bg-red-500/10 border border-red-500/30 flex items-center justify-center text-3xl">
-            ?좑툘
-          </div>
-          <div className="space-y-2">
-            <h2 className="text-xl font-black text-white">Supabase ?ㅼ젙 ?꾨씫</h2>
-            <p className="text-xs text-slate-400 leading-relaxed">
-              ?좏뵆由ъ??댁뀡??援щ룞?섍린 ?꾪븳 Supabase ?곕룞 ?뺣낫媛 濡쒖뺄 ?섍꼍 ?뚯씪???ㅼ젙?섏? ?딆븯?듬땲??
-            </p>
-          </div>
-          <div className="space-y-3 pt-2 text-left">
-            <p className="text-xs font-bold text-slate-300">?닿껐 諛⑸쾿:</p>
-            <ol className="text-[11px] text-slate-400 space-y-2 list-decimal list-inside leading-relaxed bg-slate-950 p-4 rounded-xl border border-slate-800">
-              <li>?꾨줈?앺듃 猷⑦듃??<code className="bg-slate-900 px-1 py-0.5 rounded text-white font-mono">.env</code> ?뚯씪???앹꽦?⑸땲??</li>
-              <li>?꾨옒? 媛숈씠 諛쒓툒諛쏆? Supabase URL怨?Key瑜??묒꽦?⑸땲??</li>
-            </ol>
-            <div className="bg-slate-950 p-4 rounded-xl text-left font-mono text-[10px] text-indigo-400 border border-slate-850 select-text leading-normal">
-              VITE_SUPABASE_URL=https://wcvkmdvrowljypueucwx.supabase.co<br />
-              VITE_SUPABASE_ANON_KEY=sb_publishable_BtAepfY-CiPuAsOSxJ_Y8w_ZTNZ7UWI
-            </div>
-          </div>
-        </div>
-      </div>
-    );
+    return <SupabaseConfigWarning />;
   }
 
   const [session, setSession] = useState<any>(null);
@@ -108,12 +75,6 @@ function App() {
     }
   };
 
-  // Format date helper
-  const formatDate = (dateStr: string) => {
-    const d = new Date(dateStr);
-    return `${d.getFullYear()}.${String(d.getMonth() + 1).padStart(2, '0')}.${String(d.getDate()).padStart(2, '0')}`;
-  };
-
   // Start analysis trigger
   const handleStartAnalysis = (entry: MistakeEntry) => {
     const envKey = import.meta.env.VITE_GEMINI_API_KEY || '';
@@ -252,109 +213,17 @@ function App() {
     <div className="h-full flex flex-col bg-slate-950 text-slate-100 select-none">
       
       {/* Top Header */}
-      <header className="safe-top flex-none border-b border-slate-800 bg-slate-900/90 backdrop-blur-md px-6 py-4 flex items-center justify-between sticky top-0 z-30">
-        <div className="flex items-center space-x-2">
-          <div className="w-8 h-8 rounded-lg bg-gradient-to-tr from-indigo-600 to-emerald-400 flex items-center justify-center font-bold text-white shadow-lg shadow-indigo-500/20">
-            ??-          </div>
-          <h1 className="text-lg font-bold bg-gradient-to-r from-white to-slate-300 bg-clip-text text-transparent">
-            ?붿퓼?ㅼ닔???ㅻ떟?대━??-          </h1>
-        </div>
-        <div className="flex items-center space-x-2">
-          <span className="text-[10px] text-slate-400 bg-slate-800/80 px-2 py-0.5 rounded-full border border-slate-700 font-bold max-w-[100px] truncate">
-            ?뫀 {currentUser}
-          </span>
-          <button 
-            onClick={handleLogout}
-            className="text-[10px] text-red-400 hover:text-red-300 font-bold bg-red-950/20 px-2.5 py-0.5 rounded-full border border-red-900/30 transition-colors"
-          >
-            濡쒓렇?꾩썐
-          </button>
-        </div>
-      </header>
+      <Header currentUser={currentUser} onLogout={handleLogout} />
 
       {/* Main Content Area */}
       <main className="flex-1 overflow-y-auto px-4 py-6 pb-24">
-        
         {activeTab === 'notes' && (
-          <div className="space-y-6">
-            <div className="flex items-center justify-between">
-              <div>
-                <h2 className="text-xl font-bold text-white">?섏쓽 ?ㅻ떟?명듃</h2>
-                <p className="text-xs text-slate-400 mt-1">珥?{mistakes.length}媛쒖쓽 遺꾩꽍??痍⑥빟?먯씠 ?덉뒿?덈떎.</p>
-              </div>
-              <button 
-                onClick={() => setActiveTab('camera')}
-                className="px-3.5 py-1.5 rounded-full text-xs font-semibold bg-indigo-600 hover:bg-indigo-500 active:scale-95 text-white transition-all shadow-md shadow-indigo-600/20"
-              >
-                + ???ㅻ떟 異붽?
-              </button>
-            </div>
-
-            {/* List of Mistakes */}
-            {mistakes.length === 0 ? (
-              <div className="flex flex-col items-center justify-center py-20 text-center border-2 border-dashed border-slate-800 rounded-2xl p-6 bg-slate-900/20 animate-scale-up">
-                <div className="w-16 h-16 rounded-full bg-slate-900 flex items-center justify-center text-2xl mb-4 text-slate-500">
-                  ?뱭
-                </div>
-                <p className="text-slate-300 font-medium">?꾩쭅 ?깅줉???ㅻ떟???놁뒿?덈떎</p>
-                <p className="text-xs text-slate-500 mt-1 max-w-xs leading-relaxed">
-                  ?꾨옒 移대찓??踰꾪듉???뚮윭 ?섑븰 臾몄젣瑜?珥ъ쁺?섍퀬 AI??留욎땄 遺꾩꽍??諛쏆븘蹂댁꽭??
-                </p>
-              </div>
-            ) : (
-              <div className="grid gap-4 sm:grid-cols-2">
-                {mistakes.map((entry) => (
-                  <div 
-                    key={entry.id}
-                    onClick={() => setSelectedEntry(entry)}
-                    className="group bg-slate-900/50 hover:bg-slate-900 border border-slate-800 hover:border-indigo-500/40 rounded-2xl overflow-hidden cursor-pointer transition-all duration-300 transform active:scale-[0.98]"
-                  >
-                    <div className="aspect-[16/9] w-full bg-slate-950 relative overflow-hidden flex items-center justify-center">
-                      <img 
-                        src={entry.imageUrl} 
-                        alt={entry.title}
-                        className="w-full h-full object-cover opacity-80 group-hover:scale-105 transition-transform duration-500" 
-                      />
-                      <button
-                        onClick={(e) => handleDeleteMistake(entry.id, e)}
-                        className="absolute top-2 left-2 w-7 h-7 rounded-full bg-black/60 hover:bg-red-600 flex items-center justify-center text-white text-xs opacity-0 group-hover:opacity-100 transition-opacity"
-                      >
-                        ?뿊截?-                      </button>
-                      <div className="absolute top-2 right-2 px-2.5 py-0.5 rounded-full text-[10px] font-semibold bg-slate-950/80 backdrop-blur-sm border border-slate-800 text-slate-300">
-                        {formatDate(entry.date)}
-                      </div>
-                    </div>
-                    <div className="p-4">
-                      <h3 className="font-bold text-white line-clamp-1 group-hover:text-indigo-400 transition-colors">
-                        {entry.title}
-                      </h3>
-                      {entry.analysis ? (
-                        <p className="text-xs text-slate-400 mt-1.5 line-clamp-2 leading-relaxed">
-                          ?뵇 {entry.analysis.mistakeDetail}
-                        </p>
-                      ) : (
-                        <p className="text-xs text-amber-400 mt-1.5 font-medium flex items-center">
-                          <span className="w-1.5 h-1.5 rounded-full bg-amber-400 mr-1.5 animate-pulse"></span>
-                          AI 遺꾩꽍 誘몄셿猷?(?ㅼ틪??
-                        </p>
-                      )}
-                      <div className="flex items-center justify-between mt-4 pt-3 border-t border-slate-800/60">
-                        <span className="text-[10px] text-slate-500">
-                          {entry.analysis ? 'AI ?쇰뱶諛??꾨즺' : '遺꾩꽍 ?湲?以?}
-                        </span>
-                        <span className="text-xs text-indigo-400 font-semibold group-hover:translate-x-1 transition-transform inline-flex items-center">
-                          {entry.analysis ? '?곸꽭蹂닿린' : '遺꾩꽍?섍린'} &rarr;
-                        </span>
-                      </div>
-                    </div>
-                  </div>
-                ))}
-              </div>
-            )}
-          </div>
+          <MistakeList
+            mistakes={mistakes}
+            onSelectEntry={(entry) => setSelectedEntry(entry)}
+            onDeleteMistake={handleDeleteMistake}
+            onAddClick={() => setActiveTab('camera')}
+          />
         )}
 
         {activeTab === 'camera' && (
@@ -363,146 +232,21 @@ function App() {
             onClose={() => setActiveTab('notes')}
           />
         )}
-
       </main>
 
       {/* Selected Entry Detail Modal */}
       {selectedEntry && (
-        <div className="fixed inset-0 z-50 flex items-end justify-center bg-black/70 backdrop-blur-sm p-0 sm:p-4">
-          <div className="w-full max-w-xl bg-slate-900 border-t sm:border border-slate-800 rounded-t-3xl sm:rounded-3xl max-h-[90vh] flex flex-col shadow-2xl overflow-hidden animate-slide-up">
-            
-            {/* Modal Header */}
-            <div className="px-6 py-4 border-b border-slate-800/80 flex items-center justify-between bg-slate-900/80 sticky top-0">
-              <div className="pr-4 flex-1">
-                <span className="text-[10px] text-slate-500 font-bold block uppercase tracking-wider">{formatDate(selectedEntry.date)}</span>
-                <h3 className="font-bold text-white text-base line-clamp-1">{selectedEntry.title}</h3>
-              </div>
-              <button 
-                onClick={() => setSelectedEntry(null)}
-                className="w-8 h-8 rounded-full bg-slate-800 hover:bg-slate-700 active:scale-90 flex items-center justify-center text-slate-400 text-lg transition-all flex-none"
-              >
-                ??-              </button>
-            </div>
-
-            {/* Modal Body */}
-            <div className="flex-1 overflow-y-auto p-6 space-y-6">
-              
-              {/* Problem Image Preview */}
-              <div className="aspect-[16/9] w-full rounded-2xl overflow-hidden bg-slate-950 border border-slate-800 flex items-center justify-center relative">
-                <img src={selectedEntry.imageUrl} alt={selectedEntry.title} className="w-full h-full object-cover" />
-                <button
-                  onClick={(e) => handleDeleteMistake(selectedEntry.id, e)}
-                  className="absolute bottom-3 right-3 px-3 py-1.5 rounded-xl bg-red-600/90 hover:bg-red-600 text-white text-xs font-semibold shadow-lg backdrop-blur-sm transition-all"
-                >
-                  湲곕줉 ??젣
-                </button>
-              </div>
-
-              {isAnalyzing ? (
-                <div className="py-12 flex flex-col items-center justify-center space-y-4">
-                  <div className="w-12 h-12 border-4 border-indigo-500 border-t-transparent rounded-full animate-spin"></div>
-                  <div className="text-center">
-                    <p className="text-sm font-semibold text-white">泥섎━ 以?..</p>
-                    <p className="text-xs text-slate-400 mt-1">AI ?섑븰 ?대━??吏꾨떒???묒꽦?섍퀬 ?덉뒿?덈떎.</p>
-                  </div>
-                </div>
-              ) : selectedEntry.analysis ? (
-                <div className="space-y-6 animate-scale-up">
-                  {/* Card 1: ?뺤꽍 ???怨쇱젙 */}
-                  <div className="space-y-2 border-l-4 border-indigo-500 pl-4 py-1">
-                    <h4 className="text-sm font-extrabold text-indigo-400 flex items-center">
-                      <span className="mr-1.5 text-base">?뮕</span> ?뺤꽍 ???怨쇱젙
-                    </h4>
-                    <div className="bg-slate-950 p-4.5 rounded-2xl border border-slate-850">
-                      <LaTeXRenderer text={selectedEntry.analysis.solvingProcess} className="text-sm md:text-base leading-relaxed" />
-                    </div>
-                  </div>
-
-                  {/* Card 2: ?ㅼ닔 & ?由??댁쑀 遺꾩꽍 (?붿빟 諛?媛뺤“) */}
-                  <div className="space-y-2 border-l-4 border-amber-500 pl-4 py-1">
-                    <h4 className="text-sm font-extrabold text-amber-400 flex items-center">
-                      <span className="mr-1.5 text-base">?뵇</span> ?由??댁쑀 遺꾩꽍
-                    </h4>
-                    <div className="bg-slate-950 p-4.5 rounded-2xl border border-slate-850 space-y-4">
-                      {/* ?ㅼ닔 遺꾩꽍 ?곸꽭 */}
-                      <div className="space-y-1.5">
-                        <span className="text-[11px] font-bold text-slate-500 uppercase tracking-wider block">?ㅼ닔 吏??/span>
-                        <LaTeXRenderer text={selectedEntry.analysis.mistakeDetail} className="text-sm md:text-base leading-relaxed" />
-                      </div>
-                      
-                      {/* ?ㅺ컻??洹쇰낯 ?먯씤 */}
-                      <div className="pt-3.5 border-t border-slate-850 space-y-1.5">
-                        <span className="text-[11px] font-bold text-slate-500 uppercase tracking-wider block">?由??먯씤</span>
-                        <LaTeXRenderer text={selectedEntry.analysis.rootCause} className="text-sm md:text-base leading-relaxed text-red-300" />
-                      </div>
-                    </div>
-                  </div>
-
-                  {/* Card 3: ?щ컻 諛⑹? ?梨?(?붿빟 諛?湲癒몃━ 媛뺤“) */}
-                  <div className="space-y-2 border-l-4 border-emerald-500 pl-4 py-1">
-                    <h4 className="text-sm font-extrabold text-emerald-400 flex items-center">
-                      <span className="mr-1.5 text-base">?썳截?/span> ?щ컻 諛⑹? ?梨?-                    </h4>
-                    <div className="bg-slate-950 p-4.5 rounded-2xl border border-slate-850">
-                      <LaTeXRenderer text={selectedEntry.analysis.actionPlan} className="text-sm md:text-base leading-relaxed" />
-                    </div>
-                  </div>
-                </div>
-              ) : (
-                <div className="py-8 bg-slate-950/60 rounded-2xl border border-slate-800 p-6 text-center space-y-4">
-                  <div className="text-3xl">?쨼</div>
-                  <div className="space-y-1">
-                    <p className="text-sm font-bold text-white">AI ?섑븰 ?대━??吏꾨떒</p>
-                    <p className="text-xs text-slate-400 leading-relaxed max-w-xs mx-auto">
-                      ?꾩쭅 ?ㅻ떟 ?먯씤??遺꾩꽍?섏? ?딆븯?듬땲?? AI媛 ?ㅺ퀎?섎뒗 留욎땄???ㅻ떟 泥섎갑?꾩쓣 ?뺤씤??蹂댁꽭??
-                    </p>
-                  </div>
-                  <button 
-                    onClick={() => handleStartAnalysis(selectedEntry)}
-                    className="px-6 py-2.5 rounded-full bg-gradient-to-r from-indigo-600 to-indigo-500 active:scale-95 transition-all text-xs font-bold text-white shadow-md shadow-indigo-600/20"
-                  >
-                    AI 遺꾩꽍 ?쒖옉?섍린
-                  </button>
-                </div>
-              )}
-            </div>
-
-            {/* Modal Footer */}
-            <div className="p-4 border-t border-slate-800/80 bg-slate-950/60 flex space-x-3">
-              <button 
-                onClick={() => setSelectedEntry(null)}
-                className="flex-1 py-3 rounded-xl border border-slate-800 hover:border-slate-700 active:scale-95 transition-all text-xs font-semibold text-slate-300"
-              >
-                ?リ린
-              </button>
-            </div>
-
-          </div>
-        </div>
+        <MistakeDetailModal
+          selectedEntry={selectedEntry}
+          isAnalyzing={isAnalyzing}
+          onClose={() => setSelectedEntry(null)}
+          onDeleteMistake={handleDeleteMistake}
+          onStartAnalysis={handleStartAnalysis}
+        />
       )}
 
       {/* Floating Glassmorphic Bottom Navigation Bar */}
-      <nav className="fixed bottom-4 left-4 right-4 z-40 h-16 rounded-2xl border border-slate-800/80 bg-slate-900/75 backdrop-blur-lg flex items-center justify-around px-4 shadow-xl shadow-black/40">
-        
-        {/* Tab 1: Notes List */}
-        <button 
-          onClick={() => setActiveTab('notes')}
-          className={`flex flex-col items-center justify-center w-16 h-12 rounded-xl transition-all ${activeTab === 'notes' ? 'text-indigo-400 scale-105 font-bold' : 'text-slate-500 hover:text-slate-300'}`}
-        >
-          <span className="text-lg">?뱭</span>
-          <span className="text-[10px] mt-0.5">?ㅻ떟?명듃</span>
-        </button>
-
-        {/* Tab 2: Floating Camera Trigger */}
-        <button 
-          onClick={() => setActiveTab('camera')}
-          className={`flex items-center justify-center w-14 h-14 rounded-full transition-all -translate-y-4 shadow-lg ${activeTab === 'camera' ? 'bg-gradient-to-tr from-indigo-600 to-indigo-500 text-white scale-110 shadow-indigo-600/40 ring-4 ring-slate-950' : 'bg-slate-800 text-slate-300 hover:bg-slate-750 shadow-black/50 ring-4 ring-slate-950 hover:scale-105'}`}
-        >
-          <span className="text-xl">?벜</span>
-        </button>
-
-      </nav>
+      <BottomNavigation activeTab={activeTab} setActiveTab={setActiveTab} />
 
     </div>
   );

commit 4ae394d75cce840717066692ce7ee759b22fdc11
Author: ReviewNotes Developer <developer@reviewnotes.local>
Date:   Fri Jul 3 05:58:40 2026 +0900

    Optimize UI by removing Settings and PIN codes, and restructuring AI clinical diagnosis card details

diff --git a/src/App.tsx b/src/App.tsx
index 0884191..e865170 100644
--- a/src/App.tsx
+++ b/src/App.tsx
@@ -1,7 +1,6 @@
 import { useState, useEffect } from 'react';
 import type { ActiveTab, MistakeEntry } from './types';
 import { CameraScanner } from './components/CameraScanner';
-import { encryptApiKey, decryptApiKey } from './utils/crypto';
 import { analyzeMistakeWithGemini } from './services/gemini';
 import { AuthScreen } from './components/AuthScreen';
 import { LaTeXRenderer } from './components/LaTeXRenderer';
@@ -21,6 +20,7 @@ const base64ToBlob = (base64DataUrl: string): Blob => {
 };
 
 function App() {
+  // If Supabase credentials are not configured, block and show the setup guide
   if (!isSupabaseConfigured) {
     return (
       <div className="h-full flex items-center justify-center p-6 bg-slate-950 text-slate-100 text-center">
@@ -54,21 +54,7 @@ function App() {
   const [currentUser, setCurrentUser] = useState<string>('');
   const [activeTab, setActiveTab] = useState<ActiveTab>('notes');
   const [mistakes, setMistakes] = useState<MistakeEntry[]>([]);
-  const [encryptedApiKey, setEncryptedApiKey] = useState<string>('');
-  
-  // App-level state for temporary decrypted API key (lasts for the session)
-  const [decryptedKey, setDecryptedKey] = useState<string>(() => {
-    return sessionStorage.getItem('gemini_api_key_temp') || '';
-  });
-
-  // Modal and Form States
   const [selectedEntry, setSelectedEntry] = useState<MistakeEntry | null>(null);
-  const [apiKeyInput, setApiKeyInput] = useState('');
-  const [pinInput, setPinInput] = useState('');
-  const [unlockPin, setUnlockPin] = useState('');
-  const [showUnlockModal, setShowUnlockModal] = useState(false);
-  const [showApiGuideModal, setShowApiGuideModal] = useState(false);
-  const [statusMessage, setStatusMessage] = useState<{ text: string; isError: boolean } | null>(null);
   const [isAnalyzing, setIsAnalyzing] = useState(false);
 
   // Monitor Supabase Authentication States
@@ -93,19 +79,15 @@ function App() {
       } else {
         setCurrentUser('');
         setMistakes([]);
-        setEncryptedApiKey('');
-        setDecryptedKey('');
-        sessionStorage.removeItem('gemini_api_key_temp');
       }
     });
 
     return () => subscription.unsubscribe();
   }, []);
 
-  // Fetch mistakes & API key profile from Supabase
+  // Fetch mistakes from Supabase
   const fetchUserData = async (userId: string) => {
     try {
-      // Fetch mistakes table
       const { data: dbMistakes, error: mistakesError } = await supabase
         .from('mistakes')
         .select('*')
@@ -121,17 +103,6 @@ function App() {
         analysis: m.analysis || undefined
       }));
       setMistakes(mappedMistakes);
-
-      // Fetch profile encrypted key
-      const { data: profileData, error: profileError } = await supabase
-        .from('profiles')
-        .select('encrypted_api_key')
-        .eq('id', userId)
-        .maybeSingle();
-
-      if (profileData && !profileError) {
-        setEncryptedApiKey(profileData.encrypted_api_key || '');
-      }
     } catch (err) {
       console.error('Error loading Supabase user data:', err);
     }
@@ -143,93 +114,19 @@ function App() {
     return `${d.getFullYear()}.${String(d.getMonth() + 1).padStart(2, '0')}.${String(d.getDate()).padStart(2, '0')}`;
   };
 
-  // Securely encrypt and save the API Key to Supabase database profiles table
-  const handleSaveApiKey = async (e: React.FormEvent) => {
-    e.preventDefault();
-    setStatusMessage(null);
-
-    if (!apiKeyInput.trim()) {
-      setStatusMessage({ text: 'API Key瑜??낅젰??二쇱꽭??', isError: true });
-      return;
-    }
-    if (pinInput.length < 4) {
-      setStatusMessage({ text: 'PIN 踰덊샇??理쒖냼 4?먮━ ?댁긽?댁뼱???⑸땲??', isError: true });
-      return;
-    }
-    if (!session?.user) return;
-
-    try {
-      const encrypted = await encryptApiKey(apiKeyInput.trim(), pinInput);
-      
-      const { error: profileError } = await supabase
-        .from('profiles')
-        .upsert({
-          id: session.user.id,
-          encrypted_api_key: encrypted,
-          updated_at: new Date().toISOString()
-        });
-
-      if (profileError) throw profileError;
-
-      setEncryptedApiKey(encrypted);
-      
-      // Save temporarily in session storage
-      setDecryptedKey(apiKeyInput.trim());
-      sessionStorage.setItem('gemini_api_key_temp', apiKeyInput.trim());
-
-      setApiKeyInput('');
-      setPinInput('');
-      setStatusMessage({ text: 'API Key媛 ?뷀샇?붾릺???대씪?곕뱶 ?꾨줈?꾩뿉 ?덉쟾?섍쾶 ?숆린?붾릺?덉뒿?덈떎.', isError: false });
-    } catch (err: any) {
-      console.error(err);
-      setStatusMessage({ text: `???以??ㅻ쪟 諛쒖깮: ${err.message}`, isError: true });
-    }
-  };
-
-  // Decrypt Key handler (used when analyzing)
-  const handleUnlockKey = async (e: React.FormEvent) => {
-    e.preventDefault();
-    setStatusMessage(null);
-
-    if (!encryptedApiKey) {
-      setStatusMessage({ text: '??λ맂 API Key媛 ?놁뒿?덈떎. ?ㅼ젙 ??뿉??癒쇱? ?깅줉??二쇱꽭??', isError: true });
-      return;
-    }
-
-    try {
-      const decrypted = await decryptApiKey(encryptedApiKey, unlockPin);
-      setDecryptedKey(decrypted);
-      sessionStorage.setItem('gemini_api_key_temp', decrypted);
-      setShowUnlockModal(false);
-      setUnlockPin('');
-      
-      if (selectedEntry) {
-        runAnalysisFlow(selectedEntry, decrypted);
-      }
-    } catch (err: any) {
-      setStatusMessage({ text: err.message || '蹂듯샇???ㅽ뙣', isError: true });
-    }
-  };
-
   // Start analysis trigger
   const handleStartAnalysis = (entry: MistakeEntry) => {
     const envKey = import.meta.env.VITE_GEMINI_API_KEY || '';
-    const activeKey = decryptedKey || envKey;
-
-    if (activeKey) {
-      runAnalysisFlow(entry, activeKey);
-    } else if (encryptedApiKey) {
-      setShowUnlockModal(true);
+    if (envKey) {
+      runAnalysisFlow(entry, envKey);
     } else {
-      setActiveTab('settings');
-      alert('AI ?ㅻ떟 遺꾩꽍??吏꾪뻾?섎젮硫?癒쇱? ?ㅼ젙 ??뿉??Gemini API Key瑜??깅줉?섍굅??.env ?뚯씪??VITE_GEMINI_API_KEY瑜??ㅼ젙?댁빞 ?⑸땲??');
+      alert('愿由ъ옄 ?쒕쾭??Gemini API Key(VITE_GEMINI_API_KEY)媛 ?깅줉?섏? ?딆븯?듬땲?? Vercel ?섍꼍 蹂???ㅼ젙???꾨즺??二쇱꽭??');
     }
   };
 
   // Gemini API analysis flow & database updating
   const runAnalysisFlow = async (entry: MistakeEntry, apiKey: string) => {
     setIsAnalyzing(true);
-    setStatusMessage(null);
     try {
       const result = await analyzeMistakeWithGemini(entry.imageUrl, apiKey);
       
@@ -254,7 +151,6 @@ function App() {
       setSelectedEntry(updatedEntry);
     } catch (err: any) {
       console.error(err);
-      setStatusMessage({ text: err.message || 'AI 遺꾩꽍 ?ㅽ뻾 以??ㅻ쪟媛 諛쒖깮?덉뒿?덈떎.', isError: true });
       alert(err.message || 'AI 遺꾩꽍 ?ㅽ뻾 以??ㅻ쪟媛 諛쒖깮?덉뒿?덈떎.');
     } finally {
       setIsAnalyzing(false);
@@ -345,9 +241,6 @@ function App() {
     setCurrentUser('');
     setSession(null);
     setMistakes([]);
-    setEncryptedApiKey('');
-    setDecryptedKey('');
-    sessionStorage.removeItem('gemini_api_key_temp');
     setActiveTab('notes');
   };
 
@@ -365,11 +258,11 @@ function App() {
             ??           </div>
           <h1 className="text-lg font-bold bg-gradient-to-r from-white to-slate-300 bg-clip-text text-transparent">
-            AI ?섑븰 ?ㅻ떟?명듃
+            ?붿퓼?ㅼ닔???ㅻ떟?대━??           </h1>
         </div>
         <div className="flex items-center space-x-2">
-          <span className="text-[10px] text-slate-400 bg-slate-800/80 px-2 py-0.5 rounded-full border border-slate-700 font-bold max-w-[80px] truncate">
+          <span className="text-[10px] text-slate-400 bg-slate-800/80 px-2 py-0.5 rounded-full border border-slate-700 font-bold max-w-[100px] truncate">
             ?뫀 {currentUser}
           </span>
           <button 
@@ -378,40 +271,6 @@ function App() {
           >
             濡쒓렇?꾩썐
           </button>
-          
-          {import.meta.env.VITE_GEMINI_API_KEY ? (
-            <button 
-              onClick={() => setShowApiGuideModal(true)}
-              className="flex items-center space-x-1 text-indigo-400 bg-indigo-500/10 px-2.5 py-0.5 rounded-full text-[10px] border border-indigo-500/30 active:scale-95 transition-all hover:bg-indigo-500/20"
-            >
-              <span className="w-1.5 h-1.5 rounded-full bg-indigo-400"></span>
-              <span>Env Key ?쒖꽦?붾맖 ??/span>
-            </button>
-          ) : decryptedKey ? (
-            <button 
-              onClick={() => setShowApiGuideModal(true)}
-              className="flex items-center space-x-1 text-emerald-400 bg-emerald-500/10 px-2.5 py-0.5 rounded-full text-[10px] border border-emerald-500/30 active:scale-95 transition-all hover:bg-emerald-500/20"
-            >
-              <span className="w-1.5 h-1.5 rounded-full bg-emerald-400"></span>
-              <span>蹂댁븞 ?좉툑?댁젣????/span>
-            </button>
-          ) : encryptedApiKey ? (
-            <button 
-              onClick={() => setShowApiGuideModal(true)}
-              className="flex items-center space-x-1 text-amber-400 bg-emerald-500/10 px-2.5 py-0.5 rounded-full text-[10px] border border-amber-500/30 active:scale-95 transition-all hover:bg-amber-500/20"
-            >
-              <span className="w-1.5 h-1.5 rounded-full bg-amber-400"></span>
-              <span>蹂댁븞 ?뷀샇?붾맖 ??/span>
-            </button>
-          ) : (
-            <button 
-              onClick={() => setShowApiGuideModal(true)}
-              className="text-[10px] text-slate-400 hover:text-slate-300 bg-slate-900 border border-slate-800 hover:border-slate-700 px-2.5 py-0.5 rounded-full active:scale-95 transition-all flex items-center space-x-1"
-            >
-              <span>API Key 誘몃벑濡?/span>
-              <span className="w-4 h-4 rounded-full bg-slate-800 text-slate-400 flex items-center justify-center text-[9px] font-bold">?</span>
-            </button>
-          )}
         </div>
       </header>
 
@@ -505,102 +364,6 @@ function App() {
           />
         )}
 
-        {activeTab === 'settings' && (
-          <div className="space-y-6 max-w-lg mx-auto">
-            <div>
-              <h2 className="text-xl font-bold text-white">???ㅼ젙</h2>
-              <p className="text-xs text-slate-400 mt-1">API 蹂댁븞 ?ㅼ젙 諛????섍꼍?ㅼ젙</p>
-            </div>
-
-            {/* API Security Card */}
-            <form onSubmit={handleSaveApiKey} className="bg-slate-900/40 border border-slate-800 rounded-2xl p-5 space-y-4">
-              <div className="flex items-center space-x-3">
-                <div className="w-10 h-10 rounded-xl bg-indigo-950 border border-indigo-800 flex items-center justify-center text-xl text-indigo-400">
-                  ?뵎
-                </div>
-                <div>
-                  <h3 className="font-bold text-white text-sm">Gemini API 蹂댁븞 ?ㅼ젙</h3>
-                  <p className="text-[10px] text-slate-400">?대씪?곕뱶 ?꾨줈???????媛뺣젰 ?뷀샇???곸슜</p>
-                </div>
-              </div>
-              
-              <div className="space-y-3 pt-2">
-                <div className="space-y-1">
-                  <label className="text-xs text-slate-400 font-medium">Gemini API Key</label>
-                  <input 
-                    type="password" 
-                    value={apiKeyInput}
-                    onChange={(e) => setApiKeyInput(e.target.value)}
-                    placeholder="AIzaSy..." 
-                    className="w-full px-4 py-3 rounded-xl bg-slate-950 border border-slate-800 focus:border-indigo-500 focus:ring-1 focus:ring-indigo-500 text-sm text-white placeholder-slate-600 outline-none transition-all"
-                  />
-                </div>
-                <div className="space-y-1">
-                  <label className="text-xs text-slate-400 font-medium">??蹂듯샇??PIN (4~6?먮━ ?レ옄)</label>
-                  <input 
-                    type="password" 
-                    maxLength={6}
-                    value={pinInput}
-                    onChange={(e) => setPinInput(e.target.value.replace(/[^0-9]/g, ''))}
-                    placeholder="?レ옄 4~6?먮━ ?낅젰" 
-                    className="w-full px-4 py-3 rounded-xl bg-slate-950 border border-slate-800 focus:border-indigo-500 focus:ring-1 focus:ring-indigo-500 text-sm text-white placeholder-slate-600 outline-none transition-all"
-                  />
-                  <p className="text-[9px] text-slate-500 leading-normal">
-                    * ?ㅼ젙??PIN? ??蹂듯샇?????몄쬆 ?섎떒?쇰줈 ?ъ슜?⑸땲?? ?대씪?곕뱶???곕룞 ??λ릺誘濡?湲곌린媛 諛붾뚯뼱????PIN留??덉쑝硫??몄젣???ㅻ? 遺덈윭? 遺꾩꽍?????덉뒿?덈떎.
-                  </p>
-                </div>
-
-                {statusMessage && (
-                  <div className={`p-3 rounded-lg text-xs ${statusMessage.isError ? 'bg-red-500/10 text-red-400 border border-red-500/20' : 'bg-emerald-500/10 text-emerald-400 border border-emerald-500/20'}`}>
-                    {statusMessage.text}
-                  </div>
-                )}
-
-                <button 
-                  type="submit"
-                  className="w-full py-3 mt-2 rounded-xl bg-indigo-600 hover:bg-indigo-500 active:scale-95 transition-all text-xs font-bold text-white shadow-md shadow-indigo-600/10"
-                >
-                  蹂댁븞 ??ν븯湲?-                </button>
-              </div>
-            </form>
-
-            {/* App Info Card */}
-            <div className="bg-slate-900/40 border border-slate-800 rounded-2xl p-5 space-y-3">
-              <h3 className="font-bold text-white text-sm">?좏뵆由ъ??댁뀡 ?뺣낫</h3>
-              <div className="divide-y divide-slate-800/60 text-xs">
-                <div className="flex justify-between py-2.5">
-                  <span className="text-slate-400">踰꾩쟾</span>
-                  <span className="text-slate-200 font-medium">v1.1.0 (PWA + Supabase)</span>
-                </div>
-                <div className="flex justify-between py-2.5">
-                  <span className="text-slate-400">?몄쬆 諛??곗씠?곕쿋?댁뒪</span>
-                  <span className="text-emerald-400 font-medium">Supabase Cloud</span>
-                </div>
-                <div className="flex justify-between py-2.5">
-                  <span className="text-slate-400">蹂댁븞 ?섏?</span>
-                  <span className="text-emerald-400 font-medium">AES-GCM ?뷀샇??/span>
-                </div>
-                {decryptedKey && (
-                  <div className="flex justify-between py-2.5 items-center">
-                    <span className="text-slate-400">?몄뀡 ?곹깭</span>
-                    <button 
-                      onClick={() => {
-                        setDecryptedKey('');
-                        sessionStorage.removeItem('gemini_api_key_temp');
-                        alert('API Key ?몄뀡??留뚮즺?섏뿀?듬땲?? ?ㅼ떆 ?덉쟾?섍쾶 ?좉꼈?듬땲??');
-                      }}
-                      className="px-2 py-1 bg-red-950 border border-red-800 text-red-400 rounded-lg text-[10px]"
-                    >
-                      ?몄뀡 硫붾え由??좉툑
-                    </button>
-                  </div>
-                )}
-              </div>
-            </div>
-          </div>
-        )}
-
       </main>
 
       {/* Selected Entry Detail Modal */}
@@ -641,45 +404,48 @@ function App() {
                   <div className="w-12 h-12 border-4 border-indigo-500 border-t-transparent rounded-full animate-spin"></div>
                   <div className="text-center">
                     <p className="text-sm font-semibold text-white">泥섎━ 以?..</p>
-                    <p className="text-xs text-slate-400 mt-1">?대씪?곕뱶 誘몃뵒??泥섎━ ?먮뒗 AI ?대━??吏꾨떒???묐룞?섍퀬 ?덉뒿?덈떎.</p>
+                    <p className="text-xs text-slate-400 mt-1">AI ?섑븰 ?대━??吏꾨떒???묒꽦?섍퀬 ?덉뒿?덈떎.</p>
                   </div>
                 </div>
               ) : selectedEntry.analysis ? (
-                <div className="space-y-5 animate-scale-up">
-                  {/* Item 1 */}
-                  <div className="space-y-1.5">
+                <div className="space-y-6 animate-scale-up">
+                  {/* Card 1: ?뺤꽍 ???怨쇱젙 */}
+                  <div className="space-y-2 border-l-4 border-indigo-500 pl-4 py-1">
                     <h4 className="text-sm font-extrabold text-indigo-400 flex items-center">
-                      <span className="mr-1.5">??/span> [臾몄젣 ???怨쇱젙]
+                      <span className="mr-1.5 text-base">?뮕</span> ?뺤꽍 ???怨쇱젙
                     </h4>
-                    <div className="bg-slate-950 p-4 rounded-2xl border border-slate-800/60">
-                      <LaTeXRenderer text={selectedEntry.analysis.solvingProcess} className="text-sm md:text-base" />
+                    <div className="bg-slate-950 p-4.5 rounded-2xl border border-slate-850">
+                      <LaTeXRenderer text={selectedEntry.analysis.solvingProcess} className="text-sm md:text-base leading-relaxed" />
                     </div>
                   </div>
-                  {/* Item 2 */}
-                  <div className="space-y-1.5">
+
+                  {/* Card 2: ?ㅼ닔 & ?由??댁쑀 遺꾩꽍 (?붿빟 諛?媛뺤“) */}
+                  <div className="space-y-2 border-l-4 border-amber-500 pl-4 py-1">
                     <h4 className="text-sm font-extrabold text-amber-400 flex items-center">
-                      <span className="mr-1.5">?좑툘</span> [?ㅼ닔??吏???곸꽭 遺꾩꽍]
+                      <span className="mr-1.5 text-base">?뵇</span> ?由??댁쑀 遺꾩꽍
                     </h4>
-                    <div className="bg-slate-950 p-4 rounded-2xl border border-slate-800/60">
-                      <LaTeXRenderer text={selectedEntry.analysis.mistakeDetail} className="text-sm md:text-base" />
-                    </div>
-                  </div>
-                  {/* Item 3 */}
-                  <div className="space-y-1.5">
-                    <h4 className="text-sm font-extrabold text-red-400 flex items-center">
-                      <span className="mr-1.5">?렞</span> [洹쇰낯?곸씤 ?由??댁쑀]
-                    </h4>
-                    <div className="bg-slate-950 p-4 rounded-2xl border border-slate-800/60">
-                      <LaTeXRenderer text={selectedEntry.analysis.rootCause} className="text-sm md:text-base" />
+                    <div className="bg-slate-950 p-4.5 rounded-2xl border border-slate-850 space-y-4">
+                      {/* ?ㅼ닔 遺꾩꽍 ?곸꽭 */}
+                      <div className="space-y-1.5">
+                        <span className="text-[11px] font-bold text-slate-500 uppercase tracking-wider block">?ㅼ닔 吏??/span>
+                        <LaTeXRenderer text={selectedEntry.analysis.mistakeDetail} className="text-sm md:text-base leading-relaxed" />
+                      </div>
+                      
+                      {/* ?ㅺ컻??洹쇰낯 ?먯씤 */}
+                      <div className="pt-3.5 border-t border-slate-850 space-y-1.5">
+                        <span className="text-[11px] font-bold text-slate-500 uppercase tracking-wider block">?由??먯씤</span>
+                        <LaTeXRenderer text={selectedEntry.analysis.rootCause} className="text-sm md:text-base leading-relaxed text-red-300" />
+                      </div>
                     </div>
                   </div>
-                  {/* Item 4 */}
-                  <div className="space-y-1.5">
+
+                  {/* Card 3: ?щ컻 諛⑹? ?梨?(?붿빟 諛?湲癒몃━ 媛뺤“) */}
+                  <div className="space-y-2 border-l-4 border-emerald-500 pl-4 py-1">
                     <h4 className="text-sm font-extrabold text-emerald-400 flex items-center">
-                      <span className="mr-1.5">?썳截?/span> [?ν썑 ?щ컻 諛⑹? ?梨?
+                      <span className="mr-1.5 text-base">?썳截?/span> ?щ컻 諛⑹? ?梨?                     </h4>
-                    <div className="bg-slate-950 p-4 rounded-2xl border border-slate-800/60">
-                      <LaTeXRenderer text={selectedEntry.analysis.actionPlan} className="text-sm md:text-base" />
+                    <div className="bg-slate-950 p-4.5 rounded-2xl border border-slate-850">
+                      <LaTeXRenderer text={selectedEntry.analysis.actionPlan} className="text-sm md:text-base leading-relaxed" />
                     </div>
                   </div>
                 </div>
@@ -689,7 +455,7 @@ function App() {
                   <div className="space-y-1">
                     <p className="text-sm font-bold text-white">AI ?섑븰 ?대━??吏꾨떒</p>
                     <p className="text-xs text-slate-400 leading-relaxed max-w-xs mx-auto">
-                      ?꾩쭅 ?ㅻ떟 ?먯씤??遺꾩꽍?섏? ?딆븯?듬땲?? ?깅줉??API Key瑜??듯빐 4媛吏 ?듭떖 ?먯씤 吏꾨떒??吏꾪뻾?⑸땲??
+                      ?꾩쭅 ?ㅻ떟 ?먯씤??遺꾩꽍?섏? ?딆븯?듬땲?? AI媛 ?ㅺ퀎?섎뒗 留욎땄???ㅻ떟 泥섎갑?꾩쓣 ?뺤씤??蹂댁꽭??
                     </p>
                   </div>
                   <button 
@@ -716,129 +482,16 @@ function App() {
         </div>
       )}
 
-      {/* Unlock API Key PIN Modal */}
-      {showUnlockModal && (
-        <div className="fixed inset-0 z-50 flex items-center justify-center bg-black/80 backdrop-blur-sm p-4">
-          <form 
-            onSubmit={handleUnlockKey}
-            className="w-full max-w-sm bg-slate-900 border border-slate-800 rounded-3xl p-6 space-y-4 shadow-2xl animate-scale-up"
-          >
-            <div className="text-center space-y-2">
-              <div className="w-12 h-12 rounded-full bg-amber-500/10 border border-amber-500/30 text-2xl flex items-center justify-center mx-auto text-amber-400">
-                ?뵏
-              </div>
-              <h3 className="font-bold text-white text-base">蹂댁븞 蹂듯샇??/h3>
-              <p className="text-xs text-slate-400 max-w-xs mx-auto leading-relaxed">
-                ?뷀샇?붾맂 API Key瑜?濡쒕뱶?섍린 ?꾪빐 蹂댁븞 PIN 肄붾뱶瑜??낅젰??二쇱꽭??
-              </p>
-            </div>
-
-            <div className="space-y-3 pt-2">
-              <input 
-                type="password" 
-                maxLength={6}
-                value={unlockPin}
-                onChange={(e) => setUnlockPin(e.target.value.replace(/[^0-9]/g, ''))}
-                placeholder="?レ옄 4~6?먮━ PIN ?낅젰" 
-                className="w-full px-4 py-3 text-center tracking-widest text-lg rounded-xl bg-slate-950 border border-slate-800 focus:border-indigo-500 focus:ring-1 focus:ring-indigo-500 text-white placeholder-slate-600 outline-none transition-all"
-              />
-
-              {statusMessage && (
-                <div className="p-3 rounded-lg text-xs bg-red-500/10 text-red-400 border border-red-500/20">
-                  {statusMessage.text}
-                </div>
-              )}
-
-              <div className="flex space-x-3 pt-2">
-                <button 
-                  type="button"
-                  onClick={() => {
-                    setShowUnlockModal(false);
-                    setUnlockPin('');
-                    setStatusMessage(null);
-                  }}
-                  className="flex-1 py-3 rounded-xl border border-slate-800 hover:bg-slate-800 active:scale-95 text-xs font-semibold text-slate-300"
-                >
-                  痍⑥냼
-                </button>
-                <button 
-                  type="submit"
-                  className="flex-1 py-3 rounded-xl bg-indigo-600 hover:bg-indigo-500 active:scale-95 text-xs font-bold text-white shadow-md shadow-indigo-600/20"
-                >
-                  ?좉툑 ?댁젣
-                </button>
-              </div>
-            </div>
-          </form>
-        </div>
-      )}
-      {/* Gemini API Key Guide Modal */}
-      {showApiGuideModal && (
-        <div className="fixed inset-0 z-50 flex items-center justify-center bg-black/80 backdrop-blur-sm p-4">
-          <div className="w-full max-w-md bg-slate-900 border border-slate-800 rounded-3xl p-6 space-y-4 shadow-2xl animate-scale-up">
-            <div className="text-center space-y-2">
-              <div className="w-12 h-12 rounded-full bg-indigo-500/10 border border-indigo-500/30 text-xl flex items-center justify-center mx-auto text-indigo-400">
-                ?쨼
-              </div>
-              <h3 className="font-bold text-white text-base">Gemini API ??諛쒓툒 媛?대뱶</h3>
-              <p className="text-xs text-slate-400 max-w-xs mx-auto leading-relaxed">
-                AI ?ㅻ떟?명듃???먮룞 痍⑥빟??吏꾨떒 湲곕뒫???댁슜?섎젮硫?臾대즺 Google Gemini API ?ㅺ? ?꾩슂?⑸땲??
-              </p>
-            </div>
-
-            <div className="space-y-3 pt-2 text-left text-xs text-slate-300">
-              <div className="bg-slate-950 p-4 rounded-2xl border border-slate-800/60 space-y-3.5 leading-relaxed">
-                <div className="flex items-start space-x-2.5">
-                  <span className="w-5 h-5 rounded-full bg-indigo-900 text-indigo-300 text-[10px] font-bold flex items-center justify-center flex-none mt-0.5">1</span>
-                  <p>
-                    ?꾨옒 <strong>'API ??諛쒓툒?섎윭 媛湲?</strong> 踰꾪듉???대┃?섏뿬 Google AI Studio ?ъ씠?몃줈 ?대룞?⑸땲??(援ш? 怨꾩젙 ?꾩슂).
-                  </p>
-                </div>
-                <div className="flex items-start space-x-2.5">
-                  <span className="w-5 h-5 rounded-full bg-indigo-900 text-indigo-300 text-[10px] font-bold flex items-center justify-center flex-none mt-0.5">2</span>
-                  <p>
-                    濡쒓렇?????곷떒??<strong>"Get API key"</strong> ?먮뒗 <strong>"Create API key"</strong> 踰꾪듉???대┃?섏뿬 ???ㅻ? 諛쒓툒諛쏆뒿?덈떎.
-                  </p>
-                </div>
-                <div className="flex items-start space-x-2.5">
-                  <span className="w-5 h-5 rounded-full bg-indigo-900 text-indigo-300 text-[10px] font-bold flex items-center justify-center flex-none mt-0.5">3</span>
-                  <p>
-                    諛쒓툒????<code className="bg-slate-900 px-1.5 py-0.5 rounded text-indigo-400 text-[10px] font-mono">AIzaSy...</code>)瑜?蹂듭궗???? ?ㅻ떟?명듃 <strong>[?ㅼ젙]</strong> ??뿉??PIN 踰덊샇? ?④퍡 ?깅줉?⑸땲??
-                  </p>
-                </div>
-              </div>
-
-              <div className="flex space-x-3 pt-2">
-                <button
-                  type="button"
-                  onClick={() => setShowApiGuideModal(false)}
-                  className="flex-1 py-3 rounded-xl border border-slate-800 hover:bg-slate-800 active:scale-95 text-xs font-semibold text-slate-300"
-                >
-                  ?リ린
-                </button>
-                <a 
-                  href="https://aistudio.google.com/" 
-                  target="_blank" 
-                  rel="noopener noreferrer"
-                  className="flex-1 py-3 rounded-xl bg-gradient-to-r from-indigo-600 to-indigo-500 hover:from-indigo-500 hover:to-indigo-400 active:scale-95 text-xs font-bold text-white text-center shadow-md shadow-indigo-600/20 flex items-center justify-center"
-                >
-                  API ??諛쒓툒?섍린 &rarr;
-                </a>
-              </div>
-            </div>
-          </div>
-        </div>
-      )}
       {/* Floating Glassmorphic Bottom Navigation Bar */}
       <nav className="fixed bottom-4 left-4 right-4 z-40 h-16 rounded-2xl border border-slate-800/80 bg-slate-900/75 backdrop-blur-lg flex items-center justify-around px-4 shadow-xl shadow-black/40">
         
         {/* Tab 1: Notes List */}
         <button 
           onClick={() => setActiveTab('notes')}
-          className={`flex flex-col items-center justify-center w-12 h-12 rounded-xl transition-all ${activeTab === 'notes' ? 'text-indigo-400 scale-105' : 'text-slate-500 hover:text-slate-300'}`}
+          className={`flex flex-col items-center justify-center w-16 h-12 rounded-xl transition-all ${activeTab === 'notes' ? 'text-indigo-400 scale-105 font-bold' : 'text-slate-500 hover:text-slate-300'}`}
         >
           <span className="text-lg">?뱭</span>
-          <span className="text-[10px] font-bold mt-0.5">?ㅻ떟?명듃</span>
+          <span className="text-[10px] mt-0.5">?ㅻ떟?명듃</span>
         </button>
 
         {/* Tab 2: Floating Camera Trigger */}
@@ -849,15 +502,6 @@ function App() {
           <span className="text-xl">?벜</span>
         </button>
 
-        {/* Tab 3: Settings */}
-        <button 
-          onClick={() => setActiveTab('settings')}
-          className={`flex flex-col items-center justify-center w-12 h-12 rounded-xl transition-all ${activeTab === 'settings' ? 'text-indigo-400 scale-105' : 'text-slate-500 hover:text-slate-300'}`}
-        >
-          <span className="text-lg">?숋툘</span>
-          <span className="text-[10px] font-bold mt-0.5">?ㅼ젙</span>
-        </button>
-
       </nav>
 
     </div>

commit f655706779c21cadf225f12f13caaa106f337e46
Author: ReviewNotes Developer <developer@reviewnotes.local>
Date:   Fri Jul 3 05:26:37 2026 +0900

    Correct brand name to ?붿퓼?ㅼ닔??and add Gemini API key issuance guide modal in App.tsx

diff --git a/src/App.tsx b/src/App.tsx
index ed7e8b1..0884191 100644
--- a/src/App.tsx
+++ b/src/App.tsx
@@ -67,6 +67,7 @@ function App() {
   const [pinInput, setPinInput] = useState('');
   const [unlockPin, setUnlockPin] = useState('');
   const [showUnlockModal, setShowUnlockModal] = useState(false);
+  const [showApiGuideModal, setShowApiGuideModal] = useState(false);
   const [statusMessage, setStatusMessage] = useState<{ text: string; isError: boolean } | null>(null);
   const [isAnalyzing, setIsAnalyzing] = useState(false);
 
@@ -379,24 +380,37 @@ function App() {
           </button>
           
           {import.meta.env.VITE_GEMINI_API_KEY ? (
-            <span className="flex items-center space-x-1 text-indigo-400 bg-indigo-500/10 px-2 py-0.5 rounded-full text-[10px] border border-indigo-500/30">
+            <button 
+              onClick={() => setShowApiGuideModal(true)}
+              className="flex items-center space-x-1 text-indigo-400 bg-indigo-500/10 px-2.5 py-0.5 rounded-full text-[10px] border border-indigo-500/30 active:scale-95 transition-all hover:bg-indigo-500/20"
+            >
               <span className="w-1.5 h-1.5 rounded-full bg-indigo-400"></span>
-              <span>Env Key ?쒖꽦?붾맖</span>
-            </span>
+              <span>Env Key ?쒖꽦?붾맖 ??/span>
+            </button>
           ) : decryptedKey ? (
-            <span className="flex items-center space-x-1 text-emerald-400 bg-emerald-500/10 px-2 py-0.5 rounded-full text-[10px] border border-emerald-500/30">
+            <button 
+              onClick={() => setShowApiGuideModal(true)}
+              className="flex items-center space-x-1 text-emerald-400 bg-emerald-500/10 px-2.5 py-0.5 rounded-full text-[10px] border border-emerald-500/30 active:scale-95 transition-all hover:bg-emerald-500/20"
+            >
               <span className="w-1.5 h-1.5 rounded-full bg-emerald-400"></span>
-              <span>蹂댁븞 ?좉툑?댁젣??/span>
-            </span>
+              <span>蹂댁븞 ?좉툑?댁젣????/span>
+            </button>
           ) : encryptedApiKey ? (
-            <span className="flex items-center space-x-1 text-amber-400 bg-amber-500/10 px-2 py-0.5 rounded-full text-[10px] border border-amber-500/30">
+            <button 
+              onClick={() => setShowApiGuideModal(true)}
+              className="flex items-center space-x-1 text-amber-400 bg-emerald-500/10 px-2.5 py-0.5 rounded-full text-[10px] border border-amber-500/30 active:scale-95 transition-all hover:bg-amber-500/20"
+            >
               <span className="w-1.5 h-1.5 rounded-full bg-amber-400"></span>
-              <span>蹂댁븞 ?뷀샇?붾맖</span>
-            </span>
+              <span>蹂댁븞 ?뷀샇?붾맖 ??/span>
+            </button>
           ) : (
-            <span className="text-[10px] text-slate-500 border border-slate-800 px-2 py-0.5 rounded-full">
-              API Key 誘몃벑濡?-            </span>
+            <button 
+              onClick={() => setShowApiGuideModal(true)}
+              className="text-[10px] text-slate-400 hover:text-slate-300 bg-slate-900 border border-slate-800 hover:border-slate-700 px-2.5 py-0.5 rounded-full active:scale-95 transition-all flex items-center space-x-1"
+            >
+              <span>API Key 誘몃벑濡?/span>
+              <span className="w-4 h-4 rounded-full bg-slate-800 text-slate-400 flex items-center justify-center text-[9px] font-bold">?</span>
+            </button>
           )}
         </div>
       </header>
@@ -758,7 +772,63 @@ function App() {
           </form>
         </div>
       )}
+      {/* Gemini API Key Guide Modal */}
+      {showApiGuideModal && (
+        <div className="fixed inset-0 z-50 flex items-center justify-center bg-black/80 backdrop-blur-sm p-4">
+          <div className="w-full max-w-md bg-slate-900 border border-slate-800 rounded-3xl p-6 space-y-4 shadow-2xl animate-scale-up">
+            <div className="text-center space-y-2">
+              <div className="w-12 h-12 rounded-full bg-indigo-500/10 border border-indigo-500/30 text-xl flex items-center justify-center mx-auto text-indigo-400">
+                ?쨼
+              </div>
+              <h3 className="font-bold text-white text-base">Gemini API ??諛쒓툒 媛?대뱶</h3>
+              <p className="text-xs text-slate-400 max-w-xs mx-auto leading-relaxed">
+                AI ?ㅻ떟?명듃???먮룞 痍⑥빟??吏꾨떒 湲곕뒫???댁슜?섎젮硫?臾대즺 Google Gemini API ?ㅺ? ?꾩슂?⑸땲??
+              </p>
+            </div>
+
+            <div className="space-y-3 pt-2 text-left text-xs text-slate-300">
+              <div className="bg-slate-950 p-4 rounded-2xl border border-slate-800/60 space-y-3.5 leading-relaxed">
+                <div className="flex items-start space-x-2.5">
+                  <span className="w-5 h-5 rounded-full bg-indigo-900 text-indigo-300 text-[10px] font-bold flex items-center justify-center flex-none mt-0.5">1</span>
+                  <p>
+                    ?꾨옒 <strong>'API ??諛쒓툒?섎윭 媛湲?</strong> 踰꾪듉???대┃?섏뿬 Google AI Studio ?ъ씠?몃줈 ?대룞?⑸땲??(援ш? 怨꾩젙 ?꾩슂).
+                  </p>
+                </div>
+                <div className="flex items-start space-x-2.5">
+                  <span className="w-5 h-5 rounded-full bg-indigo-900 text-indigo-300 text-[10px] font-bold flex items-center justify-center flex-none mt-0.5">2</span>
+                  <p>
+                    濡쒓렇?????곷떒??<strong>"Get API key"</strong> ?먮뒗 <strong>"Create API key"</strong> 踰꾪듉???대┃?섏뿬 ???ㅻ? 諛쒓툒諛쏆뒿?덈떎.
+                  </p>
+                </div>
+                <div className="flex items-start space-x-2.5">
+                  <span className="w-5 h-5 rounded-full bg-indigo-900 text-indigo-300 text-[10px] font-bold flex items-center justify-center flex-none mt-0.5">3</span>
+                  <p>
+                    諛쒓툒????<code className="bg-slate-900 px-1.5 py-0.5 rounded text-indigo-400 text-[10px] font-mono">AIzaSy...</code>)瑜?蹂듭궗???? ?ㅻ떟?명듃 <strong>[?ㅼ젙]</strong> ??뿉??PIN 踰덊샇? ?④퍡 ?깅줉?⑸땲??
+                  </p>
+                </div>
+              </div>
 
+              <div className="flex space-x-3 pt-2">
+                <button
+                  type="button"
+                  onClick={() => setShowApiGuideModal(false)}
+                  className="flex-1 py-3 rounded-xl border border-slate-800 hover:bg-slate-800 active:scale-95 text-xs font-semibold text-slate-300"
+                >
+                  ?リ린
+                </button>
+                <a 
+                  href="https://aistudio.google.com/" 
+                  target="_blank" 
+                  rel="noopener noreferrer"
+                  className="flex-1 py-3 rounded-xl bg-gradient-to-r from-indigo-600 to-indigo-500 hover:from-indigo-500 hover:to-indigo-400 active:scale-95 text-xs font-bold text-white text-center shadow-md shadow-indigo-600/20 flex items-center justify-center"
+                >
+                  API ??諛쒓툒?섍린 &rarr;
+                </a>
+              </div>
+            </div>
+          </div>
+        </div>
+      )}
       {/* Floating Glassmorphic Bottom Navigation Bar */}
       <nav className="fixed bottom-4 left-4 right-4 z-40 h-16 rounded-2xl border border-slate-800/80 bg-slate-900/75 backdrop-blur-lg flex items-center justify-around px-4 shadow-xl shadow-black/40">
         

commit c65a605ea892b17d5cd363631f35fbadb4162be5
Author: ReviewNotes Developer <developer@reviewnotes.local>
Date:   Fri Jul 3 05:11:26 2026 +0900

    Add safety configuration warning screen in App.tsx when env variables are missing

diff --git a/src/App.tsx b/src/App.tsx
index 841b258..ed7e8b1 100644
--- a/src/App.tsx
+++ b/src/App.tsx
@@ -5,7 +5,7 @@ import { encryptApiKey, decryptApiKey } from './utils/crypto';
 import { analyzeMistakeWithGemini } from './services/gemini';
 import { AuthScreen } from './components/AuthScreen';
 import { LaTeXRenderer } from './components/LaTeXRenderer';
-import { supabase } from './services/supabase';
+import { supabase, isSupabaseConfigured } from './services/supabase';
 
 // Helper to convert Base64 Data URL to Blob for Storage upload
 const base64ToBlob = (base64DataUrl: string): Blob => {
@@ -21,6 +21,35 @@ const base64ToBlob = (base64DataUrl: string): Blob => {
 };
 
 function App() {
+  if (!isSupabaseConfigured) {
+    return (
+      <div className="h-full flex items-center justify-center p-6 bg-slate-950 text-slate-100 text-center">
+        <div className="max-w-md w-full p-8 bg-slate-900/60 border border-slate-800 rounded-3xl space-y-6 shadow-2xl backdrop-blur-md animate-scale-up">
+          <div className="w-16 h-16 mx-auto rounded-2xl bg-red-500/10 border border-red-500/30 flex items-center justify-center text-3xl">
+            ?좑툘
+          </div>
+          <div className="space-y-2">
+            <h2 className="text-xl font-black text-white">Supabase ?ㅼ젙 ?꾨씫</h2>
+            <p className="text-xs text-slate-400 leading-relaxed">
+              ?좏뵆由ъ??댁뀡??援щ룞?섍린 ?꾪븳 Supabase ?곕룞 ?뺣낫媛 濡쒖뺄 ?섍꼍 ?뚯씪???ㅼ젙?섏? ?딆븯?듬땲??
+            </p>
+          </div>
+          <div className="space-y-3 pt-2 text-left">
+            <p className="text-xs font-bold text-slate-300">?닿껐 諛⑸쾿:</p>
+            <ol className="text-[11px] text-slate-400 space-y-2 list-decimal list-inside leading-relaxed bg-slate-950 p-4 rounded-xl border border-slate-800">
+              <li>?꾨줈?앺듃 猷⑦듃??<code className="bg-slate-900 px-1 py-0.5 rounded text-white font-mono">.env</code> ?뚯씪???앹꽦?⑸땲??</li>
+              <li>?꾨옒? 媛숈씠 諛쒓툒諛쏆? Supabase URL怨?Key瑜??묒꽦?⑸땲??</li>
+            </ol>
+            <div className="bg-slate-950 p-4 rounded-xl text-left font-mono text-[10px] text-indigo-400 border border-slate-850 select-text leading-normal">
+              VITE_SUPABASE_URL=https://wcvkmdvrowljypueucwx.supabase.co<br />
+              VITE_SUPABASE_ANON_KEY=sb_publishable_BtAepfY-CiPuAsOSxJ_Y8w_ZTNZ7UWI
+            </div>
+          </div>
+        </div>
+      </div>
+    );
+  }
+
   const [session, setSession] = useState<any>(null);
   const [currentUser, setCurrentUser] = useState<string>('');
   const [activeTab, setActiveTab] = useState<ActiveTab>('notes');

commit 0a99092d5b4de024a8f6c7ac50faed0b2987b722
Author: ReviewNotes Developer <developer@reviewnotes.local>
Date:   Fri Jul 3 05:01:47 2026 +0900

    Migrate authentication, database storage, and file upload system to Supabase

diff --git a/src/App.tsx b/src/App.tsx
index ec07965..841b258 100644
--- a/src/App.tsx
+++ b/src/App.tsx
@@ -1,39 +1,31 @@
-import { useState } from 'react';
+import { useState, useEffect } from 'react';
 import type { ActiveTab, MistakeEntry } from './types';
-import { useLocalStorage } from './hooks/useLocalStorage';
 import { CameraScanner } from './components/CameraScanner';
 import { encryptApiKey, decryptApiKey } from './utils/crypto';
 import { analyzeMistakeWithGemini } from './services/gemini';
 import { AuthScreen } from './components/AuthScreen';
 import { LaTeXRenderer } from './components/LaTeXRenderer';
-
-// Initial fallback mock data for visual demonstration
-const INITIAL_MOCKS: MistakeEntry[] = [
-  {
-    id: '1',
-    title: '?댁감諛⑹젙??洹쇱쓽 怨듭떇 遺???ㅼ닔',
-    imageUrl: 'https://images.unsplash.com/photo-1635070041078-e363dbe005cb?w=400&auto=format&fit=crop&q=60',
-    date: '2026-07-02T10:30:00Z',
-    analysis: {
-      solvingProcess: 'x짼 - 4x - 5 = 0??洹쇱쓣 援ы븷 ??洹쇱쓽 怨듭떇???곸슜?섏뿬 x = [4 짹 ??16 - 4(1)(-5))] / 2 = [4 짹 ??6] / 2 濡?援ы븯??怨쇱젙.',
-      mistakeDetail: '遺꾩옄 遺遺꾩뿉??2a???대떦?섎뒗 遺꾨え瑜??곸슜?덉쑝?? 遺꾩옄??-b瑜???낇븷 ??b=-4 ?대?濡?-b = 4媛 ?섏뼱???섎뒗???ㅼ닔濡?-4濡???낇븯??理쒖쥌 洹쇱쓽 遺?멸? 諛섎?濡??섏샂.',
-      rootCause: '洹쇱쓽 怨듭떇 ?붽린媛 遺덉븞?뺥븯嫄곕굹, ?뚯닔 湲고샇(-)媛 ?곗냽?댁꽌 ?섏삱 ?? -(-4) ) 愿꾪샇瑜??뚯썙 怨꾩궛?섏? ?딅뒗 ?섏걶 ?듦????덉쓬.',
-      actionPlan: '?뚯닔 怨꾩닔瑜?怨듭떇????낇븷 ?뚮뒗 ??긽 愿꾪샇瑜?移섍퀬 1珥덇컙 遺?몃? ?ъ젏寃?섎뒗 ?듦????ㅼ씠怨? ?좎궗???댁감諛⑹젙?????臾몄젣 5媛쒕? 異붽?濡??硫??ㅻ떟 寃???섑뻾.'
-    }
+import { supabase } from './services/supabase';
+
+// Helper to convert Base64 Data URL to Blob for Storage upload
+const base64ToBlob = (base64DataUrl: string): Blob => {
+  const parts = base64DataUrl.split(';base64,');
+  const contentType = parts[0].split(':')[1];
+  const raw = window.atob(parts[1]);
+  const rawLength = raw.length;
+  const uInt8Array = new Uint8Array(rawLength);
+  for (let i = 0; i < rawLength; ++i) {
+    uInt8Array[i] = raw.charCodeAt(i);
   }
-];
+  return new Blob([uInt8Array], { type: contentType });
+};
 
 function App() {
-  const [currentUser, setCurrentUser] = useLocalStorage<string>('ai_mistakes_current_user', '');
+  const [session, setSession] = useState<any>(null);
+  const [currentUser, setCurrentUser] = useState<string>('');
   const [activeTab, setActiveTab] = useState<ActiveTab>('notes');
-  const [mistakes, setMistakes] = useLocalStorage<MistakeEntry[]>(
-    currentUser ? `ai_mistakes_notes_${currentUser}` : 'ai_mistakes_notes',
-    INITIAL_MOCKS
-  );
-  const [encryptedApiKey, setEncryptedApiKey] = useLocalStorage<string>(
-    currentUser ? `gemini_api_key_enc_${currentUser}` : 'gemini_api_key_enc',
-    ''
-  );
+  const [mistakes, setMistakes] = useState<MistakeEntry[]>([]);
+  const [encryptedApiKey, setEncryptedApiKey] = useState<string>('');
   
   // App-level state for temporary decrypted API key (lasts for the session)
   const [decryptedKey, setDecryptedKey] = useState<string>(() => {
@@ -49,13 +41,79 @@ function App() {
   const [statusMessage, setStatusMessage] = useState<{ text: string; isError: boolean } | null>(null);
   const [isAnalyzing, setIsAnalyzing] = useState(false);
 
+  // Monitor Supabase Authentication States
+  useEffect(() => {
+    // 1. Get initial session
+    supabase.auth.getSession().then(({ data: { session } }) => {
+      setSession(session);
+      if (session?.user) {
+        const username = session.user.email?.split('@')[0] || 'User';
+        setCurrentUser(username);
+        fetchUserData(session.user.id);
+      }
+    });
+
+    // 2. Listen for auth changes
+    const { data: { subscription } } = supabase.auth.onAuthStateChange((_event, session) => {
+      setSession(session);
+      if (session?.user) {
+        const username = session.user.email?.split('@')[0] || 'User';
+        setCurrentUser(username);
+        fetchUserData(session.user.id);
+      } else {
+        setCurrentUser('');
+        setMistakes([]);
+        setEncryptedApiKey('');
+        setDecryptedKey('');
+        sessionStorage.removeItem('gemini_api_key_temp');
+      }
+    });
+
+    return () => subscription.unsubscribe();
+  }, []);
+
+  // Fetch mistakes & API key profile from Supabase
+  const fetchUserData = async (userId: string) => {
+    try {
+      // Fetch mistakes table
+      const { data: dbMistakes, error: mistakesError } = await supabase
+        .from('mistakes')
+        .select('*')
+        .order('date', { ascending: false });
+
+      if (mistakesError) throw mistakesError;
+
+      const mappedMistakes: MistakeEntry[] = (dbMistakes || []).map((m: any) => ({
+        id: m.id,
+        title: m.title,
+        imageUrl: m.image_url,
+        date: m.date,
+        analysis: m.analysis || undefined
+      }));
+      setMistakes(mappedMistakes);
+
+      // Fetch profile encrypted key
+      const { data: profileData, error: profileError } = await supabase
+        .from('profiles')
+        .select('encrypted_api_key')
+        .eq('id', userId)
+        .maybeSingle();
+
+      if (profileData && !profileError) {
+        setEncryptedApiKey(profileData.encrypted_api_key || '');
+      }
+    } catch (err) {
+      console.error('Error loading Supabase user data:', err);
+    }
+  };
+
   // Format date helper
   const formatDate = (dateStr: string) => {
     const d = new Date(dateStr);
     return `${d.getFullYear()}.${String(d.getMonth() + 1).padStart(2, '0')}.${String(d.getDate()).padStart(2, '0')}`;
   };
 
-  // Securely encrypt and save the API Key
+  // Securely encrypt and save the API Key to Supabase database profiles table
   const handleSaveApiKey = async (e: React.FormEvent) => {
     e.preventDefault();
     setStatusMessage(null);
@@ -68,20 +126,33 @@ function App() {
       setStatusMessage({ text: 'PIN 踰덊샇??理쒖냼 4?먮━ ?댁긽?댁뼱???⑸땲??', isError: true });
       return;
     }
+    if (!session?.user) return;
 
     try {
       const encrypted = await encryptApiKey(apiKeyInput.trim(), pinInput);
+      
+      const { error: profileError } = await supabase
+        .from('profiles')
+        .upsert({
+          id: session.user.id,
+          encrypted_api_key: encrypted,
+          updated_at: new Date().toISOString()
+        });
+
+      if (profileError) throw profileError;
+
       setEncryptedApiKey(encrypted);
       
-      // Save temporarily in memory for seamless UX during the session
+      // Save temporarily in session storage
       setDecryptedKey(apiKeyInput.trim());
       sessionStorage.setItem('gemini_api_key_temp', apiKeyInput.trim());
 
       setApiKeyInput('');
       setPinInput('');
-      setStatusMessage({ text: 'API Key媛 ?덉쟾?섍쾶 ?뷀샇?붾릺??LocalStorage????λ릺?덉뒿?덈떎.', isError: false });
-    } catch (err) {
-      setStatusMessage({ text: '?뷀샇?????以??ㅻ쪟媛 諛쒖깮?덉뒿?덈떎.', isError: true });
+      setStatusMessage({ text: 'API Key媛 ?뷀샇?붾릺???대씪?곕뱶 ?꾨줈?꾩뿉 ?덉쟾?섍쾶 ?숆린?붾릺?덉뒿?덈떎.', isError: false });
+    } catch (err: any) {
+      console.error(err);
+      setStatusMessage({ text: `???以??ㅻ쪟 諛쒖깮: ${err.message}`, isError: true });
     }
   };
 
@@ -102,7 +173,6 @@ function App() {
       setShowUnlockModal(false);
       setUnlockPin('');
       
-      // Trigger analysis now that key is unlocked
       if (selectedEntry) {
         runAnalysisFlow(selectedEntry, decrypted);
       }
@@ -126,12 +196,24 @@ function App() {
     }
   };
 
-  // Actual Gemini API analysis flow
+  // Gemini API analysis flow & database updating
   const runAnalysisFlow = async (entry: MistakeEntry, apiKey: string) => {
     setIsAnalyzing(true);
     setStatusMessage(null);
     try {
       const result = await analyzeMistakeWithGemini(entry.imageUrl, apiKey);
+      
+      // Update mistake database record
+      const { error: updateError } = await supabase
+        .from('mistakes')
+        .update({
+          title: result.title,
+          analysis: result.analysis
+        })
+        .eq('id', entry.id);
+
+      if (updateError) throw updateError;
+
       const updatedEntry: MistakeEntry = {
         ...entry,
         title: result.title,
@@ -149,31 +231,91 @@ function App() {
     }
   };
 
-  // Handle camera capture and add placeholder entry
-  const handleCameraCapture = (base64Image: string) => {
-    const newEntry: MistakeEntry = {
-      id: Date.now().toString(),
-      title: `?꾩떆 ?ㅼ틪 臾몄젣 #${mistakes.length + 1}`,
-      imageUrl: base64Image,
-      date: new Date().toISOString()
-    };
-    
-    setMistakes(prev => [newEntry, ...prev]);
-    setActiveTab('notes');
-    setSelectedEntry(newEntry); // Open modal for newly captured entry immediately
+  // Handle camera capture, upload to Storage, and insert database record
+  const handleCameraCapture = async (base64Image: string) => {
+    if (!session?.user) return;
+
+    setIsAnalyzing(true);
+    try {
+      const blob = base64ToBlob(base64Image);
+      const fileExt = blob.type.split('/')[1] || 'jpg';
+      const fileName = `${session.user.id}/${Date.now()}.${fileExt}`;
+
+      // 1. Upload to Supabase Storage bucket
+      const { error: uploadError } = await supabase.storage
+        .from('problem-images')
+        .upload(fileName, blob, {
+          contentType: blob.type
+        });
+
+      if (uploadError) throw uploadError;
+
+      // 2. Get Public URL
+      const { data: { publicUrl } } = supabase.storage
+        .from('problem-images')
+        .getPublicUrl(fileName);
+
+      // 3. Save DB Record
+      const newEntryTitle = `?ㅼ틪??臾몄젣 #${mistakes.length + 1}`;
+      const { data: dbEntry, error: insertError } = await supabase
+        .from('mistakes')
+        .insert({
+          user_id: session.user.id,
+          title: newEntryTitle,
+          image_url: publicUrl,
+          analysis: null
+        })
+        .select()
+        .single();
+
+      if (insertError) throw insertError;
+
+      const newEntry: MistakeEntry = {
+        id: dbEntry.id,
+        title: dbEntry.title,
+        imageUrl: dbEntry.image_url,
+        date: dbEntry.date,
+        analysis: undefined
+      };
+      
+      setMistakes(prev => [newEntry, ...prev]);
+      setActiveTab('notes');
+      setSelectedEntry(newEntry); // Open modal immediately
+    } catch (err: any) {
+      console.error(err);
+      alert('?ㅼ틪 ?대?吏 ?대씪?곕뱶 ?낅줈???ㅽ뙣: ' + err.message);
+    } finally {
+      setIsAnalyzing(false);
+    }
   };
 
-  // Delete mistake helper
-  const handleDeleteMistake = (id: string, e: React.MouseEvent) => {
+  // Delete mistake from Supabase & local state
+  const handleDeleteMistake = async (id: string, e: React.MouseEvent) => {
     e.stopPropagation();
     if (confirm('???ㅻ떟 湲곕줉????젣?섏떆寃좎뒿?덇퉴?')) {
-      setMistakes(prev => prev.filter(m => m.id !== id));
-      setSelectedEntry(null);
+      try {
+        const { error } = await supabase
+          .from('mistakes')
+          .delete()
+          .eq('id', id);
+
+        if (error) throw error;
+
+        setMistakes(prev => prev.filter(m => m.id !== id));
+        setSelectedEntry(null);
+      } catch (err: any) {
+        console.error(err);
+        alert('??젣 ?ㅽ뙣: ' + err.message);
+      }
     }
   };
 
-  const handleLogout = () => {
+  const handleLogout = async () => {
+    await supabase.auth.signOut();
     setCurrentUser('');
+    setSession(null);
+    setMistakes([]);
+    setEncryptedApiKey('');
     setDecryptedKey('');
     sessionStorage.removeItem('gemini_api_key_temp');
     setActiveTab('notes');
@@ -250,12 +392,12 @@ function App() {
 
             {/* List of Mistakes */}
             {mistakes.length === 0 ? (
-              <div className="flex flex-col items-center justify-center py-20 text-center border-2 border-dashed border-slate-800 rounded-2xl p-6 bg-slate-900/20">
+              <div className="flex flex-col items-center justify-center py-20 text-center border-2 border-dashed border-slate-800 rounded-2xl p-6 bg-slate-900/20 animate-scale-up">
                 <div className="w-16 h-16 rounded-full bg-slate-900 flex items-center justify-center text-2xl mb-4 text-slate-500">
                   ?뱭
                 </div>
                 <p className="text-slate-300 font-medium">?꾩쭅 ?깅줉???ㅻ떟???놁뒿?덈떎</p>
-                <p className="text-xs text-slate-500 mt-1 max-w-xs">
+                <p className="text-xs text-slate-500 mt-1 max-w-xs leading-relaxed">
                   ?꾨옒 移대찓??踰꾪듉???뚮윭 ?섑븰 臾몄젣瑜?珥ъ쁺?섍퀬 AI??留욎땄 遺꾩꽍??諛쏆븘蹂댁꽭??
                 </p>
               </div>
@@ -335,7 +477,7 @@ function App() {
                 </div>
                 <div>
                   <h3 className="font-bold text-white text-sm">Gemini API 蹂댁븞 ?ㅼ젙</h3>
-                  <p className="text-[10px] text-slate-400">濡쒖뺄?ㅽ넗由ъ? ?????媛뺣젰 ?뷀샇???곸슜</p>
+                  <p className="text-[10px] text-slate-400">?대씪?곕뱶 ?꾨줈???????媛뺣젰 ?뷀샇???곸슜</p>
                 </div>
               </div>
               
@@ -361,7 +503,7 @@ function App() {
                     className="w-full px-4 py-3 rounded-xl bg-slate-950 border border-slate-800 focus:border-indigo-500 focus:ring-1 focus:ring-indigo-500 text-sm text-white placeholder-slate-600 outline-none transition-all"
                   />
                   <p className="text-[9px] text-slate-500 leading-normal">
-                    * ?ㅼ젙??PIN? ??蹂듯샇?????몄쬆 ?섎떒?쇰줈 ?ъ슜?⑸땲?? 釉뚮씪?곗? ??μ냼 ?몄텧 ?쒖뿉????PIN???놁쑝硫?API Key瑜?蹂듦뎄?????놁뒿?덈떎.
+                    * ?ㅼ젙??PIN? ??蹂듯샇?????몄쬆 ?섎떒?쇰줈 ?ъ슜?⑸땲?? ?대씪?곕뱶???곕룞 ??λ릺誘濡?湲곌린媛 諛붾뚯뼱????PIN留??덉쑝硫??몄젣???ㅻ? 遺덈윭? 遺꾩꽍?????덉뒿?덈떎.
                   </p>
                 </div>
 
@@ -386,15 +528,15 @@ function App() {
               <div className="divide-y divide-slate-800/60 text-xs">
                 <div className="flex justify-between py-2.5">
                   <span className="text-slate-400">踰꾩쟾</span>
-                  <span className="text-slate-200 font-medium">v1.0.0 (PWA)</span>
+                  <span className="text-slate-200 font-medium">v1.1.0 (PWA + Supabase)</span>
                 </div>
                 <div className="flex justify-between py-2.5">
-                  <span className="text-slate-400">蹂댁븞 ?섏?</span>
-                  <span className="text-emerald-400 font-medium">AES-GCM ?뷀샇??/span>
+                  <span className="text-slate-400">?몄쬆 諛??곗씠?곕쿋?댁뒪</span>
+                  <span className="text-emerald-400 font-medium">Supabase Cloud</span>
                 </div>
                 <div className="flex justify-between py-2.5">
-                  <span className="text-slate-400">?ㅽ봽?쇱씤 吏??/span>
-                  <span className="text-emerald-400 font-medium">?쒖꽦??(Service Worker)</span>
+                  <span className="text-slate-400">蹂댁븞 ?섏?</span>
+                  <span className="text-emerald-400 font-medium">AES-GCM ?뷀샇??/span>
                 </div>
                 {decryptedKey && (
                   <div className="flex justify-between py-2.5 items-center">
@@ -455,12 +597,12 @@ function App() {
                 <div className="py-12 flex flex-col items-center justify-center space-y-4">
                   <div className="w-12 h-12 border-4 border-indigo-500 border-t-transparent rounded-full animate-spin"></div>
                   <div className="text-center">
-                    <p className="text-sm font-semibold text-white">AI 遺꾩꽍 以?..</p>
-                    <p className="text-xs text-slate-400 mt-1">?섑븰 臾몄젣 ?ㅺ컻?먯쓣 吏꾨떒?섍퀬 ?대━?됱쓣 ?ㅺ퀎?섍퀬 ?덉뒿?덈떎.</p>
+                    <p className="text-sm font-semibold text-white">泥섎━ 以?..</p>
+                    <p className="text-xs text-slate-400 mt-1">?대씪?곕뱶 誘몃뵒??泥섎━ ?먮뒗 AI ?대━??吏꾨떒???묐룞?섍퀬 ?덉뒿?덈떎.</p>
                   </div>
                 </div>
               ) : selectedEntry.analysis ? (
-                <div className="space-y-5">
+                <div className="space-y-5 animate-scale-up">
                   {/* Item 1 */}
                   <div className="space-y-1.5">
                     <h4 className="text-sm font-extrabold text-indigo-400 flex items-center">

commit 44277bab2956647ead0099afd10577bdeee19c27
Author: ReviewNotes Developer <developer@reviewnotes.local>
Date:   Fri Jul 3 04:52:58 2026 +0900

    Configure camera auto-capture ON by default, integrate KaTeX math rendering, and increase formula font size

diff --git a/src/App.tsx b/src/App.tsx
index ba94c7e..ec07965 100644
--- a/src/App.tsx
+++ b/src/App.tsx
@@ -5,6 +5,7 @@ import { CameraScanner } from './components/CameraScanner';
 import { encryptApiKey, decryptApiKey } from './utils/crypto';
 import { analyzeMistakeWithGemini } from './services/gemini';
 import { AuthScreen } from './components/AuthScreen';
+import { LaTeXRenderer } from './components/LaTeXRenderer';
 
 // Initial fallback mock data for visual demonstration
 const INITIAL_MOCKS: MistakeEntry[] = [
@@ -462,38 +463,38 @@ function App() {
                 <div className="space-y-5">
                   {/* Item 1 */}
                   <div className="space-y-1.5">
-                    <h4 className="text-xs font-extrabold text-indigo-400 flex items-center">
+                    <h4 className="text-sm font-extrabold text-indigo-400 flex items-center">
                       <span className="mr-1.5">??/span> [臾몄젣 ???怨쇱젙]
                     </h4>
-                    <div className="bg-slate-950 p-4 rounded-xl border border-slate-800/60 text-xs leading-relaxed text-slate-200">
-                      {selectedEntry.analysis.solvingProcess}
+                    <div className="bg-slate-950 p-4 rounded-2xl border border-slate-800/60">
+                      <LaTeXRenderer text={selectedEntry.analysis.solvingProcess} className="text-sm md:text-base" />
                     </div>
                   </div>
                   {/* Item 2 */}
                   <div className="space-y-1.5">
-                    <h4 className="text-xs font-extrabold text-amber-400 flex items-center">
+                    <h4 className="text-sm font-extrabold text-amber-400 flex items-center">
                       <span className="mr-1.5">?좑툘</span> [?ㅼ닔??吏???곸꽭 遺꾩꽍]
                     </h4>
-                    <div className="bg-slate-950 p-4 rounded-xl border border-slate-800/60 text-xs leading-relaxed text-slate-200">
-                      {selectedEntry.analysis.mistakeDetail}
+                    <div className="bg-slate-950 p-4 rounded-2xl border border-slate-800/60">
+                      <LaTeXRenderer text={selectedEntry.analysis.mistakeDetail} className="text-sm md:text-base" />
                     </div>
                   </div>
                   {/* Item 3 */}
                   <div className="space-y-1.5">
-                    <h4 className="text-xs font-extrabold text-red-400 flex items-center">
+                    <h4 className="text-sm font-extrabold text-red-400 flex items-center">
                       <span className="mr-1.5">?렞</span> [洹쇰낯?곸씤 ?由??댁쑀]
                     </h4>
-                    <div className="bg-slate-950 p-4 rounded-xl border border-slate-800/60 text-xs leading-relaxed text-slate-200">
-                      {selectedEntry.analysis.rootCause}
+                    <div className="bg-slate-950 p-4 rounded-2xl border border-slate-800/60">
+                      <LaTeXRenderer text={selectedEntry.analysis.rootCause} className="text-sm md:text-base" />
                     </div>
                   </div>
                   {/* Item 4 */}
                   <div className="space-y-1.5">
-                    <h4 className="text-xs font-extrabold text-emerald-400 flex items-center">
+                    <h4 className="text-sm font-extrabold text-emerald-400 flex items-center">
                       <span className="mr-1.5">?썳截?/span> [?ν썑 ?щ컻 諛⑹? ?梨?
                     </h4>
-                    <div className="bg-slate-950 p-4 rounded-xl border border-slate-800/60 text-xs leading-relaxed text-slate-200">
-                      {selectedEntry.analysis.actionPlan}
+                    <div className="bg-slate-950 p-4 rounded-2xl border border-slate-800/60">
+                      <LaTeXRenderer text={selectedEntry.analysis.actionPlan} className="text-sm md:text-base" />
                     </div>
                   </div>
                 </div>

commit b73b5cf07dd5c5f3f63388ba934396d78bb3d6b3
Author: ReviewNotes Developer <developer@reviewnotes.local>
Date:   Fri Jul 3 04:47:11 2026 +0900

    Add client-side user authentication, fix camera shutter button layout, and implement 5s auto-capture timer

diff --git a/src/App.tsx b/src/App.tsx
index 879941b..ba94c7e 100644
--- a/src/App.tsx
+++ b/src/App.tsx
@@ -4,6 +4,7 @@ import { useLocalStorage } from './hooks/useLocalStorage';
 import { CameraScanner } from './components/CameraScanner';
 import { encryptApiKey, decryptApiKey } from './utils/crypto';
 import { analyzeMistakeWithGemini } from './services/gemini';
+import { AuthScreen } from './components/AuthScreen';
 
 // Initial fallback mock data for visual demonstration
 const INITIAL_MOCKS: MistakeEntry[] = [
@@ -22,9 +23,16 @@ const INITIAL_MOCKS: MistakeEntry[] = [
 ];
 
 function App() {
+  const [currentUser, setCurrentUser] = useLocalStorage<string>('ai_mistakes_current_user', '');
   const [activeTab, setActiveTab] = useState<ActiveTab>('notes');
-  const [mistakes, setMistakes] = useLocalStorage<MistakeEntry[]>('ai_mistakes_notes', INITIAL_MOCKS);
-  const [encryptedApiKey, setEncryptedApiKey] = useLocalStorage<string>('gemini_api_key_enc', '');
+  const [mistakes, setMistakes] = useLocalStorage<MistakeEntry[]>(
+    currentUser ? `ai_mistakes_notes_${currentUser}` : 'ai_mistakes_notes',
+    INITIAL_MOCKS
+  );
+  const [encryptedApiKey, setEncryptedApiKey] = useLocalStorage<string>(
+    currentUser ? `gemini_api_key_enc_${currentUser}` : 'gemini_api_key_enc',
+    ''
+  );
   
   // App-level state for temporary decrypted API key (lasts for the session)
   const [decryptedKey, setDecryptedKey] = useState<string>(() => {
@@ -163,6 +171,17 @@ function App() {
     }
   };
 
+  const handleLogout = () => {
+    setCurrentUser('');
+    setDecryptedKey('');
+    sessionStorage.removeItem('gemini_api_key_temp');
+    setActiveTab('notes');
+  };
+
+  if (!currentUser) {
+    return <AuthScreen onLogin={(username) => setCurrentUser(username)} />;
+  }
+
   return (
     <div className="h-full flex flex-col bg-slate-950 text-slate-100 select-none">
       
@@ -177,6 +196,16 @@ function App() {
           </h1>
         </div>
         <div className="flex items-center space-x-2">
+          <span className="text-[10px] text-slate-400 bg-slate-800/80 px-2 py-0.5 rounded-full border border-slate-700 font-bold max-w-[80px] truncate">
+            ?뫀 {currentUser}
+          </span>
+          <button 
+            onClick={handleLogout}
+            className="text-[10px] text-red-400 hover:text-red-300 font-bold bg-red-950/20 px-2.5 py-0.5 rounded-full border border-red-900/30 transition-colors"
+          >
+            濡쒓렇?꾩썐
+          </button>
+          
           {import.meta.env.VITE_GEMINI_API_KEY ? (
             <span className="flex items-center space-x-1 text-indigo-400 bg-indigo-500/10 px-2 py-0.5 rounded-full text-[10px] border border-indigo-500/30">
               <span className="w-1.5 h-1.5 rounded-full bg-indigo-400"></span>

commit 9188209ecd1db92d139e85d6cb2b3b49e5a3c0c5
Author: ReviewNotes Developer <developer@reviewnotes.local>
Date:   Fri Jul 3 04:26:45 2026 +0900

    Initial commit

diff --git a/src/App.tsx b/src/App.tsx
new file mode 100644
index 0000000..879941b
--- /dev/null
+++ b/src/App.tsx
@@ -0,0 +1,596 @@
+import { useState } from 'react';
+import type { ActiveTab, MistakeEntry } from './types';
+import { useLocalStorage } from './hooks/useLocalStorage';
+import { CameraScanner } from './components/CameraScanner';
+import { encryptApiKey, decryptApiKey } from './utils/crypto';
+import { analyzeMistakeWithGemini } from './services/gemini';
+
+// Initial fallback mock data for visual demonstration
+const INITIAL_MOCKS: MistakeEntry[] = [
+  {
+    id: '1',
+    title: '?댁감諛⑹젙??洹쇱쓽 怨듭떇 遺???ㅼ닔',
+    imageUrl: 'https://images.unsplash.com/photo-1635070041078-e363dbe005cb?w=400&auto=format&fit=crop&q=60',
+    date: '2026-07-02T10:30:00Z',
+    analysis: {
+      solvingProcess: 'x짼 - 4x - 5 = 0??洹쇱쓣 援ы븷 ??洹쇱쓽 怨듭떇???곸슜?섏뿬 x = [4 짹 ??16 - 4(1)(-5))] / 2 = [4 짹 ??6] / 2 濡?援ы븯??怨쇱젙.',
+      mistakeDetail: '遺꾩옄 遺遺꾩뿉??2a???대떦?섎뒗 遺꾨え瑜??곸슜?덉쑝?? 遺꾩옄??-b瑜???낇븷 ??b=-4 ?대?濡?-b = 4媛 ?섏뼱???섎뒗???ㅼ닔濡?-4濡???낇븯??理쒖쥌 洹쇱쓽 遺?멸? 諛섎?濡??섏샂.',
+      rootCause: '洹쇱쓽 怨듭떇 ?붽린媛 遺덉븞?뺥븯嫄곕굹, ?뚯닔 湲고샇(-)媛 ?곗냽?댁꽌 ?섏삱 ?? -(-4) ) 愿꾪샇瑜??뚯썙 怨꾩궛?섏? ?딅뒗 ?섏걶 ?듦????덉쓬.',
+      actionPlan: '?뚯닔 怨꾩닔瑜?怨듭떇????낇븷 ?뚮뒗 ??긽 愿꾪샇瑜?移섍퀬 1珥덇컙 遺?몃? ?ъ젏寃?섎뒗 ?듦????ㅼ씠怨? ?좎궗???댁감諛⑹젙?????臾몄젣 5媛쒕? 異붽?濡??硫??ㅻ떟 寃???섑뻾.'
+    }
+  }
+];
+
+function App() {
+  const [activeTab, setActiveTab] = useState<ActiveTab>('notes');
+  const [mistakes, setMistakes] = useLocalStorage<MistakeEntry[]>('ai_mistakes_notes', INITIAL_MOCKS);
+  const [encryptedApiKey, setEncryptedApiKey] = useLocalStorage<string>('gemini_api_key_enc', '');
+  
+  // App-level state for temporary decrypted API key (lasts for the session)
+  const [decryptedKey, setDecryptedKey] = useState<string>(() => {
+    return sessionStorage.getItem('gemini_api_key_temp') || '';
+  });
+
+  // Modal and Form States
+  const [selectedEntry, setSelectedEntry] = useState<MistakeEntry | null>(null);
+  const [apiKeyInput, setApiKeyInput] = useState('');
+  const [pinInput, setPinInput] = useState('');
+  const [unlockPin, setUnlockPin] = useState('');
+  const [showUnlockModal, setShowUnlockModal] = useState(false);
+  const [statusMessage, setStatusMessage] = useState<{ text: string; isError: boolean } | null>(null);
+  const [isAnalyzing, setIsAnalyzing] = useState(false);
+
+  // Format date helper
+  const formatDate = (dateStr: string) => {
+    const d = new Date(dateStr);
+    return `${d.getFullYear()}.${String(d.getMonth() + 1).padStart(2, '0')}.${String(d.getDate()).padStart(2, '0')}`;
+  };
+
+  // Securely encrypt and save the API Key
+  const handleSaveApiKey = async (e: React.FormEvent) => {
+    e.preventDefault();
+    setStatusMessage(null);
+
+    if (!apiKeyInput.trim()) {
+      setStatusMessage({ text: 'API Key瑜??낅젰??二쇱꽭??', isError: true });
+      return;
+    }
+    if (pinInput.length < 4) {
+      setStatusMessage({ text: 'PIN 踰덊샇??理쒖냼 4?먮━ ?댁긽?댁뼱???⑸땲??', isError: true });
+      return;
+    }
+
+    try {
+      const encrypted = await encryptApiKey(apiKeyInput.trim(), pinInput);
+      setEncryptedApiKey(encrypted);
+      
+      // Save temporarily in memory for seamless UX during the session
+      setDecryptedKey(apiKeyInput.trim());
+      sessionStorage.setItem('gemini_api_key_temp', apiKeyInput.trim());
+
+      setApiKeyInput('');
+      setPinInput('');
+      setStatusMessage({ text: 'API Key媛 ?덉쟾?섍쾶 ?뷀샇?붾릺??LocalStorage????λ릺?덉뒿?덈떎.', isError: false });
+    } catch (err) {
+      setStatusMessage({ text: '?뷀샇?????以??ㅻ쪟媛 諛쒖깮?덉뒿?덈떎.', isError: true });
+    }
+  };
+
+  // Decrypt Key handler (used when analyzing)
+  const handleUnlockKey = async (e: React.FormEvent) => {
+    e.preventDefault();
+    setStatusMessage(null);
+
+    if (!encryptedApiKey) {
+      setStatusMessage({ text: '??λ맂 API Key媛 ?놁뒿?덈떎. ?ㅼ젙 ??뿉??癒쇱? ?깅줉??二쇱꽭??', isError: true });
+      return;
+    }
+
+    try {
+      const decrypted = await decryptApiKey(encryptedApiKey, unlockPin);
+      setDecryptedKey(decrypted);
+      sessionStorage.setItem('gemini_api_key_temp', decrypted);
+      setShowUnlockModal(false);
+      setUnlockPin('');
+      
+      // Trigger analysis now that key is unlocked
+      if (selectedEntry) {
+        runAnalysisFlow(selectedEntry, decrypted);
+      }
+    } catch (err: any) {
+      setStatusMessage({ text: err.message || '蹂듯샇???ㅽ뙣', isError: true });
+    }
+  };
+
+  // Start analysis trigger
+  const handleStartAnalysis = (entry: MistakeEntry) => {
+    const envKey = import.meta.env.VITE_GEMINI_API_KEY || '';
+    const activeKey = decryptedKey || envKey;
+
+    if (activeKey) {
+      runAnalysisFlow(entry, activeKey);
+    } else if (encryptedApiKey) {
+      setShowUnlockModal(true);
+    } else {
+      setActiveTab('settings');
+      alert('AI ?ㅻ떟 遺꾩꽍??吏꾪뻾?섎젮硫?癒쇱? ?ㅼ젙 ??뿉??Gemini API Key瑜??깅줉?섍굅??.env ?뚯씪??VITE_GEMINI_API_KEY瑜??ㅼ젙?댁빞 ?⑸땲??');
+    }
+  };
+
+  // Actual Gemini API analysis flow
+  const runAnalysisFlow = async (entry: MistakeEntry, apiKey: string) => {
+    setIsAnalyzing(true);
+    setStatusMessage(null);
+    try {
+      const result = await analyzeMistakeWithGemini(entry.imageUrl, apiKey);
+      const updatedEntry: MistakeEntry = {
+        ...entry,
+        title: result.title,
+        analysis: result.analysis
+      };
+
+      setMistakes(prev => prev.map(m => m.id === entry.id ? updatedEntry : m));
+      setSelectedEntry(updatedEntry);
+    } catch (err: any) {
+      console.error(err);
+      setStatusMessage({ text: err.message || 'AI 遺꾩꽍 ?ㅽ뻾 以??ㅻ쪟媛 諛쒖깮?덉뒿?덈떎.', isError: true });
+      alert(err.message || 'AI 遺꾩꽍 ?ㅽ뻾 以??ㅻ쪟媛 諛쒖깮?덉뒿?덈떎.');
+    } finally {
+      setIsAnalyzing(false);
+    }
+  };
+
+  // Handle camera capture and add placeholder entry
+  const handleCameraCapture = (base64Image: string) => {
+    const newEntry: MistakeEntry = {
+      id: Date.now().toString(),
+      title: `?꾩떆 ?ㅼ틪 臾몄젣 #${mistakes.length + 1}`,
+      imageUrl: base64Image,
+      date: new Date().toISOString()
+    };
+    
+    setMistakes(prev => [newEntry, ...prev]);
+    setActiveTab('notes');
+    setSelectedEntry(newEntry); // Open modal for newly captured entry immediately
+  };
+
+  // Delete mistake helper
+  const handleDeleteMistake = (id: string, e: React.MouseEvent) => {
+    e.stopPropagation();
+    if (confirm('???ㅻ떟 湲곕줉????젣?섏떆寃좎뒿?덇퉴?')) {
+      setMistakes(prev => prev.filter(m => m.id !== id));
+      setSelectedEntry(null);
+    }
+  };
+
+  return (
+    <div className="h-full flex flex-col bg-slate-950 text-slate-100 select-none">
+      
+      {/* Top Header */}
+      <header className="safe-top flex-none border-b border-slate-800 bg-slate-900/90 backdrop-blur-md px-6 py-4 flex items-center justify-between sticky top-0 z-30">
+        <div className="flex items-center space-x-2">
+          <div className="w-8 h-8 rounded-lg bg-gradient-to-tr from-indigo-600 to-emerald-400 flex items-center justify-center font-bold text-white shadow-lg shadow-indigo-500/20">
+            ??+          </div>
+          <h1 className="text-lg font-bold bg-gradient-to-r from-white to-slate-300 bg-clip-text text-transparent">
+            AI ?섑븰 ?ㅻ떟?명듃
+          </h1>
+        </div>
+        <div className="flex items-center space-x-2">
+          {import.meta.env.VITE_GEMINI_API_KEY ? (
+            <span className="flex items-center space-x-1 text-indigo-400 bg-indigo-500/10 px-2 py-0.5 rounded-full text-[10px] border border-indigo-500/30">
+              <span className="w-1.5 h-1.5 rounded-full bg-indigo-400"></span>
+              <span>Env Key ?쒖꽦?붾맖</span>
+            </span>
+          ) : decryptedKey ? (
+            <span className="flex items-center space-x-1 text-emerald-400 bg-emerald-500/10 px-2 py-0.5 rounded-full text-[10px] border border-emerald-500/30">
+              <span className="w-1.5 h-1.5 rounded-full bg-emerald-400"></span>
+              <span>蹂댁븞 ?좉툑?댁젣??/span>
+            </span>
+          ) : encryptedApiKey ? (
+            <span className="flex items-center space-x-1 text-amber-400 bg-amber-500/10 px-2 py-0.5 rounded-full text-[10px] border border-amber-500/30">
+              <span className="w-1.5 h-1.5 rounded-full bg-amber-400"></span>
+              <span>蹂댁븞 ?뷀샇?붾맖</span>
+            </span>
+          ) : (
+            <span className="text-[10px] text-slate-500 border border-slate-800 px-2 py-0.5 rounded-full">
+              API Key 誘몃벑濡?+            </span>
+          )}
+        </div>
+      </header>
+
+      {/* Main Content Area */}
+      <main className="flex-1 overflow-y-auto px-4 py-6 pb-24">
+        
+        {activeTab === 'notes' && (
+          <div className="space-y-6">
+            <div className="flex items-center justify-between">
+              <div>
+                <h2 className="text-xl font-bold text-white">?섏쓽 ?ㅻ떟?명듃</h2>
+                <p className="text-xs text-slate-400 mt-1">珥?{mistakes.length}媛쒖쓽 遺꾩꽍??痍⑥빟?먯씠 ?덉뒿?덈떎.</p>
+              </div>
+              <button 
+                onClick={() => setActiveTab('camera')}
+                className="px-3.5 py-1.5 rounded-full text-xs font-semibold bg-indigo-600 hover:bg-indigo-500 active:scale-95 text-white transition-all shadow-md shadow-indigo-600/20"
+              >
+                + ???ㅻ떟 異붽?
+              </button>
+            </div>
+
+            {/* List of Mistakes */}
+            {mistakes.length === 0 ? (
+              <div className="flex flex-col items-center justify-center py-20 text-center border-2 border-dashed border-slate-800 rounded-2xl p-6 bg-slate-900/20">
+                <div className="w-16 h-16 rounded-full bg-slate-900 flex items-center justify-center text-2xl mb-4 text-slate-500">
+                  ?뱭
+                </div>
+                <p className="text-slate-300 font-medium">?꾩쭅 ?깅줉???ㅻ떟???놁뒿?덈떎</p>
+                <p className="text-xs text-slate-500 mt-1 max-w-xs">
+                  ?꾨옒 移대찓??踰꾪듉???뚮윭 ?섑븰 臾몄젣瑜?珥ъ쁺?섍퀬 AI??留욎땄 遺꾩꽍??諛쏆븘蹂댁꽭??
+                </p>
+              </div>
+            ) : (
+              <div className="grid gap-4 sm:grid-cols-2">
+                {mistakes.map((entry) => (
+                  <div 
+                    key={entry.id}
+                    onClick={() => setSelectedEntry(entry)}
+                    className="group bg-slate-900/50 hover:bg-slate-900 border border-slate-800 hover:border-indigo-500/40 rounded-2xl overflow-hidden cursor-pointer transition-all duration-300 transform active:scale-[0.98]"
+                  >
+                    <div className="aspect-[16/9] w-full bg-slate-950 relative overflow-hidden flex items-center justify-center">
+                      <img 
+                        src={entry.imageUrl} 
+                        alt={entry.title}
+                        className="w-full h-full object-cover opacity-80 group-hover:scale-105 transition-transform duration-500" 
+                      />
+                      <button
+                        onClick={(e) => handleDeleteMistake(entry.id, e)}
+                        className="absolute top-2 left-2 w-7 h-7 rounded-full bg-black/60 hover:bg-red-600 flex items-center justify-center text-white text-xs opacity-0 group-hover:opacity-100 transition-opacity"
+                      >
+                        ?뿊截?+                      </button>
+                      <div className="absolute top-2 right-2 px-2.5 py-0.5 rounded-full text-[10px] font-semibold bg-slate-950/80 backdrop-blur-sm border border-slate-800 text-slate-300">
+                        {formatDate(entry.date)}
+                      </div>
+                    </div>
+                    <div className="p-4">
+                      <h3 className="font-bold text-white line-clamp-1 group-hover:text-indigo-400 transition-colors">
+                        {entry.title}
+                      </h3>
+                      {entry.analysis ? (
+                        <p className="text-xs text-slate-400 mt-1.5 line-clamp-2 leading-relaxed">
+                          ?뵇 {entry.analysis.mistakeDetail}
+                        </p>
+                      ) : (
+                        <p className="text-xs text-amber-400 mt-1.5 font-medium flex items-center">
+                          <span className="w-1.5 h-1.5 rounded-full bg-amber-400 mr-1.5 animate-pulse"></span>
+                          AI 遺꾩꽍 誘몄셿猷?(?ㅼ틪??
+                        </p>
+                      )}
+                      <div className="flex items-center justify-between mt-4 pt-3 border-t border-slate-800/60">
+                        <span className="text-[10px] text-slate-500">
+                          {entry.analysis ? 'AI ?쇰뱶諛??꾨즺' : '遺꾩꽍 ?湲?以?}
+                        </span>
+                        <span className="text-xs text-indigo-400 font-semibold group-hover:translate-x-1 transition-transform inline-flex items-center">
+                          {entry.analysis ? '?곸꽭蹂닿린' : '遺꾩꽍?섍린'} &rarr;
+                        </span>
+                      </div>
+                    </div>
+                  </div>
+                ))}
+              </div>
+            )}
+          </div>
+        )}
+
+        {activeTab === 'camera' && (
+          <CameraScanner 
+            onCapture={handleCameraCapture}
+            onClose={() => setActiveTab('notes')}
+          />
+        )}
+
+        {activeTab === 'settings' && (
+          <div className="space-y-6 max-w-lg mx-auto">
+            <div>
+              <h2 className="text-xl font-bold text-white">???ㅼ젙</h2>
+              <p className="text-xs text-slate-400 mt-1">API 蹂댁븞 ?ㅼ젙 諛????섍꼍?ㅼ젙</p>
+            </div>
+
+            {/* API Security Card */}
+            <form onSubmit={handleSaveApiKey} className="bg-slate-900/40 border border-slate-800 rounded-2xl p-5 space-y-4">
+              <div className="flex items-center space-x-3">
+                <div className="w-10 h-10 rounded-xl bg-indigo-950 border border-indigo-800 flex items-center justify-center text-xl text-indigo-400">
+                  ?뵎
+                </div>
+                <div>
+                  <h3 className="font-bold text-white text-sm">Gemini API 蹂댁븞 ?ㅼ젙</h3>
+                  <p className="text-[10px] text-slate-400">濡쒖뺄?ㅽ넗由ъ? ?????媛뺣젰 ?뷀샇???곸슜</p>
+                </div>
+              </div>
+              
+              <div className="space-y-3 pt-2">
+                <div className="space-y-1">
+                  <label className="text-xs text-slate-400 font-medium">Gemini API Key</label>
+                  <input 
+                    type="password" 
+                    value={apiKeyInput}
+                    onChange={(e) => setApiKeyInput(e.target.value)}
+                    placeholder="AIzaSy..." 
+                    className="w-full px-4 py-3 rounded-xl bg-slate-950 border border-slate-800 focus:border-indigo-500 focus:ring-1 focus:ring-indigo-500 text-sm text-white placeholder-slate-600 outline-none transition-all"
+                  />
+                </div>
+                <div className="space-y-1">
+                  <label className="text-xs text-slate-400 font-medium">??蹂듯샇??PIN (4~6?먮━ ?レ옄)</label>
+                  <input 
+                    type="password" 
+                    maxLength={6}
+                    value={pinInput}
+                    onChange={(e) => setPinInput(e.target.value.replace(/[^0-9]/g, ''))}
+                    placeholder="?レ옄 4~6?먮━ ?낅젰" 
+                    className="w-full px-4 py-3 rounded-xl bg-slate-950 border border-slate-800 focus:border-indigo-500 focus:ring-1 focus:ring-indigo-500 text-sm text-white placeholder-slate-600 outline-none transition-all"
+                  />
+                  <p className="text-[9px] text-slate-500 leading-normal">
+                    * ?ㅼ젙??PIN? ??蹂듯샇?????몄쬆 ?섎떒?쇰줈 ?ъ슜?⑸땲?? 釉뚮씪?곗? ??μ냼 ?몄텧 ?쒖뿉????PIN???놁쑝硫?API Key瑜?蹂듦뎄?????놁뒿?덈떎.
+                  </p>
+                </div>
+
+                {statusMessage && (
+                  <div className={`p-3 rounded-lg text-xs ${statusMessage.isError ? 'bg-red-500/10 text-red-400 border border-red-500/20' : 'bg-emerald-500/10 text-emerald-400 border border-emerald-500/20'}`}>
+                    {statusMessage.text}
+                  </div>
+                )}
+
+                <button 
+                  type="submit"
+                  className="w-full py-3 mt-2 rounded-xl bg-indigo-600 hover:bg-indigo-500 active:scale-95 transition-all text-xs font-bold text-white shadow-md shadow-indigo-600/10"
+                >
+                  蹂댁븞 ??ν븯湲?+                </button>
+              </div>
+            </form>
+
+            {/* App Info Card */}
+            <div className="bg-slate-900/40 border border-slate-800 rounded-2xl p-5 space-y-3">
+              <h3 className="font-bold text-white text-sm">?좏뵆由ъ??댁뀡 ?뺣낫</h3>
+              <div className="divide-y divide-slate-800/60 text-xs">
+                <div className="flex justify-between py-2.5">
+                  <span className="text-slate-400">踰꾩쟾</span>
+                  <span className="text-slate-200 font-medium">v1.0.0 (PWA)</span>
+                </div>
+                <div className="flex justify-between py-2.5">
+                  <span className="text-slate-400">蹂댁븞 ?섏?</span>
+                  <span className="text-emerald-400 font-medium">AES-GCM ?뷀샇??/span>
+                </div>
+                <div className="flex justify-between py-2.5">
+                  <span className="text-slate-400">?ㅽ봽?쇱씤 吏??/span>
+                  <span className="text-emerald-400 font-medium">?쒖꽦??(Service Worker)</span>
+                </div>
+                {decryptedKey && (
+                  <div className="flex justify-between py-2.5 items-center">
+                    <span className="text-slate-400">?몄뀡 ?곹깭</span>
+                    <button 
+                      onClick={() => {
+                        setDecryptedKey('');
+                        sessionStorage.removeItem('gemini_api_key_temp');
+                        alert('API Key ?몄뀡??留뚮즺?섏뿀?듬땲?? ?ㅼ떆 ?덉쟾?섍쾶 ?좉꼈?듬땲??');
+                      }}
+                      className="px-2 py-1 bg-red-950 border border-red-800 text-red-400 rounded-lg text-[10px]"
+                    >
+                      ?몄뀡 硫붾え由??좉툑
+                    </button>
+                  </div>
+                )}
+              </div>
+            </div>
+          </div>
+        )}
+
+      </main>
+
+      {/* Selected Entry Detail Modal */}
+      {selectedEntry && (
+        <div className="fixed inset-0 z-50 flex items-end justify-center bg-black/70 backdrop-blur-sm p-0 sm:p-4">
+          <div className="w-full max-w-xl bg-slate-900 border-t sm:border border-slate-800 rounded-t-3xl sm:rounded-3xl max-h-[90vh] flex flex-col shadow-2xl overflow-hidden animate-slide-up">
+            
+            {/* Modal Header */}
+            <div className="px-6 py-4 border-b border-slate-800/80 flex items-center justify-between bg-slate-900/80 sticky top-0">
+              <div className="pr-4 flex-1">
+                <span className="text-[10px] text-slate-500 font-bold block uppercase tracking-wider">{formatDate(selectedEntry.date)}</span>
+                <h3 className="font-bold text-white text-base line-clamp-1">{selectedEntry.title}</h3>
+              </div>
+              <button 
+                onClick={() => setSelectedEntry(null)}
+                className="w-8 h-8 rounded-full bg-slate-800 hover:bg-slate-700 active:scale-90 flex items-center justify-center text-slate-400 text-lg transition-all flex-none"
+              >
+                ??+              </button>
+            </div>
+
+            {/* Modal Body */}
+            <div className="flex-1 overflow-y-auto p-6 space-y-6">
+              
+              {/* Problem Image Preview */}
+              <div className="aspect-[16/9] w-full rounded-2xl overflow-hidden bg-slate-950 border border-slate-800 flex items-center justify-center relative">
+                <img src={selectedEntry.imageUrl} alt={selectedEntry.title} className="w-full h-full object-cover" />
+                <button
+                  onClick={(e) => handleDeleteMistake(selectedEntry.id, e)}
+                  className="absolute bottom-3 right-3 px-3 py-1.5 rounded-xl bg-red-600/90 hover:bg-red-600 text-white text-xs font-semibold shadow-lg backdrop-blur-sm transition-all"
+                >
+                  湲곕줉 ??젣
+                </button>
+              </div>
+
+              {isAnalyzing ? (
+                <div className="py-12 flex flex-col items-center justify-center space-y-4">
+                  <div className="w-12 h-12 border-4 border-indigo-500 border-t-transparent rounded-full animate-spin"></div>
+                  <div className="text-center">
+                    <p className="text-sm font-semibold text-white">AI 遺꾩꽍 以?..</p>
+                    <p className="text-xs text-slate-400 mt-1">?섑븰 臾몄젣 ?ㅺ컻?먯쓣 吏꾨떒?섍퀬 ?대━?됱쓣 ?ㅺ퀎?섍퀬 ?덉뒿?덈떎.</p>
+                  </div>
+                </div>
+              ) : selectedEntry.analysis ? (
+                <div className="space-y-5">
+                  {/* Item 1 */}
+                  <div className="space-y-1.5">
+                    <h4 className="text-xs font-extrabold text-indigo-400 flex items-center">
+                      <span className="mr-1.5">??/span> [臾몄젣 ???怨쇱젙]
+                    </h4>
+                    <div className="bg-slate-950 p-4 rounded-xl border border-slate-800/60 text-xs leading-relaxed text-slate-200">
+                      {selectedEntry.analysis.solvingProcess}
+                    </div>
+                  </div>
+                  {/* Item 2 */}
+                  <div className="space-y-1.5">
+                    <h4 className="text-xs font-extrabold text-amber-400 flex items-center">
+                      <span className="mr-1.5">?좑툘</span> [?ㅼ닔??吏???곸꽭 遺꾩꽍]
+                    </h4>
+                    <div className="bg-slate-950 p-4 rounded-xl border border-slate-800/60 text-xs leading-relaxed text-slate-200">
+                      {selectedEntry.analysis.mistakeDetail}
+                    </div>
+                  </div>
+                  {/* Item 3 */}
+                  <div className="space-y-1.5">
+                    <h4 className="text-xs font-extrabold text-red-400 flex items-center">
+                      <span className="mr-1.5">?렞</span> [洹쇰낯?곸씤 ?由??댁쑀]
+                    </h4>
+                    <div className="bg-slate-950 p-4 rounded-xl border border-slate-800/60 text-xs leading-relaxed text-slate-200">
+                      {selectedEntry.analysis.rootCause}
+                    </div>
+                  </div>
+                  {/* Item 4 */}
+                  <div className="space-y-1.5">
+                    <h4 className="text-xs font-extrabold text-emerald-400 flex items-center">
+                      <span className="mr-1.5">?썳截?/span> [?ν썑 ?щ컻 諛⑹? ?梨?
+                    </h4>
+                    <div className="bg-slate-950 p-4 rounded-xl border border-slate-800/60 text-xs leading-relaxed text-slate-200">
+                      {selectedEntry.analysis.actionPlan}
+                    </div>
+                  </div>
+                </div>
+              ) : (
+                <div className="py-8 bg-slate-950/60 rounded-2xl border border-slate-800 p-6 text-center space-y-4">
+                  <div className="text-3xl">?쨼</div>
+                  <div className="space-y-1">
+                    <p className="text-sm font-bold text-white">AI ?섑븰 ?대━??吏꾨떒</p>
+                    <p className="text-xs text-slate-400 leading-relaxed max-w-xs mx-auto">
+                      ?꾩쭅 ?ㅻ떟 ?먯씤??遺꾩꽍?섏? ?딆븯?듬땲?? ?깅줉??API Key瑜??듯빐 4媛吏 ?듭떖 ?먯씤 吏꾨떒??吏꾪뻾?⑸땲??
+                    </p>
+                  </div>
+                  <button 
+                    onClick={() => handleStartAnalysis(selectedEntry)}
+                    className="px-6 py-2.5 rounded-full bg-gradient-to-r from-indigo-600 to-indigo-500 active:scale-95 transition-all text-xs font-bold text-white shadow-md shadow-indigo-600/20"
+                  >
+                    AI 遺꾩꽍 ?쒖옉?섍린
+                  </button>
+                </div>
+              )}
+            </div>
+
+            {/* Modal Footer */}
+            <div className="p-4 border-t border-slate-800/80 bg-slate-950/60 flex space-x-3">
+              <button 
+                onClick={() => setSelectedEntry(null)}
+                className="flex-1 py-3 rounded-xl border border-slate-800 hover:border-slate-700 active:scale-95 transition-all text-xs font-semibold text-slate-300"
+              >
+                ?リ린
+              </button>
+            </div>
+
+          </div>
+        </div>
+      )}
+
+      {/* Unlock API Key PIN Modal */}
+      {showUnlockModal && (
+        <div className="fixed inset-0 z-50 flex items-center justify-center bg-black/80 backdrop-blur-sm p-4">
+          <form 
+            onSubmit={handleUnlockKey}
+            className="w-full max-w-sm bg-slate-900 border border-slate-800 rounded-3xl p-6 space-y-4 shadow-2xl animate-scale-up"
+          >
+            <div className="text-center space-y-2">
+              <div className="w-12 h-12 rounded-full bg-amber-500/10 border border-amber-500/30 text-2xl flex items-center justify-center mx-auto text-amber-400">
+                ?뵏
+              </div>
+              <h3 className="font-bold text-white text-base">蹂댁븞 蹂듯샇??/h3>
+              <p className="text-xs text-slate-400 max-w-xs mx-auto leading-relaxed">
+                ?뷀샇?붾맂 API Key瑜?濡쒕뱶?섍린 ?꾪빐 蹂댁븞 PIN 肄붾뱶瑜??낅젰??二쇱꽭??
+              </p>
+            </div>
+
+            <div className="space-y-3 pt-2">
+              <input 
+                type="password" 
+                maxLength={6}
+                value={unlockPin}
+                onChange={(e) => setUnlockPin(e.target.value.replace(/[^0-9]/g, ''))}
+                placeholder="?レ옄 4~6?먮━ PIN ?낅젰" 
+                className="w-full px-4 py-3 text-center tracking-widest text-lg rounded-xl bg-slate-950 border border-slate-800 focus:border-indigo-500 focus:ring-1 focus:ring-indigo-500 text-white placeholder-slate-600 outline-none transition-all"
+              />
+
+              {statusMessage && (
+                <div className="p-3 rounded-lg text-xs bg-red-500/10 text-red-400 border border-red-500/20">
+                  {statusMessage.text}
+                </div>
+              )}
+
+              <div className="flex space-x-3 pt-2">
+                <button 
+                  type="button"
+                  onClick={() => {
+                    setShowUnlockModal(false);
+                    setUnlockPin('');
+                    setStatusMessage(null);
+                  }}
+                  className="flex-1 py-3 rounded-xl border border-slate-800 hover:bg-slate-800 active:scale-95 text-xs font-semibold text-slate-300"
+                >
+                  痍⑥냼
+                </button>
+                <button 
+                  type="submit"
+                  className="flex-1 py-3 rounded-xl bg-indigo-600 hover:bg-indigo-500 active:scale-95 text-xs font-bold text-white shadow-md shadow-indigo-600/20"
+                >
+                  ?좉툑 ?댁젣
+                </button>
+              </div>
+            </div>
+          </form>
+        </div>
+      )}
+
+      {/* Floating Glassmorphic Bottom Navigation Bar */}
+      <nav className="fixed bottom-4 left-4 right-4 z-40 h-16 rounded-2xl border border-slate-800/80 bg-slate-900/75 backdrop-blur-lg flex items-center justify-around px-4 shadow-xl shadow-black/40">
+        
+        {/* Tab 1: Notes List */}
+        <button 
+          onClick={() => setActiveTab('notes')}
+          className={`flex flex-col items-center justify-center w-12 h-12 rounded-xl transition-all ${activeTab === 'notes' ? 'text-indigo-400 scale-105' : 'text-slate-500 hover:text-slate-300'}`}
+        >
+          <span className="text-lg">?뱭</span>
+          <span className="text-[10px] font-bold mt-0.5">?ㅻ떟?명듃</span>
+        </button>
+
+        {/* Tab 2: Floating Camera Trigger */}
+        <button 
+          onClick={() => setActiveTab('camera')}
+          className={`flex items-center justify-center w-14 h-14 rounded-full transition-all -translate-y-4 shadow-lg ${activeTab === 'camera' ? 'bg-gradient-to-tr from-indigo-600 to-indigo-500 text-white scale-110 shadow-indigo-600/40 ring-4 ring-slate-950' : 'bg-slate-800 text-slate-300 hover:bg-slate-750 shadow-black/50 ring-4 ring-slate-950 hover:scale-105'}`}
+        >
+          <span className="text-xl">?벜</span>
+        </button>
+
+        {/* Tab 3: Settings */}
+        <button 
+          onClick={() => setActiveTab('settings')}
+          className={`flex flex-col items-center justify-center w-12 h-12 rounded-xl transition-all ${activeTab === 'settings' ? 'text-indigo-400 scale-105' : 'text-slate-500 hover:text-slate-300'}`}
+        >
+          <span className="text-lg">?숋툘</span>
+          <span className="text-[10px] font-bold mt-0.5">?ㅼ젙</span>
+        </button>
+
+      </nav>
+
+    </div>
+  );
+}
+
+export default App;
